"""Property-based (Hypothesis) tests for tests/governance.

Mirrors ``tests/identity/`` discipline — the property-based slice lives in a
``property/`` subdirectory so the @given fixtures and Hypothesis settings
stay cleanly separated from the example-based contract tests.
"""
