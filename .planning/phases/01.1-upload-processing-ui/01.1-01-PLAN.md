---
phase: 01.1-upload-processing-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/models/__init__.py
  - api/models/processing.py
  - api/routes/corpus.py
  - api/routes/upload.py
  - api/routes/__init__.py
  - api/main.py
  - pyproject.toml
  - tests/test_corpus_api.py
  - tests/test_upload_api.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can create a named corpus via POST /api/v1/corpora"
    - "User can list all corpora via GET /api/v1/corpora"
    - "User can delete a corpus via DELETE /api/v1/corpora/{id}"
    - "User can upload files to a corpus via POST /api/v1/corpus/{id}/upload"
    - "Uploaded ZIP archives are extracted server-side with path traversal protection"
    - "Corpus ID is a slugified version of the user-provided name"
  artifacts:
    - path: "api/models/processing.py"
      provides: "CorpusInfo, ProcessingJob, ProcessingStatus pydantic models"
      exports: ["CorpusInfo", "ProcessingJob", "ProcessingStatus", "ActivityEntry"]
    - path: "api/routes/corpus.py"
      provides: "Corpus CRUD endpoints"
      exports: ["router"]
    - path: "api/routes/upload.py"
      provides: "File upload endpoint with zip extraction"
      exports: ["router"]
    - path: "tests/test_corpus_api.py"
      provides: "Integration tests for corpus CRUD"
    - path: "tests/test_upload_api.py"
      provides: "Integration tests for file upload including zip safety"
  key_links:
    - from: "api/routes/corpus.py"
      to: "api/models/processing.py"
      via: "import CorpusInfo"
      pattern: "from api\\.models\\.processing import"
    - from: "api/main.py"
      to: "api/routes/corpus.py"
      via: "include_router"
      pattern: "app\\.include_router\\(corpus\\.router"
    - from: "api/main.py"
      to: "api/routes/upload.py"
      via: "include_router"
      pattern: "app\\.include_router\\(upload\\.router"
---

<objective>
Backend foundation: Pydantic models for corpus management and processing jobs, corpus CRUD API routes (create, list, get, delete), and file upload endpoint with multipart FormData support and ZIP archive extraction.

Purpose: Establish the backend data layer and API surface that all subsequent plans (processing trigger, SSE streaming, frontend integration) depend on.
Output: Working corpus CRUD and file upload API with integration tests.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-upload-processing-ui/01.1-CONTEXT.md
@.planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From api/main.py (existing FastAPI app to extend):
```python
app = FastAPI(title="folio-insights Review Viewer", lifespan=lifespan)
app.include_router(tree.router, prefix="/api/v1")
app.include_router(review.router, prefix="/api/v1")
app.include_router(source.router, prefix="/api/v1")
_output_dir: Path = Path("./output")
_default_corpus: str = "default"
```

From src/folio_insights/config.py (settings):
```python
class Settings(BaseSettings):
    output_dir: Path = Path("./output")
    corpus_name: str = "default"
```

From pyproject.toml (current dependencies):
```toml
dependencies = [
    "pydantic>=2.7.0",
    "pydantic-settings>=2.7.0",
    "aiosqlite>=0.20.0",
    # ... others
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic models + corpus CRUD routes + tests</name>
  <files>api/models/__init__.py, api/models/processing.py, api/routes/corpus.py, api/routes/__init__.py, api/main.py, pyproject.toml, tests/test_corpus_api.py</files>
  <read_first>
    - api/main.py (current FastAPI app -- need to see router registration pattern, _output_dir, lifespan)
    - api/routes/review.py (existing route pattern to follow -- prefix, response models, query params)
    - api/routes/__init__.py (existing exports)
    - src/folio_insights/config.py (Settings.output_dir used for corpus storage paths)
    - pyproject.toml (current dependencies -- need to add sse-starlette, python-multipart already present)
    - .planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md (Pydantic model definitions, architecture patterns)
  </read_first>
  <action>
    1. Create `api/models/__init__.py` (empty).

    2. Create `api/models/processing.py` with these Pydantic models:
       - `ProcessingStatus(str, Enum)` with values: PENDING, PROCESSING, COMPLETED, FAILED
       - `ActivityEntry(BaseModel)` with fields: timestamp (str), stage (str), message (str)
       - `CorpusInfo(BaseModel)` with fields: id (str), name (str), file_count (int = 0), processing_status (str = "not_processed"), last_processed (str | None = None), created_at (str)
       - `ProcessingJob(BaseModel)` with fields: id (UUID, default_factory=uuid4), corpus_id (str), status (ProcessingStatus = PENDING), current_stage (str | None = None), progress_pct (int = 0), total_units (int = 0), activity_log (list[ActivityEntry] = []), error (str | None = None), created_at (str), updated_at (str)
       - `CorpusCreateRequest(BaseModel)` with field: name (str, min_length=1, max_length=100)
       - Helper function `slugify(name: str) -> str` that lowercases, replaces spaces/special chars with hyphens, strips leading/trailing hyphens, collapses multiple hyphens. Uses `re.sub(r'[^a-z0-9]+', '-', name.lower()).strip('-')` and `re.sub(r'-+', '-', result)`.

    3. Create `api/routes/corpus.py` with router prefix="/api/v1", tag="corpus":
       - `POST /corpora` -- accepts CorpusCreateRequest body. Slugifies name to create corpus_id. Creates source dir at `{output_dir}/{corpus_id}/sources/` and output dir at `{output_dir}/{corpus_id}/`. Writes a `corpus-meta.json` file in the corpus dir with {"id": corpus_id, "name": name, "created_at": iso_timestamp}. Returns CorpusInfo with 201 status. Raises 409 if corpus dir already exists.
       - `GET /corpora` -- scans `{output_dir}/` for subdirectories containing `corpus-meta.json`, reads each, counts files in `sources/` subdir, returns list[CorpusInfo].
       - `GET /corpora/{corpus_id}` -- reads `corpus-meta.json` from `{output_dir}/{corpus_id}/`, counts source files, returns CorpusInfo. Raises 404 if not found.
       - `DELETE /corpora/{corpus_id}` -- removes entire `{output_dir}/{corpus_id}/` directory tree via `shutil.rmtree`. Returns 204. Raises 404 if not found.
       - `GET /corpora/{corpus_id}/files` -- lists files in `{output_dir}/{corpus_id}/sources/`, returns list of dicts with filename, size_bytes, format (extension).
       - Use `api.main._output_dir` for the output directory (import from api.main). This follows the existing pattern where routes import from main.

    4. Update `api/routes/__init__.py` to export corpus module.

    5. Update `api/main.py`:
       - Add `from api.routes import corpus, upload` (upload will be added in Task 2, import it conditionally or add both now)
       - Add `app.include_router(corpus.router)` (no prefix -- routes already include /api/v1)
       - Note: Do NOT add prefix="/api/v1" to include_router since routes already define their full paths

    6. Add `"sse-starlette>=3.3.0"` to pyproject.toml dependencies (needed by Plan 02, install now to avoid second dependency change).

    7. Run `pip install sse-starlette>=3.3.0` to install the new dependency.

    8. Create `tests/test_corpus_api.py`:
       - Use `httpx.AsyncClient` with `ASGITransport(app=app)` for async testing (matching existing test_review_api.py pattern)
       - Configure app with a temp directory before tests
       - Test create corpus: POST /api/v1/corpora with {"name": "Test Corpus"} -> 201, response has id="test-corpus"
       - Test create duplicate: POST same name -> 409
       - Test list corpora: GET /api/v1/corpora -> includes created corpus
       - Test get corpus: GET /api/v1/corpora/test-corpus -> 200 with correct data
       - Test get nonexistent: GET /api/v1/corpora/nope -> 404
       - Test list files (empty): GET /api/v1/corpora/test-corpus/files -> empty list
       - Test delete corpus: DELETE /api/v1/corpora/test-corpus -> 204
       - Test delete nonexistent: DELETE /api/v1/corpora/nope -> 404

    9. Run tests: `pytest tests/test_corpus_api.py -x -v`
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-advocate" && pytest tests/test_corpus_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `api/models/processing.py` exists and contains `class ProcessingStatus`, `class CorpusInfo`, `class ProcessingJob`, `class ActivityEntry`, `class CorpusCreateRequest`, `def slugify`
    - File `api/routes/corpus.py` exists and contains `router = APIRouter`, `async def create_corpus`, `async def list_corpora`, `async def get_corpus`, `async def delete_corpus`, `async def list_corpus_files`
    - File `api/main.py` contains `app.include_router(corpus.router)`
    - File `pyproject.toml` contains `"sse-starlette>=3.3.0"`
    - `pytest tests/test_corpus_api.py -x` passes with 0 failures
    - POST /api/v1/corpora with {"name": "My Test"} returns 201 with id "my-test"
    - GET /api/v1/corpora returns a list including the created corpus
    - DELETE /api/v1/corpora/{id} returns 204
  </acceptance_criteria>
  <done>Corpus CRUD API fully operational with integration tests passing. Models reusable by subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 2: File upload endpoint with ZIP extraction and path traversal protection</name>
  <files>api/routes/upload.py, tests/test_upload_api.py</files>
  <read_first>
    - api/routes/corpus.py (just created -- need to understand corpus directory structure: {output_dir}/{corpus_id}/sources/)
    - api/models/processing.py (just created -- CorpusInfo model)
    - api/main.py (router registration pattern, _output_dir)
    - .planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md (file upload endpoint example, ZIP path traversal pitfall)
    - src/folio_insights/pipeline/stages/ingestion.py (SUPPORTED_EXTENSIONS for format validation reference)
  </read_first>
  <action>
    1. Create `api/routes/upload.py` with router tag="upload":
       - `POST /api/v1/corpus/{corpus_id}/upload` -- accepts `files: list[UploadFile] = File(...)` via multipart/FormData.
       - Validate corpus exists (check {output_dir}/{corpus_id}/corpus-meta.json), raise 404 if not.
       - For each uploaded file:
         - If file extension is `.zip`: extract to a temp directory, validate each entry's resolved path stays within the extraction target (Zip Slip protection: `str(target.resolve()).startswith(str(extract_dir.resolve()))`), then move extracted files to `{output_dir}/{corpus_id}/sources/`. Skip directories and `__MACOSX` entries.
         - Otherwise: write file directly to `{output_dir}/{corpus_id}/sources/{filename}` using `shutil.copyfileobj(file.file, f)`.
       - Return `{"uploaded": [{"filename": str, "size": int}], "count": int}`.
       - Supported extensions for individual files: .md, .txt, .docx, .pdf, .html, .rtf, .eml, .msg, .xml, .csv, .xlsx, .tsv, .wpd -- validate and reject unsupported formats with 400 error listing the rejected filenames.
       - ZIP files may contain any of the supported formats inside.

    2. Wire the upload router in `api/main.py`:
       - Add `from api.routes import upload` (if not already added in Task 1)
       - Add `app.include_router(upload.router)`

    3. Create `tests/test_upload_api.py`:
       - Test upload single MD file: POST multipart with a .md file -> 200, file exists in sources dir
       - Test upload multiple files: POST with 3 files -> all 3 in sources dir
       - Test upload unsupported format: POST with .exe file -> 400
       - Test upload to nonexistent corpus: POST -> 404
       - Test ZIP extraction: Create a zip in-memory containing 2 .md files, upload -> files extracted to sources dir
       - Test ZIP path traversal protection: Create a zip with `../evil.txt` entry -> 400 error, file NOT written outside corpus dir
       - Test list files after upload: GET /api/v1/corpora/{id}/files shows uploaded files

    4. Run tests: `pytest tests/test_upload_api.py tests/test_corpus_api.py -x -v`
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-advocate" && pytest tests/test_upload_api.py tests/test_corpus_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `api/routes/upload.py` exists and contains `async def upload_files`, `list[UploadFile]`, `zipfile`, path traversal check with `resolve().startswith`
    - File `api/main.py` contains `app.include_router(upload.router)`
    - `pytest tests/test_upload_api.py -x` passes with 0 failures
    - Uploading a .md file creates it at `{output_dir}/{corpus_id}/sources/{filename}`
    - Uploading a .zip extracts its contents to the sources directory
    - Uploading a zip with `../` path entries returns 400 and does NOT write outside corpus dir
    - Uploading a .exe returns 400
  </acceptance_criteria>
  <done>File upload API accepts 14 formats + ZIP archives, extracts ZIPs safely with path traversal protection, and all tests pass.</done>
</task>

</tasks>

<verification>
- All corpus CRUD endpoints respond correctly (create 201, list 200, get 200, delete 204, not-found 404)
- File upload writes files to correct corpus source directory
- ZIP extraction works and path traversal is blocked
- Integration test suite passes: `pytest tests/test_corpus_api.py tests/test_upload_api.py -x`
</verification>

<success_criteria>
- 6 corpus CRUD endpoints operational and tested
- File upload with multipart/FormData working for all 14 formats
- ZIP extraction with Zip Slip protection verified by test
- sse-starlette dependency installed and in pyproject.toml
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-upload-processing-ui/01.1-01-SUMMARY.md`
</output>
