---
phase: 01.1-upload-processing-ui
plan: 04
type: execute
wave: 3
depends_on: ["01.1-02", "01.1-03"]
files_modified:
  - viewer/src/lib/components/UploadZone.svelte
  - viewer/src/lib/components/FileList.svelte
  - viewer/src/lib/components/ProcessButton.svelte
  - viewer/src/lib/components/ProgressDisplay.svelte
  - viewer/src/lib/components/ActivityLog.svelte
  - viewer/src/lib/stores/processing.ts
  - viewer/src/routes/upload/+page.svelte
autonomous: false

requirements: []

must_haves:
  truths:
    - "User can drag-and-drop files onto the upload zone"
    - "User can click the upload zone to open a file picker"
    - "User can upload ZIP archives that are extracted server-side"
    - "User can see uploaded files in a list with format icons and status indicators"
    - "User can press Process Corpus to trigger pipeline processing"
    - "User can see real-time stage-by-stage progress via SSE"
    - "User can see a collapsible activity log with timestamped messages"
    - "After processing completes, user auto-navigates to the review page with the corpus selected"
  artifacts:
    - path: "viewer/src/lib/components/UploadZone.svelte"
      provides: "Drag-and-drop upload zone with file picker"
    - path: "viewer/src/lib/components/FileList.svelte"
      provides: "Uploaded file list with status indicators"
    - path: "viewer/src/lib/components/ProcessButton.svelte"
      provides: "Process Corpus CTA button with state variants"
    - path: "viewer/src/lib/components/ProgressDisplay.svelte"
      provides: "Progress bar + stage pills for pipeline tracking"
    - path: "viewer/src/lib/components/ActivityLog.svelte"
      provides: "Collapsible timestamped log of pipeline events"
    - path: "viewer/src/lib/stores/processing.ts"
      provides: "SSE connection management and processing state"
      exports: ["processingStatus", "currentStage", "progressPct", "activityLog", "startProcessingStream", "closeStream"]
  key_links:
    - from: "viewer/src/lib/stores/processing.ts"
      to: "viewer/src/lib/api/client.ts"
      via: "getSSEUrl for EventSource connection, triggerProcessing for POST"
      pattern: "import.*getSSEUrl.*triggerProcessing.*from.*client"
    - from: "viewer/src/routes/upload/+page.svelte"
      to: "viewer/src/lib/stores/processing.ts"
      via: "subscribes to processing state, calls startProcessingStream"
      pattern: "import.*processingStatus.*from.*processing"
    - from: "viewer/src/lib/stores/processing.ts"
      to: "EventSource"
      via: "native browser SSE API for real-time updates"
      pattern: "new EventSource"
    - from: "viewer/src/routes/upload/+page.svelte"
      to: "$app/navigation"
      via: "goto() for auto-navigation to review on completion"
      pattern: "import.*goto.*from.*navigation"
---

<objective>
Upload page components and processing integration: drag-drop upload zone, file list with status indicators, Process Corpus button, SSE-driven progress display with stage pills, collapsible activity log, and auto-navigation to review page on completion. This completes the Upload & Processing UI.

Purpose: Deliver the complete upload-to-review workflow where users upload files, trigger processing, watch real-time progress, and seamlessly transition to reviewing extracted knowledge units.
Output: Fully functional Upload page with all interactive components and SSE integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-upload-processing-ui/01.1-CONTEXT.md
@.planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md
@.planning/phases/01.1-upload-processing-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-upload-processing-ui/01.1-02-SUMMARY.md
@.planning/phases/01.1-upload-processing-ui/01.1-03-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01/02/03 that this plan depends on -->

From viewer/src/lib/api/client.ts (extended in Plan 03):
```typescript
export async function uploadFiles(corpusId: string, files: FileList | File[]): Promise<{ uploaded: ...; count: number } | { error: string }>;
export async function triggerProcessing(corpusId: string): Promise<{ job_id: string; status: string } | { error: string }>;
export function getSSEUrl(corpusId: string): string;
export async function fetchCorpusFiles(corpusId: string): Promise<CorpusFile[] | { error: string }>;
export interface CorpusFile { filename: string; size_bytes: number; format: string; }
```

From viewer/src/lib/stores/corpus.ts (created in Plan 03):
```typescript
export const corpora: Writable<CorpusInfo[]>;
export const selectedCorpus: Writable<CorpusInfo | null>;
export async function loadCorpora(): Promise<void>;
```

From viewer/src/routes/upload/+page.svelte (created in Plan 03):
```svelte
<!-- Two-column layout with CorpusSidebar in left column -->
<!-- Main content area (right column) to be filled with upload components -->
```

SSE events from backend (Plan 02):
- `status` event: `{ job_id, status, stage, progress }` (progress is 0-100 int)
- `activity` event: `{ timestamp, stage, message }`
- `complete` event: `{ status, total_units, error }`

Pipeline stages (7): ingestion, structure_parser, boundary_detection, distiller, knowledge_classifier, folio_tagger, deduplicator
Display names for stage pills: Ingestion, Structure, Boundaries, Distillation, Classification, FOLIO Tagging, Deduplication
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UploadZone, FileList, ProcessButton components + processing store with SSE</name>
  <files>viewer/src/lib/components/UploadZone.svelte, viewer/src/lib/components/FileList.svelte, viewer/src/lib/components/ProcessButton.svelte, viewer/src/lib/stores/processing.ts</files>
  <read_first>
    - viewer/src/lib/api/client.ts (uploadFiles, triggerProcessing, getSSEUrl, fetchCorpusFiles, CorpusFile interface)
    - viewer/src/lib/stores/corpus.ts (selectedCorpus store, loadCorpora)
    - viewer/src/lib/components/FolioTree.svelte (existing component pattern for Svelte 5 runes, $props, $state, style conventions)
    - viewer/src/lib/components/ReviewControls.svelte (existing component with button styling patterns)
    - viewer/src/app.css (CSS variables for all styling)
    - .planning/phases/01.1-upload-processing-ui/01.1-UI-SPEC.md (Upload Zone states, File List spec, Process Button states, SSE Connection States, Copywriting Contract)
    - .planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md (Pattern 5: Frontend SSE Consumption, SSE store example)
  </read_first>
  <action>
    1. Create `viewer/src/lib/stores/processing.ts`:
       - Writable stores:
         - `processingStatus: Writable<'idle' | 'processing' | 'complete' | 'error'>` (default: 'idle')
         - `currentStage: Writable<string>` (default: '')
         - `progressPct: Writable<number>` (default: 0)
         - `activityLog: Writable<Array<{timestamp: string; stage: string; message: string}>>` (default: [])
         - `sseState: Writable<'connected' | 'reconnecting' | 'closed'>` (default: 'closed')
         - `totalUnits: Writable<number>` (default: 0)
         - `processingError: Writable<string | null>` (default: null)
       - Module-level: `let eventSource: EventSource | null = null;`
       - `export function startProcessingStream(corpusId: string): void`
         - Call closeStream() first
         - Reset all stores to initial values except activityLog (clear it)
         - Set processingStatus to 'processing', sseState to 'connected'
         - Create `new EventSource(getSSEUrl(corpusId))`
         - Add event listeners:
           - `status`: parse JSON, update currentStage and progressPct
           - `activity`: parse JSON, append to activityLog using `update(log => [...log, data])`
           - `complete`: parse JSON data. Set totalUnits, processingError (if data.error). Set processingStatus to `data.status === 'completed' ? 'complete' : 'error'`. Set sseState to 'closed'. Close EventSource.
           - `onerror`: set sseState to 'reconnecting'
       - `export function closeStream(): void` -- close EventSource, set to null, set sseState to 'closed'
       - `export function resetProcessing(): void` -- closeStream + reset all stores to defaults

    2. Create `viewer/src/lib/components/UploadZone.svelte` (Svelte 5 runes):
       - Props: `onfiles: (files: FileList) => void`, `disabled: boolean = false`
       - State: `let dragover = $state(false)`, `let fileInput: HTMLInputElement`
       - Structure:
         - Outer div with class "upload-zone", conditional classes: `dragover`, `disabled`
         - `role="button"`, `tabindex="0"`, `aria-label="Upload files. Drop files here or press Enter to open file picker."`
         - Event handlers:
           - `ondragover`: `e.preventDefault(); if (!disabled) dragover = true;`
           - `ondragleave`: `dragover = false`
           - `ondrop`: `e.preventDefault(); dragover = false; if (!disabled && e.dataTransfer?.files.length) onfiles(e.dataTransfer.files);`
           - `onclick`: `if (!disabled) fileInput.click();`
           - `onkeydown`: Enter/Space -> `e.preventDefault(); if (!disabled) fileInput.click();`
         - Hidden file input: `type="file"`, `multiple`, `accept=".md,.txt,.docx,.pdf,.html,.rtf,.eml,.msg,.xml,.csv,.xlsx,.tsv,.wpd,.zip"`
         - Also include a folder-upload hidden input with `webkitdirectory` attribute, triggered by a separate "Upload Folder" link/button inside the zone
         - Content: Upload icon SVG (24x24, `stroke: currentColor`, cloud-upload style) + "Drop files here or click to browse" + hint line "Supports MD, TXT, DOCX, PDF, HTML, RTF, EML, MSG, XML, CSV, XLSX, TSV, WPD, and ZIP archives"
       - Styling from UI-SPEC:
         - Default: `border: 2px dashed var(--border)`, `background: transparent`, `color: var(--text-dim)`
         - Hover: `border-color: var(--accent-dim)`, `color: var(--text)`
         - Drag-over: `border-color: var(--accent)`, `background: rgba(108, 140, 255, 0.05)`, `color: var(--text)`
         - Disabled: `opacity: 0.5`, pointer-events none
         - Min-height: 120px, border-radius: 8px, padding: `var(--md)`, text-align: center
         - Transition: `all 200ms ease`

    3. Create `viewer/src/lib/components/FileList.svelte`:
       - Props: `files: Array<{filename: string; size_bytes: number; format: string; status?: string}>`, `onremove?: (filename: string) => void`
       - Empty state: If files.length === 0, show "No documents yet" + "Upload files to this corpus using the drop zone above." (from UI-SPEC copywriting)
       - File list: `role="list"`
       - Each file item: `role="listitem"`
         - Format icon: small colored dot or simple SVG based on extension (use first 2-3 chars of extension as text badge if full icons are complex)
         - Filename: 14px, truncated with `text-overflow: ellipsis`, `white-space: nowrap`, `overflow: hidden`
         - File size: 11px, `var(--text-dim)`, formatted (KB/MB)
         - Status indicator: small circle with color per status: pending=`var(--text-dim)`, processing=`var(--orange)` with pulsing animation, complete=`var(--green)`, error=`var(--red)`
         - Remove button (X): appears on hover, `color: var(--text-dim)` -> `var(--red)` on hover. Calls `onremove` if provided.
       - Item styling from UI-SPEC:
         - Height: 36px, padding: `var(--xs) var(--sm)`, background: `var(--surface2)`, border: `1px solid var(--border)`, border-radius: 4px, gap between items: `var(--xs)`
       - File size formatter: `function formatSize(bytes: number): string` -- returns "1.2 KB", "3.4 MB" etc.
       - Pulse animation for processing status: `@keyframes pulse { 0%, 100% { opacity: 1 } 50% { opacity: 0.4 } }` with `animation: pulse 1000ms ease-in-out infinite`

    4. Create `viewer/src/lib/components/ProcessButton.svelte`:
       - Props: `onclick: () => void`, `disabled: boolean = false`, `status: 'idle' | 'processing' | 'complete' | 'error' = 'idle'`
       - Derived label: idle="Process Corpus", processing="Processing...", complete="Process Corpus" (re-enables), error="Retry Processing" (from UI-SPEC copywriting)
       - Derived disabled state: disabled OR status === 'processing'
       - Styling from UI-SPEC:
         - Default: `background: var(--accent)`, `color: #ffffff`
         - Hover: `filter: brightness(0.9)` on the accent background
         - Disabled (no files): `background: var(--surface3)`, `color: var(--text-dim)`
         - Processing: `background: var(--accent)` at 50% opacity, `color: #ffffff`
         - Height: 44px, padding: `0 var(--lg)`, border-radius: 6px, font: 14px weight 600
         - Full width: `width: 100%`
  </action>
  <verify>
    <automated>cd viewer && npm run build 2>&1 | tail -5 && npm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `viewer/src/lib/stores/processing.ts` contains `export const processingStatus`, `export const currentStage`, `export const progressPct`, `export const activityLog`, `export function startProcessingStream`, `new EventSource`
    - File `viewer/src/lib/components/UploadZone.svelte` contains `role="button"`, `aria-label="Upload files`, `ondrop`, `ondragover`, `accept=".md,.txt,.docx,.pdf,.html,.rtf,.eml,.msg,.xml,.csv,.xlsx,.tsv,.wpd,.zip"`, `webkitdirectory`
    - File `viewer/src/lib/components/FileList.svelte` contains `role="list"`, `role="listitem"`, `No documents yet`, pulse animation keyframes
    - File `viewer/src/lib/components/ProcessButton.svelte` contains `Process Corpus`, `Processing...`, `Retry Processing`, height 44px, `var(--accent)` background
    - `npm run build` in viewer/ succeeds
    - `npm run check` (svelte-check) passes with no type errors in processing.ts or new components -- validates SSE state machine types are sound
  </acceptance_criteria>
  <done>Upload zone accepts drag-drop and file picker. File list shows files with status indicators. Process button has correct state variants. Processing store manages SSE connection and state. Type-checked by svelte-check.</done>
</task>

<task type="auto">
  <name>Task 2: ProgressDisplay, ActivityLog, Upload page wiring, and auto-navigation</name>
  <files>viewer/src/lib/components/ProgressDisplay.svelte, viewer/src/lib/components/ActivityLog.svelte, viewer/src/routes/upload/+page.svelte</files>
  <read_first>
    - viewer/src/routes/upload/+page.svelte (current state from Plan 03 + Task 1 -- need to see what's there and wire everything together)
    - viewer/src/lib/stores/processing.ts (just created -- processingStatus, currentStage, progressPct, activityLog, startProcessingStream, closeStream, resetProcessing)
    - viewer/src/lib/stores/corpus.ts (selectedCorpus, loadCorpora)
    - viewer/src/lib/api/client.ts (uploadFiles, triggerProcessing, fetchCorpusFiles)
    - viewer/src/lib/components/UploadZone.svelte (just created -- onfiles prop)
    - viewer/src/lib/components/FileList.svelte (just created -- files prop)
    - viewer/src/lib/components/ProcessButton.svelte (just created -- onclick, disabled, status props)
    - viewer/src/lib/components/CorpusSidebar.svelte (created in Plan 03 -- already in upload page)
    - viewer/src/app.css (CSS variables)
    - .planning/phases/01.1-upload-processing-ui/01.1-UI-SPEC.md (Progress Display spec, Activity Log spec, Interaction States, Animation & Motion, Layout diagrams, Copywriting)
    - .planning/phases/01.1-upload-processing-ui/01.1-RESEARCH.md (auto-navigation example with goto + 1.5s delay)
  </read_first>
  <action>
    1. Create `viewer/src/lib/components/ProgressDisplay.svelte`:
       - Props: `progress: number` (0-100), `currentStage: string`, `status: 'idle' | 'processing' | 'complete' | 'error'`
       - Stage definitions (constant array):
         ```typescript
         const STAGES = [
           { key: 'ingestion', label: 'Ingestion' },
           { key: 'structure_parser', label: 'Structure' },
           { key: 'boundary_detection', label: 'Boundaries' },
           { key: 'distiller', label: 'Distillation' },
           { key: 'knowledge_classifier', label: 'Classification' },
           { key: 'folio_tagger', label: 'FOLIO Tagging' },
           { key: 'deduplicator', label: 'Deduplication' },
         ];
         ```
       - Derive stage states: for each stage, determine if it's 'pending', 'active', 'complete', or 'error' based on currentStage and status.
         - Stages before currentStage = 'complete'
         - Stage matching currentStage = 'active' (or 'error' if status === 'error')
         - Stages after currentStage = 'pending'
         - If status === 'complete', all stages = 'complete'
       - Progress bar from UI-SPEC:
         - Container: height 3px, `background: var(--surface3)`, border-radius 2px
         - Fill: width = `{progress}%`, `background: var(--accent)` (or `var(--green)` if status==='complete', `var(--red)` if status==='error')
         - `transition: width 0.3s ease`
         - `role="progressbar"`, `aria-valuenow={progress}`, `aria-valuemin="0"`, `aria-valuemax="100"`, `aria-label="Pipeline processing progress"`
       - Stage pills row from UI-SPEC:
         - Gap: `var(--xs)` (4px), flex-wrap: wrap
         - Each pill: 11px uppercase text, `letter-spacing: 0.5px`, padding `var(--xs) var(--sm)` (4px 8px), border-radius 3px
         - Pill states (from UI-SPEC):
           - Pending: `background: var(--surface2)`, `color: var(--text-dim)`
           - Active: `background: rgba(108, 140, 255, 0.2)`, `color: var(--accent)`, `aria-current="step"`
           - Complete: `background: rgba(76, 175, 124, 0.2)`, `color: var(--green)`
           - Error: `background: rgba(224, 85, 85, 0.2)`, `color: var(--red)`
         - Transition: `background 150ms ease, color 150ms ease`

    2. Create `viewer/src/lib/components/ActivityLog.svelte`:
       - Props: `entries: Array<{timestamp: string; stage: string; message: string}>`, `expanded: boolean = false`
       - State: `let isExpanded = $state(expanded)` -- collapsed by default per UI-SPEC
       - Toggle header: chevron SVG icon + "Activity Log" label
         - 11px, `color: var(--text-dim)`, cursor pointer
         - Chevron rotates: `-90deg` collapsed, `0deg` expanded, transition `0.15s ease`
         - `aria-expanded={isExpanded}`, `aria-controls="activity-log-content"`
       - Log content container: `id="activity-log-content"`
         - `max-height: 240px`, `overflow-y: auto`
         - When collapsed: `max-height: 0`, `opacity: 0`, `overflow: hidden`
         - Transition: `max-height 150ms ease-out, opacity 150ms ease-out`
       - Each log entry:
         - Font: monospace (`'SF Mono', 'Fira Code', 'Consolas', monospace`), 11px
         - Timestamp: 11px, `color: var(--text-dim)`, format HH:MM:SS from ISO string
         - Stage badge: colored per stage pill state conventions (use same colors as ProgressDisplay pills)
         - Message: 11px, `color: var(--text)`
         - Entry padding: `var(--xs) 0` (4px 0)
       - Auto-scroll to bottom when new entries are added (use $effect watching entries.length)
       - Auto-expand on error (if an entry contains error-related content or if processing status is 'error')

    3. Wire everything together in `viewer/src/routes/upload/+page.svelte`:
       - Import all components: CorpusSidebar (already there), UploadZone, FileList, ProcessButton, ProgressDisplay, ActivityLog
       - Import stores: `selectedCorpus` from corpus store, all processing stores
       - Import `goto` from `$app/navigation`
       - Import `uploadFiles`, `fetchCorpusFiles`, `triggerProcessing` from API client
       - Import `onDestroy` from 'svelte' for cleanup

       - Local state:
         - `let files = $state<Array<{filename: string; size_bytes: number; format: string; status: string}>>([])` -- loaded from API + local uploads
         - `let uploading = $state(false)` -- while files are being uploaded to server

       - Effects ($effect):
         - When `$selectedCorpus` changes: load files from `fetchCorpusFiles($selectedCorpus.id)`, reset processing state
         - When `$processingStatus` changes to 'complete': auto-navigate after 1.5s delay using `setTimeout(() => goto(\`/?corpus=${$selectedCorpus.id}\`), 1500)`. Show brief success state first.
         - When `$processingStatus` changes to 'error': auto-expand activity log

       - Handlers:
         - `async function handleFiles(fileList: FileList)`: set uploading=true, call `uploadFiles($selectedCorpus.id, fileList)`, on success refresh file list from API, set uploading=false
         - `async function handleProcess()`: call `triggerProcessing($selectedCorpus.id)`, then `startProcessingStream($selectedCorpus.id)`
         - `function handleRemoveFile(filename: string)`: TODO -- could call a DELETE endpoint or just filter from local list (API for individual file removal is discretionary per CONTEXT.md)

       - Layout (right column content, conditionally rendered):
         - **No corpus selected**: Show empty state: "No corpus selected" heading + "Create a new corpus or select an existing one from the sidebar." body
         - **Corpus selected, NOT processing** ($processingStatus === 'idle' or 'error'):
           - UploadZone at top (disabled when uploading)
           - ProcessButton below upload zone (disabled when files.length === 0 or uploading, status from $processingStatus -- 'error' shows "Retry Processing")
           - FileList below button
         - **Corpus selected, PROCESSING** ($processingStatus === 'processing'):
           - ProgressDisplay (replaces upload zone) with progress=$progressPct, currentStage=$currentStage, status='processing'
           - ActivityLog below progress display
           - ProcessButton showing "Processing..." (disabled)
           - FileList below (files updating status in real-time if possible, or just showing last known state)
         - **Corpus selected, COMPLETE** ($processingStatus === 'complete'):
           - ProgressDisplay with progress=100, status='complete' (all green)
           - Brief success text: "Processing complete -- {n} files processed, {m} knowledge units extracted." (from UI-SPEC copywriting, using $totalUnits)
           - Auto-navigates to review after 1.5s

       - ARIA live region: `<div aria-live="polite" class="sr-only">` for announcing processing status changes (e.g., "Processing stage: Distillation", "Processing complete")

       - Cleanup: `onDestroy(() => closeStream())` to close SSE connection when leaving page

    4. Build and verify: `cd viewer && npm run build && npm run check`
  </action>
  <verify>
    <automated>cd viewer && npm run build 2>&1 | tail -5 && npm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `viewer/src/lib/components/ProgressDisplay.svelte` contains `role="progressbar"`, `aria-valuenow`, `aria-valuemax="100"`, `STAGES` array with 7 entries, `aria-current="step"`, pill state colors from UI-SPEC
    - File `viewer/src/lib/components/ActivityLog.svelte` contains `aria-expanded`, `aria-controls`, chevron rotation, monospace font family, `max-height: 240px`, `Activity Log` text
    - File `viewer/src/routes/upload/+page.svelte` contains `import UploadZone`, `import FileList`, `import ProcessButton`, `import ProgressDisplay`, `import ActivityLog`, `goto(`, `startProcessingStream`, `closeStream`, `onDestroy`, `setTimeout`, `1500`, `aria-live="polite"`, all three conditional states (no corpus / idle / processing / complete)
    - `npm run build` in viewer/ succeeds with no errors
    - `npm run check` (svelte-check) passes -- validates type correctness of store subscriptions, component prop passing, and SSE event handler types
    - Upload page wires all components with correct props
    - Auto-navigation uses goto with 1.5s delay after processing completes
    - SSE cleanup happens on page destroy
  </acceptance_criteria>
  <done>All upload page components wired together. Progress display shows real-time pipeline advancement via SSE. Activity log collapses/expands. Auto-navigation to review on completion works. All types validated by svelte-check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification (covers SSE state machine behavioral testing)</name>
  <files>none</files>
  <action>
    Human verifies the complete Upload and Processing UI end-to-end.
    Note: The processing store (processing.ts) contains non-trivial SSE state machine logic (idle->processing->complete/error transitions, EventSource lifecycle, store resets). No vitest framework exists in the viewer -- svelte-check validates types, and this checkpoint provides behavioral coverage of the state machine through live end-to-end testing.

    1. Start the backend: `python -m api.main` (port 8700)
    2. Start the frontend dev server: `cd viewer && npm run dev` (port 5173)
    3. Open http://localhost:5173/upload
    4. Verify navigation tabs (Upload/Review) appear in header, Upload tab is active with accent underline
    5. Click "+ New Corpus" in the sidebar, enter a name, verify corpus appears in list
    6. Drag-and-drop a .md file onto the upload zone (or click to browse), verify it appears in the file list
    7. Click "Process Corpus" button, verify:
       - Progress bar advances stage by stage
       - Stage pills change color (blue=active, green=complete)
       - Activity log shows timestamped messages
       - SSE state transitions: idle -> processing -> complete (verify no stale state)
    8. After processing completes, verify auto-navigation to the Review page with the processed corpus selected
    9. Navigate back to Upload tab, verify the corpus shows "Processed" status
    10. Click "Review" tab, verify the review page works normally with the processed corpus
    11. (Error path) If possible, test with an invalid file to verify error state: status='error', activity log auto-expands, "Retry Processing" button appears
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User has visually verified the complete upload-to-review workflow including SSE state transitions and approved the UI.</done>
</task>

</tasks>

<verification>
- Upload zone accepts drag-drop and file picker with all 14 formats + ZIP
- File list shows uploaded files with pending/processing/complete/error status
- Process button triggers pipeline and shows Processing... state
- Progress display shows real-time SSE-driven stage advancement
- Activity log is collapsible with timestamped messages
- Auto-navigation to review page after 1.5s on completion
- All components match UI-SPEC styling (dark theme, spacing, typography)
- SvelteKit build and svelte-check both pass (type safety for SSE state machine)
- Human checkpoint covers behavioral testing of SSE state transitions (no vitest in viewer)
</verification>

<success_criteria>
- Drag-and-drop upload works with visual feedback (border/background changes)
- File picker accepts all 14 supported formats
- ZIP archives uploaded and extracted server-side
- Process Corpus button triggers background pipeline processing
- SSE stream drives real-time progress bar and stage pills
- Activity log shows timestamped pipeline events
- Auto-navigation to review page on completion
- All components use Svelte 5 runes and match UI-SPEC
- Accessibility: ARIA roles, keyboard navigation, screen reader announcements
- Type safety: svelte-check passes with no errors on processing store and all components
- Visual verification approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-upload-processing-ui/01.1-04-SUMMARY.md`
</output>
