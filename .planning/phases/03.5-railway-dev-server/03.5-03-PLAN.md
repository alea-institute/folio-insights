---
phase: 03.5-railway-dev-server
plan: 03
type: execute
wave: 3
depends_on: ["03.5-02"]
files_modified: ["README.md", ".planning/STATE.md"]
autonomous: true
requirements: []          # No primary REQ-IDs; QUALITY-03/OBS-04 SUPPORTING-only and N/A this phase

must_haves:
  truths:
    - "The live URL passes the full smoke trio: /health 200 {status:ok}, / 200 (SSR), /api/v1/corpora JSON list"
    - "The SSR viewer renders visually (Chrome DevTools MCP screenshot inspected)"
    - "A push to master triggers an automatic Railway redeploy that reaches healthy state (auto-deploy proven)"
    - "README 'Deploying to Railway' matches the restored web-only + flat-toml + auto-deploy reality"
    - "STATE.md records the Phase 3.5 outcome and the restored live URL"
  artifacts:
    - path: "README.md"
      provides: "Updated 'Deploying to Railway' section reflecting web-only Dockerfile.web + flat railway.toml + master auto-deploy"
      contains: "Dockerfile.web"
    - path: ".planning/STATE.md"
      provides: "Phase 3.5 completion + restored live URL recorded"
      contains: "03.5"
  key_links:
    - from: "README 'Deploying to Railway'"
      to: "Dockerfile.web + flat railway.toml"
      via: "documented deploy flow"
      pattern: "Dockerfile.web"
    - from: "git push master"
      to: "Railway web service redeploy"
      via: "GitHub App auto-deploy trigger"
      pattern: "master"
  rationale: >
    Acceptance is live-URL smoke, not the pytest suite (infra phase). QUALITY-03/OBS-04 are N/A:
    no worker image is built (web-only) and Railway's builder is not the Dagger pipeline.
---

<objective>
Prove the restored dev server end-to-end and update the docs/state to match reality. Run the live
smoke trio plus a Chrome DevTools MCP screenshot of the SSR viewer, prove auto-deploy by pushing a
no-op commit to `master` and observing a fresh build, then rewrite the stale README "Deploying to
Railway" section and record the outcome in STATE.md.

Purpose: The phase exit criteria are runtime/live-URL behaviors. This plan converts "deploy looks
healthy" into proof and leaves the repo's deploy docs accurate for the next operator.
Output: Verified live endpoints + screenshot, proven auto-deploy, updated README + STATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-railway-dev-server/03.5-CONTEXT.md
@.planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md
@.planning/phases/03.5-railway-dev-server/03.5-VALIDATION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the live smoke trio + MCP screenshot, then prove auto-deploy on master</name>
  <files>(operational — live URL smoke + Chrome DevTools MCP + a no-op master push; no committed repo files beyond the trivial no-op touch)</files>
  <read_first>
    - .planning/phases/03.5-railway-dev-server/03.5-VALIDATION.md ("Per-Task Verification Map"; "Sampling Rate" → phase gate)
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("Code Examples → Smoke verify (Stage 4)"; "Pattern 2: Auto-deploy on push to master"; "Common Pitfalls 3/6")
    - .planning/phases/03.5-railway-dev-server/03.5-02-SUMMARY.md (confirm trigger branch = master was wired in Plan 02)
  </read_first>
  <action>
    With `$URL=https://folio-insights-production.up.railway.app`, run the live smoke trio:
    `curl -sf "$URL/health" | jq -e '.status=="ok"'`, `curl -sI "$URL/" | head -1 | grep -q 200`,
    `curl -sf "$URL/api/v1/corpora" | jq -e 'length>=0'`. If `/` returns a 502 with header
    `X-Railway-Fallback: true`, the domain target port is wrong (Pitfall 3) — flag for re-check in
    Plan 02's dashboard step rather than masking it. Then take a Chrome DevTools MCP screenshot of
    `$URL`: navigate_page to the URL, wait_for async content to load, take_screenshot to a file under
    $HOME, read the screenshot to confirm the SvelteKit viewer renders the bundled corpora (not an
    error page), then delete the screenshot file (per CLAUDE.md). Do NOT fall back to headless
    Chromium — if MCP is unavailable, stop and ask the operator. Finally prove auto-deploy: create a
    trivial no-op commit on `master` (e.g. a whitespace/comment touch in README or a docs line) and
    push it, then observe `railway logs --build -n 100 --latest` for a NEW build triggered by that
    push and confirm it reaches healthy state, re-running `curl -sf "$URL/health"` after it completes.
  </action>
  <verify>
    <automated>URL="https://folio-insights-production.up.railway.app"; curl -sf "$URL/health" | jq -e '.status=="ok"' && curl -sI "$URL/" | head -1 | grep -q 200 && curl -sf "$URL/api/v1/corpora" | jq -e 'length>=0'</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sf "$URL/health" | jq -e '.status=="ok"'` exits 0.
    - `curl -sI "$URL/" | head -1` reports HTTP 200 (SSR) and the response does NOT carry
      `X-Railway-Fallback: true`.
    - `curl -sf "$URL/api/v1/corpora" | jq -e 'length>=0'` exits 0 (JSON corpora list returned).
    - A Chrome DevTools MCP screenshot of `$URL` was captured and inspected; the viewer renders the
      bundled corpora (recorded in the SUMMARY); the screenshot file was deleted afterward.
    - A no-op `master` push triggered a NEW Railway build (`railway logs --build --latest` shows a
      build newer than the push) that reached healthy state, and `/health` returns 200 after it.
  </acceptance_criteria>
  <done>
    The live dev URL passes the full smoke trio, the SSR viewer renders, and auto-deploy on master is
    proven by a fresh build from a no-op push.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README "Deploying to Railway" and record the outcome in STATE.md</name>
  <files>README.md, .planning/STATE.md</files>
  <read_first>
    - README.md (lines 300-343, the current stale "Deploying to Railway" section describing the single-Dockerfile v1.1 flow)
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("State of the Art" → README is stale; "Railway CLI v4.31.0 — Verified Command Surface" for correct commands)
    - .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md (final scope decision to document)
    - .planning/STATE.md (Current Position + Roadmap Evolution + Performance Metrics blocks to update)
  </read_first>
  <action>
    Rewrite README "Deploying to Railway" to match the restored reality: a single WEB service built
    from `Dockerfile.web` (not the old single `Dockerfile`), pinned by a FLAT `railway.toml`
    (`[build]` builder=DOCKERFILE + dockerfilePath=Dockerfile.web; `[deploy]` healthcheckPath=/health
    + timeout + restartPolicy). State that the worker tier is intentionally NOT deployed in the dev
    environment (idle stub until Phase 10) and that Phase 20 owns the GA cut. Correct the one-time
    setup to v4.31.0 commands (`railway login`, `railway link` to the EXISTING project — not
    `railway init`; do not create a new project), note that secrets are set as masked variables via
    `railway variable set ... --stdin`, and document auto-deploy: Service -> Settings -> Source ->
    trigger branch = master so every push redeploys. Keep the existing Verify smoke trio and the
    Notes about bundled corpora and ~8.7 GB image size (CPU-only torch lever). Then update STATE.md:
    set the Phase 3.5 entry to complete with the restored live URL
    (https://folio-insights-production.up.railway.app), add a Decisions line recording the flat-toml
    web-only fix and the v4.x ci/railway.py correction, and append the phase to the metrics/roadmap
    evolution where appropriate. Do not invent metrics — record only what is known.
  </action>
  <verify>
    <automated>grep -q "Dockerfile.web" README.md && grep -q "trigger branch" README.md && ! grep -qE "railway init --name" README.md && grep -q "03.5" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - README "Deploying to Railway" references `Dockerfile.web` and the flat `railway.toml`, and no
      longer claims a single `Dockerfile` builds the service (`grep -c "single multi-stage Dockerfile" README.md` == 0).
    - README documents the master trigger-branch auto-deploy and the `railway link` (existing project)
      flow; it no longer instructs `railway init --name` (`grep -c "railway init --name" README.md` == 0).
    - README states the worker is not deployed in the dev environment (web-only) and Phase 20 owns GA.
    - STATE.md records Phase 3.5 as complete with the restored live URL and the flat-toml/web-only +
      ci/railway.py v4.x decisions.
  </acceptance_criteria>
  <done>
    README "Deploying to Railway" is accurate for the restored web-only auto-deploy reality and
    STATE.md reflects the Phase 3.5 outcome.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → live dev URL | Pre-GA endpoints are publicly reachable on the dev URL |
| git push master → Railway build | The no-op push that proves auto-deploy triggers a real build |
| repo files → git | README/STATE are committed; must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.5-07 | Information Disclosure | public dev URL exposes pre-GA endpoints | accept | Dev/testing server by scope; no PII; bundled corpora only; Phase 20 owns hardening — documented, not mitigated here |
| T-03.5-08 | Information Disclosure | screenshot / smoke output in transcript | mitigate | Screenshot written under $HOME and deleted after inspection (CLAUDE.md); smoke endpoints return no secrets |
| T-03.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase; RESEARCH.md Package Legitimacy Audit = Not Applicable |
</threat_model>

<verification>
- Live smoke trio passes (`/health` 200 {status:ok}, `/` 200 SSR, `/api/v1/corpora` JSON).
- MCP screenshot confirms the SSR viewer renders.
- No-op master push triggers a fresh healthy build (auto-deploy proven).
- README "Deploying to Railway" updated; STATE.md records the outcome.
</verification>

<success_criteria>
All phase exit criteria proven on the live URL, auto-deploy demonstrated by a master push, and the
repo's deploy docs + project state updated to match the restored web-only dev server.
</success_criteria>

<output>
Create `.planning/phases/03.5-railway-dev-server/03.5-03-SUMMARY.md` when done.
</output>
