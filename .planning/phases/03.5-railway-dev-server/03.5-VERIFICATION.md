---
phase: 03.5-railway-dev-server
verified: 2026-05-22T22:40:00Z
status: passed
score: 5/5 roadmap exit criteria verified (17/17 plan must-haves; 1 ACCEPTED deviation: SPA-for-SSR)
overrides_applied: 1
overrides:
  - must_have: "/ returns 200 and the SvelteKit viewer renders (SSR) the bundled corpora"
    reason: >
      Restored as a static SPA (adapter-static) instead of SSR — operator-approved decision
      recorded in 03.5-02-SUMMARY and STATE.md. The exit-criterion INTENT is met: / returns
      HTTP/2 200 text/html (no x-railway-fallback), serves the SvelteKit shell, and the viewer
      renders the bundled demo+test1 corpora (verified live: API returns real tree data, SPA JS
      assets serve 200). Full SSR + Gate 4 (QUALITY-04) are explicitly deferred to Phase 20.
    accepted_by: "Damien Riehl (operator)"
    accepted_at: "2026-05-22T17:33:00Z"
---

# Phase 3.5: Railway Dev/Testing Server Verification Report

**Phase Goal:** Restore the Railway dev server (was returning HTTP 502) to a healthy web-only deployment at https://folio-insights-production.up.railway.app, with config-as-code that Railway honors, the viewer rendering bundled corpora, and auto-deploy on master.
**Verified:** 2026-05-22T22:40:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

This is an **infra phase**; acceptance is **live-URL smoke**, not the pytest suite (per the plans'
rationale). I verified the 5 ROADMAP exit criteria AND the 17 plan-level must-haves against BOTH the
codebase AND the live URL (server is up and responding).

### Observable Truths — ROADMAP Exit Criteria (the contract)

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | `/health` returns 200 `{"status":"ok"}` on the live URL | ✓ VERIFIED | Live: `curl /health` → `{"status":"ok"}`. Endpoint defined api/main.py:46-49. |
| 2 | `/` returns 200 and the viewer renders (SSR) the bundled corpora | ✓ PASSED (override) | Live: `/` → HTTP/2 200 `text/html`, **no** `x-railway-fallback`; body is the SvelteKit shell (`<title>FOLIO Insights</title>` + `_app/` assets, which serve 200). SSR→SPA is operator-approved (see override). |
| 3 | `/api/v1/corpora` returns the bundled corpora as JSON | ✓ VERIFIED | Live: returns `[demo, test1]`; `/api/v1/tree?corpus=demo` returns real FOLIO tree units (Litigation branch, real IRIs) — corpora flowed through `COPY output/` → `load_extraction()` → API. |
| 4 | A push to `master` triggers an automatic Railway redeploy that reaches healthy state | ✓ VERIFIED | Plan 02 master pushes `a275b59`, `197d67a` each auto-triggered a build; final (deploy `c5e4b32e`, commit `197d67a`) reached healthy. README + dashboard wire trigger branch = master. All commits exist in git. |
| 5 | README "Deploying to Railway" updated to match restored reality | ✓ VERIFIED | README.md:300-375 rewritten: Dockerfile.web (5×), flat railway.toml, trigger branch, adapter-static SPA, railway link (existing). `single multi-stage Dockerfile` = 0; `railway init --name` = 0. |

**Score:** 5/5 ROADMAP exit criteria verified (criterion 2 via operator-approved override).

### Observable Truths — Plan Must-Haves (detail)

**Plan 01 (auth/link/diagnose):**

| Truth | Status | Evidence |
| ----- | ------ | -------- |
| Operator authenticated + repo linked to existing project | ✓ VERIFIED | `railway status --json` resolves project `folio-insights` (id `4a02ae26…`), the EXISTING project — not a new one. |
| Real 502 root cause identified from live logs | ✓ VERIFIED | 03.5-DIAGNOSIS.md: nested toml ignored → stale `/Dockerfile` `COPY output/` fails against output-excluding `.dockerignore`; quoted live build error from FAILED deploy `251b2e40` / commit `c4d84ec`. |
| Service topology known | ✓ VERIFIED | DIAGNOSIS: single `folio-insights` service, no worker wired, dynamic `$PORT`. |
| Variables enumerated vs `.env.example` | ✓ VERIFIED | DIAGNOSIS lists present (RAILWAY_*) vs missing runtime vars by NAME only; no secret literals (verified grep — clean). |
| Scope decided + recorded | ✓ VERIFIED | DIAGNOSIS: WEB-ONLY (locked, no worker to remove). |

**Plan 02 (apply fix / redeploy):**

| Truth | Status | Evidence |
| ----- | ------ | -------- |
| railway.toml is flat single-service (no `[services.*]`) | ✓ VERIFIED | `tomllib` parse OK: `build`+`deploy` present, `services` absent; `[services` count = 0. |
| Flat config pins builder/dockerfilePath/healthcheck/restartPolicy | ✓ VERIFIED | railway.toml:15-25 — `builder="DOCKERFILE"`, `dockerfilePath="Dockerfile.web"`, `healthcheckPath="/health"`, timeout 120, ON_FAILURE/3. |
| Build used Dockerfile.web (not Railpack) | ✓ VERIFIED (live-inference) | Live `/api/v1/corpora` returns bundled corpora — only present because `Dockerfile.web COPY output/` ran. Stale `/Dockerfile` would 502 (the diagnosed failure). SPA shell + assets confirm the adapter-static frontend stage built. (Direct build-log grep not retrievable this session; live data flow is independent proof.) |
| Required runtime variables present as masked vars | ✓ VERIFIED | SUMMARY + STATE: `LLM_PROVIDER`, `LLM_MODEL` set (non-secret); ANTHROPIC_API_KEY intentionally BYOK (operator decision, documented). A2 confirms not needed for boot. |
| GitHub source trigger branch = master | ✓ VERIFIED | DIAGNOSIS topology + README:347-351; master pushes auto-triggered builds (operator-confirmed dashboard step). |
| Clean redeploy reaches healthy + `/health` 200 | ✓ VERIFIED | Live `/health` 200; deploy `c5e4b32e` healthy. |
| ci/railway.py no longer uses `up --image` | ✓ VERIFIED | py_compile OK; live (non-comment) `up --image` count = 0; uses `railway add -i` (ci/railway.py:44); `env=`-only token preserved (3 matches). |

**Plan 03 (prove + docs):**

| Truth | Status | Evidence |
| ----- | ------ | -------- |
| Live smoke trio passes | ✓ VERIFIED | All three live (see exit criteria 1/3 + `/` 200). |
| Viewer renders visually (MCP screenshot) | ✓ VERIFIED (corroborated) | Screenshot claim in 03.5-03-SUMMARY corroborated by live HTML shell (`<title>FOLIO Insights</title>`), SPA JS asset 200, and real API tree data. (Chrome DevTools MCP not available in this verifier session — re-confirmed via live HTTP evidence instead.) |
| Master push triggers auto-redeploy to healthy | ✓ VERIFIED | See exit criterion 4. |
| README matches restored reality | ✓ VERIFIED | See exit criterion 5. |
| STATE records outcome + restored live URL | ✓ VERIFIED | STATE.md:166-172 — 7 Phase 03.5 decision lines incl. restored live URL. ROADMAP marks 3.5 Complete (line 42, 600). |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `railway.toml` | Flat web-only config Railway honors | ✓ VERIFIED | Flat `[build]`/`[deploy]`, no nesting, parses, pins Dockerfile.web + /health. |
| `ci/railway.py` | v4.x deploy wrapper (`railway add -i`) | ✓ VERIFIED | Compiles; image branch uses `railway add -i`; token via `env=` only. |
| `Dockerfile.web` | Buildable/bootable web tier | ✓ VERIFIED | COPY src/ before install; default SOURCE_DATE_EPOCH=0; COPY output/; CMD binds `${PORT:-8000}`. |
| `.dockerignore` | Re-includes demo corpora | ✓ VERIFIED | `output` excluded, `!output/{default,demo,test1}` re-included; db-wal/shm re-excluded. |
| `api/main.py` | SPAStaticFiles index.html fallback | ✓ VERIFIED | `SPAStaticFiles` (main.py:134-155): /api 404 stays JSON, non-api 404 → index.html. Mounted after routers. |
| `viewer/svelte.config.js` | adapter-static SPA | ✓ VERIFIED | `adapter-static` with `fallback: 'index.html'`. |
| `pyproject.toml` | owlready2 → `reasoning` extra | ✓ VERIFIED | owlready2 in `reasoning`/`dev` extras, not core. |
| `README.md` | Updated deploy section | ✓ VERIFIED | Lines 300-375 rewritten. |
| `.planning/STATE.md` | Phase 3.5 outcome + URL | ✓ VERIFIED | Lines 166-172. |
| `03.5-DIAGNOSIS.md` | Root cause + scope + vars | ✓ VERIFIED | 118 lines; Root cause, topology, vars (names only), scope. |
| `.railway` (Plan 01) | Local link state file | ✓ PASSED (accepted deviation) | Not created — v4.31 stores link state globally in `~/.railway/config.json`. Functional intent met: `railway status` resolves the existing project. Documented in DIAGNOSIS + SUMMARY. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| railway.toml [build] | Dockerfile.web | dockerfilePath | ✓ WIRED | `dockerfilePath = "Dockerfile.web"`. |
| railway.toml [deploy] | /health | healthcheckPath | ✓ WIRED | `healthcheckPath = "/health"`; live /health 200. |
| GitHub master | Railway web service | GitHub App + trigger branch | ✓ WIRED | Master pushes auto-triggered builds; final healthy. |
| README | Dockerfile.web + flat railway.toml | documented flow | ✓ WIRED | README references both, embeds the flat toml. |
| api/main.py SPAStaticFiles | viewer/build/index.html | StaticFiles html fallback | ✓ WIRED | Live `/tasks` → 200 SPA shell; `/api/v1/unknown` → JSON 404. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Viewer `/` | corpora list / tree | `/api/v1/corpora`, `/api/v1/tree` ← `load_extraction()` ← bundled `output/{demo,test1}` | Yes | ✓ FLOWING — live API returns 2 real corpora + real FOLIO tree units (not `[]`). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Health probe | `curl /health` | `{"status":"ok"}` | ✓ PASS |
| Root SPA shell | `curl -I /` | HTTP/2 200 text/html, no x-railway-fallback | ✓ PASS |
| SPA JS asset | `curl /_app/.../start.*.js` | 200 application/javascript | ✓ PASS |
| Corpora API | `curl /api/v1/corpora` | `[demo, test1]` | ✓ PASS |
| Tree API (real data) | `curl /api/v1/tree?corpus=demo` | real FOLIO units | ✓ PASS |
| Deep-link SPA fallback | `curl /tasks` | 200 SPA shell | ✓ PASS |
| API 404 stays JSON | `curl /api/v1/nonexistent` | 404 application/json | ✓ PASS |
| railway.toml parses flat | tomllib assertions | OK | ✓ PASS |
| ci/railway.py compiles, no `up --image` | py_compile + grep | OK, 0 matches | ✓ PASS |

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` declared; acceptance is live-URL smoke (infra phase). The
live smoke trio + behavioral spot-checks above serve as the runnable verification. SKIPPED (no probe scripts).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| QUALITY-03 | (supporting only) | Worker Docker image < 500 MB | N/A (correctly) | Worker tier not built in this web-only phase; no worker image produced. Plans' rationale verified against REQUIREMENTS.md (QUALITY-03 is about the worker tier). |
| OBS-04 | (supporting only) | Dagger↔Railway build digest parity | N/A (correctly) | Railway's GitHub-source builder is not the Dagger pipeline; parity is Phase 20 scope. No orphaned REQ maps to 3.5 as primary owner. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No TBD/FIXME/XXX in any modified file | — | Clean. All `output/` empty-bundling and SSR-deferral are documented decisions, not debt markers. |

No blocker or warning anti-patterns. The `requirements.lock` web-branch incompleteness and full-SSR
deferral are explicitly carried to Phase 20 / Plan 05 (documented), not unresolved debt in this phase.

### Human Verification Required

None outstanding. The one item the plan routed to a human (Chrome DevTools MCP screenshot of the
rendered viewer) is independently corroborated here via live HTTP evidence: the `/` SvelteKit shell,
a served SPA JS asset (200), and real corpus tree data from the API. The MCP screenshot itself is
documented in 03.5-03-SUMMARY; this verifier session has no MCP access, but the live data flow makes
the rendered-viewer claim verifiable without it.

### Gaps Summary

No gaps. The phase goal is achieved end-to-end:

- **502 → healthy:** live `/health` 200, `/` 200 with no `x-railway-fallback`.
- **Config-as-code Railway honors:** flat `railway.toml` pins Dockerfile.web + /health (root cause
  was the silently-ignored nested toml — now removed and proven via the bundled-corpora data flow).
- **Viewer renders bundled corpora:** live API returns real demo+test1 corpora and FOLIO tree units;
  the SvelteKit SPA shell + assets serve 200.
- **Auto-deploy on master:** master pushes auto-triggered builds, final reaching healthy.

**Two ACCEPTED deviations** (not gaps), both operator-approved and documented:
1. **SSR → static SPA** for `/` — intent met (200 + viewer renders bundled corpora); full SSR + Gate 4
   deferred to Phase 20. Recorded as an override in this report's frontmatter.
2. **`.railway` local file absent** — Railway CLI v4.31 stores link state globally; functional intent
   met (`railway status` resolves the existing project).

The Plan 02 execution involved heavy, necessary deviations (4 latent-bug fixes in a Dockerfile.web that
had never actually been built/deployed, plus 1 operator-approved corpora-bundling scope add). All are
documented in the SUMMARYs, all were prerequisites to a deploy that had never run, and none undermine
the phase goal — the live URL is the proof.

---

_Verified: 2026-05-22T22:40:00Z_
_Verifier: Claude (gsd-verifier)_
