---
phase: 03.5-railway-dev-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []          # No primary REQ-IDs; QUALITY-03/OBS-04 are SUPPORTING-only and N/A this phase (see must_haves rationale)
user_setup:
  - service: railway
    why: "Interactive `railway login` (device/browser pairing) cannot be scripted by an agent; the repo is logged out with no .railway link"
    dashboard_config:
      - task: "Run `railway login` and complete the browser/device pairing"
        location: "Operator terminal in this repo (in-session)"

must_haves:
  truths:
    - "Operator is authenticated to Railway (`railway whoami` succeeds) and the repo is linked to the existing project/service"
    - "The real 502 root cause is identified from live build/deploy logs, not guessed"
    - "Service topology is known: which services exist (web, and whether a broken worker is wired), service status, and the public-domain target port"
    - "Current service variables are enumerated and cross-checked against .env.example runtime expectations"
    - "Deploy scope is decided (web-only vs web+worker) and recorded, informed by the diagnosis"
  artifacts:
    - path: ".railway"
      provides: "Local link state associating this directory with the existing Railway project/environment/service"
    - path: ".planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md"
      provides: "Recorded 502 root cause, topology snapshot, variable inventory, and scope decision"
      contains: "Root cause"
      min_lines: 20
  key_links:
    - from: "railway CLI session"
      to: "existing Railway project folio-insights-production"
      via: "railway login (human) then railway link"
      pattern: "railway (status|whoami)"
  rationale: >
    QUALITY-03 (worker image <500MB) and OBS-04 (Dagger/Railway build parity) are SUPPORTING-only
    and explicitly N/A this phase: the worker is an idle stub (Phase 10) and stays uncreated in the
    dev environment, so no worker image is built; Railway's GitHub-source builder is not the Dagger
    pipeline, so build-digest parity is Phase 20 scope. Neither is exercised here — do not force a task.
---

<objective>
Authenticate the operator to Railway, link this repo to the existing `folio-insights-production`
project, and diagnose the live HTTP 502 BEFORE any config change is committed. This is the LOCKED
diagnose-first task: it determines the real 502 cause and the current service topology, then resolves
the web-only-vs-web+worker scope decision gate.

Purpose: Avoid blindly applying the flat-toml fix. Research strongly indicates the nested
`[services.*]` `railway.toml` is silently ignored (a prime 502 cause), but suspension, a missing
env var, a wrong domain target-port, or a stranded broken worker service are alternative/compounding
causes that only live diagnosis can rule in or out.
Output: An authenticated + linked CLI session, a `.railway` link, and `03.5-DIAGNOSIS.md` recording
the root cause, topology, variable inventory, and the scope decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-railway-dev-server/03.5-CONTEXT.md
@.planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator authenticates to Railway and agent links the existing project</name>
  <files>.railway</files>
  <read_first>
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("Railway CLI v4.31.0 — Verified Command Surface" → Authentication + Link sections; "Environment Availability")
    - .planning/phases/03.5-railway-dev-server/03.5-CONTEXT.md (Authentication is human-gated)
  </read_first>
  <what-built>
    The agent has prepared the link sequence but CANNOT run `railway login` — it is an interactive
    device/browser pairing with no token argument and no headless-scriptable path (even
    `--browserless` requires a human to open the URL and confirm). The repo is currently logged out
    with no `.railway` link.
  </what-built>
  <how-to-verify>
    Operator runs, in this repo's terminal (same shell the agent will continue in):
    1. `railway login` — complete the browser/device pairing.
    2. `railway whoami` — must print the authenticated account (currently returns "Unauthorized").
    Then signal the agent to continue. The agent will run `railway link` (interactive: pick
    workspace → project `folio-insights-production` → environment → service `web`), or
    `railway link -p <PROJECT> -e <ENVIRONMENT> -s web` once the names are known from
    `railway list` / `railway status --json`. Do NOT create a new project (locked: reuse existing).
  </how-to-verify>
  <action>
    Operator-gated: surface `! railway login` then `railway whoami`. After the operator confirms a
    successful login, the agent runs `railway list` to find the existing project, then
    `railway link` (or `railway link -p ... -e ... -s web`) to associate this directory, producing a
    `.railway` link. Do NOT run `railway init` and do NOT create a new project/service. Do NOT pass
    `RAILWAY_TOKEN` on argv; rely on the interactive session credential.
  </action>
  <verify>
    <automated>railway whoami && railway status --json | jq -e '.project.name'</automated>
  </verify>
  <acceptance_criteria>
    - `railway whoami` exits 0 and prints an account (not "Unauthorized").
    - `railway status --json` exits 0 and reports a linked project named for the existing
      `folio-insights-production` deployment (not a freshly created project).
    - A `.railway` link exists in the repo (`ls .railway` succeeds) — was absent before this task.
  </acceptance_criteria>
  <done>Operator is authenticated (`railway whoami` succeeds) and the repo is linked to the existing project (`.railway` present, `railway status` reports the existing project) — not a new one.</done>
  <resume-signal>Type "logged in" once `railway whoami` shows your account, or describe the auth error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Diagnose the 502, snapshot topology + variables, and record the scope decision</name>
  <files>.planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("Diagnose the 502" + "Code Examples → Diagnose the 502 (Stage 1)"; "Common Pitfalls" 1/3/5/6; "Runtime State Inventory"; "Open Questions" 1 and 3; "Assumptions Log" A1–A5)
    - railway.toml (the current nested two-service config — confirm it is what the deploy used or ignored)
    - .env.example (LLM_PROVIDER/LLM_MODEL + provider key, DOCTOR_URL, sibling-repo paths — runtime var expectations)
    - .env.docker.example (build-digest registry only — NOT runtime vars; do not set these as service vars)
    - Dockerfile.web (lines 71-78: EXPOSE 8000, HEALTHCHECK, CMD binds 0.0.0.0:${PORT:-8000})
    - api/main.py (lines 1-55: GET /health at line 45; confirm no LLM client init at import/startup — resolves A2)
  </read_first>
  <action>
    Run the verified v4.31.0 diagnosis surface against the linked project and capture outputs:
    `railway status --json` (topology: services present, environments, whether a `worker` service is
    wired — Open Question 1), `railway service status` (per-service deploy state — rule out
    suspended/usage-capped per Pitfall 5), `railway logs --build -n 200 --latest` (did the image
    build, and from which Dockerfile — confirm Pitfall 1: nested toml ignored vs Railpack/auto-detect),
    `railway logs --deployment -n 200 --latest -f "@level:error"` (runtime crash / missing env / wrong
    port), and `railway variables list --kv` (present vars vs .env.example expectations). Determine
    the public-domain target port and whether it matches `$PORT`/8000 (Pitfall 3). Confirm via grep of
    api/main.py that no LLM client initializes at import/startup (resolves Assumption A2 — the web tier
    can serve `/health` and `/api/v1/corpora` without an LLM key, but note the key should be set
    defensively in Plan 02). Write `.planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md` with
    sections: Root cause (primary + contributing), Topology snapshot, Variable inventory (present vs
    expected; mark secrets MASKED, never paste values), Domain target-port finding, and Scope decision
    (web-only vs web+worker) with one-line justification. Per research + locked scope, the expected
    decision is web-only with the worker left uncreated; if a broken worker service is found wired,
    record the plan to pause/delete it in the dev environment (do not delete in this plan — note it for
    Plan 02). Do NOT paste any secret value into the diagnosis file or logs (T-03.5-01).
  </action>
  <verify>
    <automated>test -f .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md && grep -qi "root cause" .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md && grep -qiE "web-only|web \+ worker|web\+worker" .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `03.5-DIAGNOSIS.md` exists and contains a "Root cause" section naming the identified 502 cause(s)
      with the supporting log/status evidence referenced.
    - The file records a Scope decision matching one of {web-only, web+worker} with justification.
    - The file lists current service variables by NAME only (no secret values) cross-checked against
      .env.example; missing runtime vars are flagged for Plan 02.
    - The file states whether a `worker` service is currently wired in the env and whether the public
      domain target port matches `$PORT`/8000.
    - `grep -niE "(sk-|ANTHROPIC_API_KEY=|RAILWAY_TOKEN=)[a-z0-9]" .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md` returns no matches (no leaked secret values).
  </acceptance_criteria>
  <done>
    Live 502 root cause is identified with evidence, topology + variables snapshotted, and the deploy
    scope decision is recorded — unblocking the Plan 02 config-fix without guessing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Railway API | Auth credential + project actions cross here; secrets must not hit argv/logs |
| repo files → git | Diagnosis output is committed; must not contain secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.5-01 | Information Disclosure | `railway variables list` output / 03.5-DIAGNOSIS.md | mitigate | Record variable NAMES only, never values; acceptance grep asserts no `sk-`/`*_API_KEY=`/`RAILWAY_TOKEN=` literals in the committed file |
| T-03.5-02 | Information Disclosure | deploy/build logs in transcript | mitigate | Use `-f "@level:error"` to scope log volume; do not paste full logs containing env dumps into committed docs |
| T-03.5-03 | Spoofing | wrong-project link | accept | Operator confirms project name during `railway link`; acceptance asserts existing project, not a new one |
| T-03.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (Railway CLI v4.31.0 already installed, verified); RESEARCH.md "Package Legitimacy Audit" = Not Applicable. No install task to gate. |
</threat_model>

<verification>
- `railway whoami` and `railway status --json` succeed (authenticated + linked to existing project).
- `03.5-DIAGNOSIS.md` exists with Root cause + Scope decision + variable inventory (names only).
- No secret values appear in any committed file.
</verification>

<success_criteria>
Operator authenticated, repo linked to the existing service, 502 root cause identified from live
evidence, and the web-only-vs-web+worker scope decision recorded — all before any `railway.toml`
change is committed.
</success_criteria>

<output>
Create `.planning/phases/03.5-railway-dev-server/03.5-01-SUMMARY.md` when done.
Also produce `.planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md` (consumed by Plan 02).
</output>
