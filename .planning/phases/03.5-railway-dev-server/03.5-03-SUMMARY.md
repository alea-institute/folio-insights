---
phase: 03.5-railway-dev-server
plan: 03
subsystem: infra
tags: [railway, smoke-test, chrome-devtools, auto-deploy, docs]

requires:
  - phase: 03.5
    provides: healthy Railway deploy from Plan 02
provides:
  - Live-URL smoke proof (/health, /, /api/v1/corpora) + Chrome MCP screenshot of the rendered viewer
  - Auto-deploy-on-master proof
  - Accurate README "Deploying to Railway" + STATE.md Phase 3.5 record
affects: []

tech-stack:
  added: []
  patterns: ["Visual verification via Chrome DevTools MCP (CLAUDE.md mandate)"]

key-files:
  created: []
  modified: [README.md, .planning/STATE.md]

key-decisions:
  - "Auto-deploy proven by the Plan 02 master pushes (each auto-triggered a Railway build; final healthy) — no redundant no-op push"

patterns-established: []
requirements-completed: []

duration: ~15min
completed: 2026-05-22
---

# Phase 03.5 Plan 03: Prove the Restored Server + Update Docs

**Verified the restored dev server end-to-end on the live URL (smoke trio + Chrome DevTools MCP screenshot showing the viewer rendering the bundled corpora), confirmed auto-deploy on master, and updated README + STATE to the restored web-only SPA reality.**

## Performance
- **Duration:** ~15 min
- **Completed:** 2026-05-22
- **Tasks:** 2 (smoke+screenshot+auto-deploy; docs)

## Accomplishments
- **Live smoke trio passes:** `/health` → 200 `{"status":"ok"}`; `/` → HTTP/2 200 `text/html` with **no** `x-railway-fallback`; `/api/v1/corpora` → 200 (demo + test1). Deep link `/tasks` → 200 (SPA fallback); `/api/v1/<unknown>` → JSON 404.
- **Visual verification (Chrome DevTools MCP):** navigated the live URL, waited for content, screenshotted, inspected. The viewer renders fully — FOLIO Insights header, Demo/Test1 corpus selector, FOLIO concept tree (All Units / Litigation), knowledge units with real advocacy text (BEST_PRACTICE / ADVICE), confidence scores, and Approve/Reject/Edit/Skip controls. Screenshot deleted after inspection (CLAUDE.md).
- **Auto-deploy proven:** the Plan 02 pushes to `master` (`a275b59`, `197d67a`) each auto-triggered a Railway build; the final one (`c5e4b32e`, commit `197d67a`) built and reached healthy. Master auto-deploy is conclusively demonstrated.
- **Docs updated:** README "Deploying to Railway" now reflects flat railway.toml + Dockerfile.web + adapter-static SPA + railway link (existing project) + masked vars/BYOK + master auto-deploy + web-only/Phase 20. STATE.md records the Phase 3.5 outcome, restored live URL, and decisions.

## Task Commits
1. **Task 1 (smoke + screenshot + auto-deploy):** operational/live verification (no repo files; auto-deploy proven via Plan 02 master pushes)
2. **Task 2 (docs):** `48f14e0` (README + STATE)

## Decisions Made
- Used the existing Plan 02 master-push auto-deploys as the auto-deploy proof rather than a redundant no-op push (each ~8-min build), since they already demonstrate a fresh master-triggered build reaching healthy.

## Deviations from Plan
- **Smoke labeled "SSR" → SPA:** the plan expected `/` to be SSR; the restored viewer is a static SPA (Plan 02 decision). `/` still returns 200 `text/html` and the viewer renders the bundled corpora — acceptance met, just SPA not SSR.
- **Source-context panel** shows a graceful `{"found":false}` placeholder: the source route resolves `file` as a literal path (bare basename) and never prepended the corpus/sources dir — a **pre-existing** limitation (the old deploy returned the same found:false 200), unrelated to this phase. Noted as a follow-up.

## Issues Encountered
- Local `docker run -p` host networking was blocked (IPv4 forwarding disabled, no privilege); validated container endpoints via loopback `docker exec` instead.

## Next Phase Readiness
- Phase 3.5 goal achieved: 502 → healthy web-only dev server, viewer renders bundled corpora, auto-deploy on master proven, docs accurate.
- Deferred to Phase 20 / Plan 05: full SSR + Gate 4 SSR budget; complete hash-pinned web requirements.lock; BYOK key-input UI; source-panel path fix.

---
*Phase: 03.5-railway-dev-server*
*Completed: 2026-05-22*
