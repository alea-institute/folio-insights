---
phase: 03.5-railway-dev-server
plan: 02
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified: ["railway.toml", "ci/railway.py"]
autonomous: false
requirements: []          # No primary REQ-IDs; QUALITY-03/OBS-04 SUPPORTING-only and N/A this phase
user_setup:
  - service: railway
    why: "Setting the trigger branch on the EXISTING service is dashboard-only (CLI `add --repo` is for new services); the GitHub App contributor access cannot be granted by the agent"
    dashboard_config:
      - task: "Service → Settings → Source → connect repo alea-institute/folio-insights → set trigger branch = master"
        location: "Railway dashboard for the folio-insights-production service"
      - task: "Confirm the Railway GitHub App is installed on alea-institute/folio-insights with no pending permission updates"
        location: "GitHub → Settings → Applications → Railway"

must_haves:
  truths:
    - "railway.toml is a flat single-service web config (no [services.*] nesting) so config-as-code actually applies"
    - "The flat config pins builder=DOCKERFILE, dockerfilePath=Dockerfile.web, healthcheckPath=/health, healthcheckTimeout, restartPolicy"
    - "Build logs confirm the deploy used Dockerfile.web (not Railpack/auto-detect)"
    - "Required runtime variables are present as masked Railway variables on the web service"
    - "The existing service's GitHub source trigger branch is master"
    - "A clean redeploy of the corrected config reaches healthy state and /health returns 200"
    - "ci/railway.py no longer invokes the broken `railway up --image` path (fixed for v4.x)"
  artifacts:
    - path: "railway.toml"
      provides: "Flat single-service web config Railway applies"
      contains: "[build]"
    - path: "ci/railway.py"
      provides: "Deploy wrapper corrected for CLI v4.x (no `up --image`)"
      contains: "add"
  key_links:
    - from: "railway.toml [build]"
      to: "Dockerfile.web"
      via: "dockerfilePath"
      pattern: "dockerfilePath\\s*=\\s*\"Dockerfile.web\""
    - from: "railway.toml [deploy]"
      to: "/health endpoint"
      via: "healthcheckPath"
      pattern: "healthcheckPath\\s*=\\s*\"/health\""
    - from: "GitHub repo alea-institute/folio-insights (master)"
      to: "Railway web service"
      via: "GitHub App source + trigger branch"
      pattern: "master"
  rationale: >
    QUALITY-03 and OBS-04 remain N/A: the worker is left uncreated (web-only scope), so no worker
    image is built; Railway's builder is not the Dagger pipeline, so build-parity is Phase 20.
---

<objective>
Apply the diagnosed fix: collapse `railway.toml` to a valid flat single-service web config so
config-as-code actually engages, set the required runtime variables as masked Railway variables,
wire the existing service's GitHub source trigger branch to `master` (dashboard), and trigger a
clean redeploy that reaches healthy state. Also correct `ci/railway.py`'s broken v3-era
`railway up --image` invocation for CLI v4.x.

Purpose: The nested `[services.web]`/`[services.worker]` toml is silently ignored by Railway
(unsupported feature request), so the committed builder/healthcheck never applied — a prime 502
cause. Flattening to a single web-tier config makes Railway honor `Dockerfile.web` and the
`/health` healthcheck. Web-only scope is locked (worker is an idle stub until Phase 10).
Output: A corrected `railway.toml`, a fixed `ci/railway.py`, masked runtime variables, master-branch
auto-deploy wiring, and a healthy redeploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-railway-dev-server/03.5-CONTEXT.md
@.planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md
@.planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md

<interfaces>
<!-- Current (invalid, nested) railway.toml shape — Plan 01 confirmed it is silently ignored. -->
<!-- Target: flat schema per docs.railway.com/config-as-code/reference. Field VALUES below already -->
<!-- mirror the repo; only the [services.*] nesting is removed. Executor uses these directly. -->

Current railway.toml (to be replaced): nested [services.web] / [services.web.build] /
[services.web.deploy] and [services.worker] / [services.worker.build] / [services.worker.deploy].

Target flat keys (web-only):
  [build]  builder = "DOCKERFILE", dockerfilePath = "Dockerfile.web"
  [deploy] healthcheckPath = "/health", healthcheckTimeout = 120,
           restartPolicyType = "ON_FAILURE", restartPolicyMaxRetries = 3

Dockerfile.web already binds 0.0.0.0:${PORT:-8000} and EXPOSE 8000 (verified) — no change needed.

ci/railway.py deploy_service(): the `image` branch currently appends `--image` to `railway up`,
which has NO --image flag on v4.x. v4.x image-deploy is `railway add -i <IMAGE> -s <service>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse railway.toml to a flat single-service web config and fix ci/railway.py for v4.x</name>
  <files>railway.toml, ci/railway.py</files>
  <read_first>
    - railway.toml (current nested two-service config — the whole file is replaced)
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("Pattern 1: Flat single-service config-as-code"; "v3 -> v4 changes the planner must know"; "Pitfall 2"; "Common Pitfalls 1")
    - .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md (confirmed scope decision — proceed web-only unless diagnosis recorded web+worker)
    - ci/railway.py (deploy_service: the `--image` branch at lines 39-45 is broken on v4.x)
    - Dockerfile.web (lines 71-78 — confirm dockerfilePath + healthcheck target are correct; no edit)
  </read_first>
  <action>
    Replace the entire `railway.toml` with a FLAT single-service web config: a top-level `[build]`
    table with builder = "DOCKERFILE" and dockerfilePath = "Dockerfile.web", and a top-level
    `[deploy]` table with healthcheckPath = "/health", healthcheckTimeout = 120, restartPolicyType =
    "ON_FAILURE", restartPolicyMaxRetries = 3. Remove ALL `[services.web]` / `[services.worker]`
    nesting — leave the worker entirely out (web-only, locked scope; worker is an idle stub until
    Phase 10). Keep a short header comment citing docs.railway.com/config-as-code/reference and noting
    that multi-service for Phase 20 will use the Railway monorepo per-directory pattern, not nested
    tables. Do NOT add a startCommand (Dockerfile.web CMD expands ${PORT:-8000}; adding one bypasses
    that substitution — prior decision 01-02). Then fix `ci/railway.py`: in `deploy_service`, replace
    the broken `railway up --service X --image Y` path with the v4.x form. When `image` is provided,
    build the command as `railway add -i <image> -s <service_name>` (image-deploy moved to
    `railway add` on v4.x); when `image` is None keep `railway up --service <name> --ci`
    (build-from-source). Update the stale code comment (currently "railway-cli >= 3.x") to note the
    v4.x correction. Preserve the T-00-19 mitigation: token via `env=` only, never on argv, never
    logged. Do not change the module's public signature.
  </action>
  <verify>
    <automated>python -c "import tomllib,pathlib; d=tomllib.loads(pathlib.Path('railway.toml').read_text()); assert 'build' in d and 'deploy' in d; assert 'services' not in d; assert d['build']['dockerfilePath']=='Dockerfile.web'; assert d['deploy']['healthcheckPath']=='/health'; print('railway.toml flat OK')" && python -m py_compile ci/railway.py && test "$(grep -vE '^\s*#' ci/railway.py | grep -c 'up.*--image')" -eq 0 && grep -vE '^\s*#' ci/railway.py | grep -q 'add'</automated>
  </verify>
  <acceptance_criteria>
    - `railway.toml` parses as TOML with top-level `[build]` and `[deploy]` tables and NO `services` key.
    - `railway.toml` `[build].dockerfilePath == "Dockerfile.web"` and `[deploy].healthcheckPath == "/health"`.
    - No `[services.web]` / `[services.worker]` tables remain (`grep -c '\[services' railway.toml` == 0).
    - `ci/railway.py` compiles (`python -m py_compile ci/railway.py` exits 0) and has no live (non-comment)
      `railway up ... --image` invocation; the image branch uses `railway add -i`.
    - The T-00-19 pattern is preserved: `RAILWAY_TOKEN` passed via `env=` only, not on argv, not logged
      (`grep -n 'env=' ci/railway.py` still present; no `cmd.*token`/`print.*token`).
  </acceptance_criteria>
  <done>
    railway.toml is a valid flat web-only config Railway will honor, and ci/railway.py's deploy wrapper
    is correct for CLI v4.31.0.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set masked runtime variables, wire master trigger branch (dashboard), and trigger a clean redeploy</name>
  <files>(operational — Railway service variables + dashboard source/branch; no repo files)</files>
  <read_first>
    - .planning/phases/03.5-railway-dev-server/03.5-DIAGNOSIS.md (missing-variable list flagged in Plan 01; whether a broken worker service must be paused/deleted; domain target-port finding)
    - .planning/phases/03.5-railway-dev-server/03.5-RESEARCH.md ("Set runtime variables (secrets)"; "Deploy"; "Pattern 2: Auto-deploy on push to master"; "Common Pitfalls 3/5/6"; "Assumptions Log A2/A3")
    - .env.example (LLM_PROVIDER, LLM_MODEL, provider API key, DOCTOR_URL — runtime var names)
  </read_first>
  <what-built>
    The agent sets/confirms required runtime variables as MASKED Railway variables using the modern
    v4.x form, then commits and triggers a clean redeploy from the corrected flat toml. Two steps are
    operator-gated because they are dashboard-only or require secret values the agent must not
    fabricate: (a) the GitHub source trigger branch on the EXISTING service is dashboard-only, and
    (b) the operator supplies any secret API-key value via stdin.
  </what-built>
  <how-to-verify>
    Agent does (autonomously, after operator provides any secret values via stdin):
    1. For each non-secret runtime var flagged missing in DIAGNOSIS (e.g. LLM_PROVIDER=anthropic,
       LLM_MODEL=claude-sonnet-4-6): `railway variable set KEY=value -s web --skip-deploys`.
    2. For each secret (e.g. ANTHROPIC_API_KEY): `railway variable set ANTHROPIC_API_KEY --stdin -s web --skip-deploys`
       reading the value from stdin (never argv, never shell history) — operator pastes when prompted.
       Defensive: set the LLM provider key even though A2 confirms /health and /api/v1/corpora do not
       need it at boot.
    3. If DIAGNOSIS found a broken `worker` service wired in the env, pause or remove it in the dev
       environment so it cannot keep failing (web-only scope).
    Operator does (dashboard, cannot be scripted):
    4. Service -> Settings -> Source: confirm repo = alea-institute/folio-insights and set trigger
       branch = master. Confirm the public domain target port is 8000 / matches $PORT (Pitfall 3).
    Then agent triggers the build:
    5. `railway up -s web -c` (build + stream build logs, then exit) OR push a commit to master to let
       auto-deploy build. Watch `railway logs --build -n 200 --latest` and confirm the build used
       Dockerfile.web (NOT Railpack/auto-detect — proves the flat toml engaged).
  </how-to-verify>
  <action>
    Set the runtime variables flagged in DIAGNOSIS as masked Railway variables on `-s web`: non-secrets
    via `railway variable set KEY=value -s web --skip-deploys`; secrets via
    `railway variable set KEY --stdin -s web --skip-deploys` (stdin only — T-03.5-01). Set the LLM
    provider key defensively. If a broken worker service exists in the dev env (per DIAGNOSIS), pause
    or `railway down`/remove it for web-only scope. Surface the dashboard step `! Service -> Settings
    -> Source -> trigger branch = master` and the target-port confirmation to the operator. After the
    operator confirms the source/branch, trigger a clean build with `railway up -s web -c` (or via a
    master push) and tail `railway logs --build -n 200 --latest` to confirm the build used
    Dockerfile.web and the deploy reaches healthy state. Do NOT echo any secret value into logs,
    commits, or the transcript.
  </action>
  <verify>
    <automated>railway variables list --kv -s web | grep -q "LLM_PROVIDER" && curl -sf "https://folio-insights-production.up.railway.app/health" | jq -e '.status=="ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `railway variables list --kv -s web` shows the runtime vars flagged in DIAGNOSIS as present
      (LLM_PROVIDER at minimum); secret values are masked in output.
    - Build logs (`railway logs --build --latest`) show the build used `Dockerfile.web` (not
      Railpack/auto-detect) — proving the flat toml engaged.
    - The deploy reaches healthy state and `curl -sf "$URL/health" | jq -e '.status=="ok"'` succeeds
      where `$URL=https://folio-insights-production.up.railway.app`.
    - The existing service's GitHub source trigger branch is `master` (operator-confirmed in dashboard).
    - No secret value appears in logs, commit, or transcript.
  </acceptance_criteria>
  <done>Required runtime vars are set as masked variables on `-s web`, the master trigger branch is wired on the existing service, and a clean redeploy of the flat config reaches healthy state with `/health` returning 200.</done>
  <resume-signal>Type "branch set + deployed" once the dashboard trigger branch is master and the redeploy is healthy, or describe the deploy/build error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator stdin → Railway variables | Secret API-key values cross here; must use `--stdin`, never argv |
| GitHub master push → Railway build | Auto-deploy of a master push triggers an unreviewed-by-Railway build |
| repo files → git | railway.toml / ci/railway.py are committed; must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.5-01 | Information Disclosure | `railway variable set` secret values | mitigate | Use `--stdin` for all secret vars so values never hit argv/shell history; non-secrets via `KEY=value`; never echo values; matches ci/railway.py T-00-19 |
| T-03.5-04 | Tampering | auto-deploy of an unreviewed master push | accept | `master` is the team's reviewed branch by convention; optional Railway "Wait for CI" gate documented but not required for a dev server |
| T-03.5-05 | Elevation of Privilege | Railway GitHub App scope | mitigate | App granted only on alea-institute/folio-insights; operator reviews pending permission updates during dashboard wiring |
| T-03.5-06 | Information Disclosure | RAILWAY_TOKEN in ci/railway.py | mitigate | Preserve env=-only token passing; no argv, no logging (T-00-19 carried forward) |
| T-03.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (Railway CLI v4.31.0 already installed); RESEARCH.md Package Legitimacy Audit = Not Applicable |
</threat_model>

<verification>
- `railway.toml` is flat (no `[services.*]`), pins Dockerfile.web + /health; parses as TOML.
- `ci/railway.py` compiles and uses `railway add -i` for image deploys (no `up --image`).
- Required runtime vars present as masked variables on `-s web`.
- Build used Dockerfile.web; redeploy healthy; `/health` returns 200 `{"status":"ok"}`.
- Trigger branch = master on the existing service (dashboard-confirmed).
</verification>

<success_criteria>
The corrected flat config is committed, Railway honors Dockerfile.web + the /health healthcheck,
required masked variables are set, the master trigger branch is wired, and a clean redeploy reaches
healthy state with `/health` returning 200.
</success_criteria>

<output>
Create `.planning/phases/03.5-railway-dev-server/03.5-02-SUMMARY.md` when done.
</output>
