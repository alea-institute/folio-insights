---
phase: 05-content-versioning-6-4
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/folio_insights/revision/content_edit_shape.ttl
  - src/folio_insights/revision/shape_validation.py
  - tests/revision/test_forward_only_validator.py
  - tests/revision/test_shacl_forward_only.py
autonomous: true
requirements: [SHARD-09]
must_haves:
  truths:
    - "A real pyshacl TTL shape (content_edit_shape.ttl) enforces forward-only monotonic edited_at via a sh:sparql self-join over the edit chain [D-07.2]"
    - "validate_content_edit_shape(shard) builds a minimal local RDF graph + runs pyshacl and reports conformance [D-07.2]"
    - "The SHACL guard rejects an edit to a past version (back-dated insert) — conforms=False; an ordered chain conforms=True (exit criterion 2) [D-08b]"
    - "The pyshacl/rdflib code lives in revision/ (NOT shards/) so the dep-leak guard stays green [boundary constraint]"
    - "SHACL enforces only the forward-only half; immutability of past entries is carried by Pydantic frozen + the gate (documented division) [D-08a vs D-08b]"
    - "The Pydantic @model_validator (Plan 01) is independently confirmed as the authoritative forward-only gate [D-07.1]"
  artifacts:
    - path: "src/folio_insights/revision/content_edit_shape.ttl"
      provides: "Forward-only sh:sparql NodeShape over the edit chain"
      contains: "sh:sparql"
    - path: "src/folio_insights/revision/shape_validation.py"
      provides: "validate_content_edit_shape() — model->RDF->pyshacl defense-in-depth"
      contains: "def validate_content_edit_shape"
    - path: "tests/revision/test_shacl_forward_only.py"
      provides: "SHACL-guard exit-criterion-2 test (negative + positive polarity)"
      contains: "def test_"
  key_links:
    - from: "src/folio_insights/revision/shape_validation.py"
      to: "pyshacl.validate"
      via: "pyshacl.validate(data_graph, shacl_graph=shapes, inference='none', abort_on_first=False)"
      pattern: "pyshacl\\.validate"
    - from: "src/folio_insights/revision/shape_validation.py"
      to: "content_edit_shape.ttl"
      via: "Graph().parse(..., format='turtle')"
      pattern: "parse\\(.*turtle"
---

<objective>
Ship the literal "SHACL guard" (exit criterion 2, D-07.2): a real pyshacl TTL shape
(`content_edit_shape.ttl`) enforcing forward-only monotonic `edited_at` via a
`sh:sparql` self-join over an `rdf:List` edit chain, plus
`validate_content_edit_shape(shard)` that builds a minimal local RDF graph from the
shard's `content_edits` and runs pyshacl against it (defense-in-depth, mirroring the
Phase 1 distinguo `validate_*_shape()` idiom). Add the SHACL forward-only test
(negative + positive polarity) and a focused test confirming the Plan-01 Pydantic
`@model_validator` is the authoritative gate.

Purpose: Exit criterion 2 is read LITERALLY (operator decision) — a real pyshacl
shape must exist in Phase 5, not be deferred to Phase 11. This is defense-in-depth
beside the always-on Pydantic validator. SHACL enforces ONLY the forward-only half
(it is stateless over a single snapshot and cannot see deletions of past entries —
RESEARCH L115-124); the immutability half is carried by ContentEdit frozen + the
IMMUTABLE_FIELD_PATHS gate. This division is stated explicitly so no effort is wasted
making SHACL do what it can't.

Output: `revision/content_edit_shape.ttl`, `revision/shape_validation.py`,
`tests/revision/test_shacl_forward_only.py`, `tests/revision/test_forward_only_validator.py`.

LOAD-BEARING BOUNDARY: the rdflib/pyshacl code MUST live in `revision/`, NEVER under
`src/folio_insights/shards/` — `tests/shards/test_dep_leak_guard.py` L23 forbids
rdflib/pyoxigraph/oxrdflib/owlready2 imports in shards/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-versioning-6-4/05-CONTEXT.md
@.planning/phases/05-content-versioning-6-4/05-RESEARCH.md
@.planning/phases/05-content-versioning-6-4/05-PATTERNS.md
@.planning/phases/05-content-versioning-6-4/05-01-SUMMARY.md

<interfaces>
<!-- Enriched ContentEdit (from Plan 05-01): field_path, edited_at (datetime), ... -->

<!-- pyshacl load/run pattern (services/shacl_validator.py L64-86) — copy exactly: -->
<!--   shapes = Graph(); shapes.parse(str(path), format="turtle")           -->
<!--   conforms, _g, results_text = pyshacl.validate(                       -->
<!--     data_graph, shacl_graph=shapes, inference="none", abort_on_first=False) -->
<!--   ValidationResult dataclass: conforms / violations / results_text (L26-32) -->

<!-- VERIFIED sh:sparql forward-only shape (RESEARCH L83-103) — spiked live   -->
<!--   against pyshacl 0.31.0: SELECT matches the BAD (back-dated) condition  -->
<!--   FILTER(?ct < ?pt) over adjacent fi:seq pairs; a violation fires when   -->
<!--   the SELECT returns >=1 row. Polarity is load-bearing (Pitfall 1).      -->

<!-- Minimal RDF mapping (RESEARCH L79): each shard node -> fi:contentEdits   -->
<!--   -> rdf:List of edit nodes; each edit node carries fi:editedAt          -->
<!--   (xsd:dateTime) + integer fi:seq (0,1,2,...) = append order.            -->

<!-- distinguo idiom (polysemy/distinguo.py L99-127): defense-in-depth        -->
<!--   validate_*_shape naming/intent — but D-07.2 wants a REAL pyshacl run.  -->

<!-- export/shapes.ttl prefix block (L1-7) — copy prefix style; DO NOT append  -->
<!--   to that file (it targets the OWL export graph, a different domain).     -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: content_edit_shape.ttl + validate_content_edit_shape() (model->RDF->pyshacl)</name>
  <files>src/folio_insights/revision/content_edit_shape.ttl, src/folio_insights/revision/shape_validation.py</files>
  <read_first>
    - .planning/phases/05-content-versioning-6-4/05-RESEARCH.md L71-132 (the FULL verified SHACL-guard recipe: minimal RDF mapping, the working sh:sparql shape, the load/run pattern, the CAN/CANNOT division, the validate_content_edit_shape naming)
    - src/folio_insights/services/shacl_validator.py L1-106 (the pyshacl load/validate pattern + ValidationResult dataclass shape to mirror exactly)
    - src/folio_insights/polysemy/distinguo.py L99-127 (the validate_*_shape defense-in-depth naming/intent analog)
    - src/folio_insights/export/shapes.ttl L1-23 (prefix block + NodeShape structure — copy the style, do NOT append to this file)
    - .planning/phases/05-content-versioning-6-4/05-PATTERNS.md L138-160 (shape_validation.py + content_edit_shape.ttl assignments) and L241-248 (LOAD-BEARING boundary constraint)
    - tests/shards/test_dep_leak_guard.py (the boundary this file must NOT trip — must live in revision/, not shards/)
  </read_first>
  <action>
    Create `src/folio_insights/revision/content_edit_shape.ttl`: copy the prefix block style from `export/shapes.ttl` L1-7 (declare `sh:`, `rdf:`, `xsd:`, and a phase-local `fi:` namespace) and the VERIFIED forward-only `sh:sparql` NodeShape from RESEARCH L83-103 verbatim — `fi:ForwardOnlyShape a sh:NodeShape ; sh:targetClass fi:Shard ; sh:sparql [ ... ]` with the SELECT matching the BAD condition `FILTER(?ct < ?pt)` over adjacent `fi:seq` pairs (polarity is load-bearing — a violation fires when the SELECT returns >=1 row; Pitfall 1). Do NOT append to `export/shapes.ttl`. Create `src/folio_insights/revision/shape_validation.py`: import `pyshacl` and `from rdflib import Graph, Namespace, Literal, BNode, RDF` (these imports are SAFE here — revision/ is outside shards/). Define a `ValidationResult` dataclass mirroring `shacl_validator.py` L26-32 (`conforms: bool`, `violations: list[str]`, `results_text: str`) — or import/reuse it. Define `_build_edit_graph(shard) -> Graph`: emit the shard as an `fi:Shard` node with `fi:contentEdits` pointing at an `rdf:List` (rdflib `Collection` or manual rdf:first/rdf:rest BNodes) of edit nodes; each edit node carries `fi:editedAt` as a typed `Literal(edit.edited_at.isoformat(), datatype=XSD.dateTime)` and `fi:seq` as `Literal(i)` (integer, 0-based append order) — exactly the RESEARCH L79 mapping. Define `validate_content_edit_shape(shard) -> ValidationResult`: lazy-`Graph().parse(<shape ttl path>, format="turtle")`, build the data graph via `_build_edit_graph(shard)`, run `pyshacl.validate(data_graph, shacl_graph=shapes, inference="none", abort_on_first=False)` (copy the `shacl_validator.py` L81-86 call exactly), parse violation messages into the `ValidationResult` (mirror L88-100), and return it. Resolve the TTL path as `Path(__file__).parent / "content_edit_shape.ttl"`. Module docstring must state: this is the DEFENSE-IN-DEPTH forward-only SHACL guard (D-07.2); the AUTHORITATIVE gate is the Pydantic @model_validator (Plan 01); SHACL CANNOT enforce immutability of past entries (RESEARCH L115-124) — that half is Pydantic frozen + the IMMUTABLE_FIELD_PATHS gate.
  </action>
  <verify>
    <automated>uv run python -c "from folio_insights.revision.shape_validation import validate_content_edit_shape; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/folio_insights/revision/content_edit_shape.ttl` exists and contains `sh:sparql` and `FILTER(?ct < ?pt)` (load-bearing polarity).
    - `src/folio_insights/revision/shape_validation.py` contains `def validate_content_edit_shape(` and `pyshacl.validate(`.
    - `src/folio_insights/revision/shape_validation.py` does NOT append to or reference `export/shapes.ttl`.
    - The file is under `revision/`, NOT `shards/`: `uv run pytest tests/shards/test_dep_leak_guard.py -x` exits 0 (no rdflib leak into shards/).
    - `uv run python -c "from folio_insights.revision.shape_validation import validate_content_edit_shape"` exits 0 (imports + TTL parse path resolve).
  </acceptance_criteria>
  <done>content_edit_shape.ttl ships the verified forward-only sh:sparql shape; validate_content_edit_shape builds local RDF + runs pyshacl; lives in revision/ (dep-leak guard green); CAN/CANNOT division documented.</done>
</task>

<task type="auto">
  <name>Task 2: SHACL forward-only test (both polarities, exit criterion 2) + validator confirmation test</name>
  <files>tests/revision/test_shacl_forward_only.py, tests/revision/test_forward_only_validator.py</files>
  <read_first>
    - .planning/phases/05-content-versioning-6-4/05-RESEARCH.md L105 (negative case conforms=False, positive conforms=True — the exact polarity to assert) and L365-369 (Pitfall 1 — test both polarities)
    - src/folio_insights/revision/shape_validation.py (the validate_content_edit_shape API from Task 1 — returns ValidationResult with .conforms)
    - tests/shards/conftest.py (the _sample_shard builder + the _content_edit helper added in Plan 05-01 — reuse to build edit chains)
    - tests/shards/test_audit_log.py L1-25 (module header + pytestmark = pytest.mark.shards conventions)
    - src/folio_insights/shards/envelope.py (the Plan-01 @model_validator on ShardEnvelope — the authoritative gate this test confirms independently)
  </read_first>
  <action>
    Create `tests/revision/test_shacl_forward_only.py` (`pytestmark = pytest.mark.shards`): build a `SimpleAssertionShard` (via `_sample_shard`) whose `content_edits` is an ORDERED chain (strictly-increasing `edited_at`) and assert `validate_content_edit_shape(shard).conforms is True` (positive polarity). Build a second shard whose edit chain has a BACK-DATED entry (an edit whose `edited_at` precedes its predecessor's — construct it by `model_construct` or by appending to a list that bypasses the Plan-01 validator, since the validator would otherwise reject it at construction; use `SimpleAssertionShard.model_construct(...)` or set the list then call validate_content_edit_shape directly on the object without re-validating through Pydantic) and assert `validate_content_edit_shape(shard).conforms is False` with at least one violation mentioning the forward-only message (negative polarity — exit criterion 2: "SHACL guard rejects edits to past versions"). Create `tests/revision/test_forward_only_validator.py` (`pytestmark = pytest.mark.shards`): independently confirm the Plan-01 AUTHORITATIVE Pydantic `@model_validator` — assert an ordered chain validates and a non-monotonic chain raises `ValueError`/`ValidationError` when constructed/re-validated through `SimpleAssertionShard.model_validate(...)` (D-07.1). Add a comment in both modules noting the two-layer division (validator authoritative; SHACL defense-in-depth; immutability half is frozen + gate). `async`/decorator: these are sync tests; no asyncio needed. Reuse the `shards` marker (no new marker).
  </action>
  <verify>
    <automated>uv run pytest tests/revision/test_shacl_forward_only.py tests/revision/test_forward_only_validator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/revision/test_shacl_forward_only.py` exists, contains `pytestmark = pytest.mark.shards`, and asserts BOTH `conforms is True` (ordered) and `conforms is False` (back-dated) — both polarities (Pitfall 1).
    - `tests/revision/test_forward_only_validator.py` exists and asserts the Pydantic validator rejects a non-monotonic chain (D-07.1).
    - `uv run pytest tests/revision/test_shacl_forward_only.py -x` exits 0 (exit criterion 2 — SHACL guard rejects edits to past versions).
    - `uv run pytest tests/revision/test_forward_only_validator.py -x` exits 0.
  </acceptance_criteria>
  <done>SHACL forward-only guard tested in both polarities (exit criterion 2 green); the authoritative Pydantic validator independently confirmed; two-layer division documented in the tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shard content_edits -> RDF graph -> pyshacl | The edit chain is mapped to RDF and validated; SHACL is a defense-in-depth integrity check on the forward-only invariant |
| shacl code -> shards/ package | The rdflib/pyshacl imports must NOT cross into shards/ (dep-leak boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | back-dated edit insert (history forgery) | mitigate | sh:sparql forward-only shape flags any adjacent pair where editedAt decreases (defense-in-depth beside the authoritative Pydantic validator); polarity verified both ways |
| T-05-12 | Tampering | dep-leak — rdflib/pyshacl import drifting into shards/ | mitigate | shape_validation.py + content_edit_shape.ttl live in revision/; tests/shards/test_dep_leak_guard.py enforces the boundary and is in the verification gate |
| T-05-13 | Repudiation/Tampering | mutation/deletion of past entries (NOT SHACL-enforceable) | accept | SHACL is stateless over one snapshot (RESEARCH L115-124); this half is carried by ContentEdit frozen + IMMUTABLE_FIELD_PATHS gate (Plan 01 + Plan 02). Division documented; not a gap. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: pyshacl/rdflib pre-existing/pinned). No install task. |
</threat_model>

<verification>
- `uv run pytest tests/revision/test_shacl_forward_only.py -x` exits 0 (exit criterion 2).
- `uv run pytest tests/shards/test_dep_leak_guard.py -x` exits 0 (no rdflib leak into shards/).
- `uv run pytest tests/revision -x -q` green when combined with Plan 02 modules.
</verification>

<success_criteria>
- A real pyshacl TTL shape (content_edit_shape.ttl) + validate_content_edit_shape() enforce forward-only monotonicity (D-07.2).
- Exit criterion 2 green: SHACL guard rejects a back-dated edit (conforms=False) and an ordered chain conforms (conforms=True), both polarities tested.
- The pyshacl/rdflib code lives in revision/; dep-leak guard stays green.
- The two-layer division (Pydantic authoritative + SHACL defense-in-depth; immutability via frozen+gate) is documented in code and tests.
</success_criteria>

<output>
Create `.planning/phases/05-content-versioning-6-4/05-03-SUMMARY.md` when done.
</output>
