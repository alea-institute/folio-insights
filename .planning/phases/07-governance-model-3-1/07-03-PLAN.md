---
phase: 07-governance-model-3-1
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/folio_insights/governance/log.py
  - src/folio_insights/governance/shapes/governance_log_shape.ttl
  - src/folio_insights/governance/shape_validation.py
  - tests/governance/test_governance_log_is_append_only.py
  - tests/governance/test_governance_log_protocol_contract.py
  - tests/governance/test_governance_log_shape.py
  - tests/governance/test_governance_log_exports_as_provo.py
  - tests/governance/property/__init__.py
  - tests/governance/property/test_log_append_monotonic.py
autonomous: true
requirements: [GOV-02]
deferred: []
tags: [governance, protocol, append-only, shacl, in-memory-seam]

must_haves:
  truths:
    - "[D-04] `GovernanceLog` Protocol seam ships in `governance/log.py` — `aiosqlite`/`rdflib`/`pyoxigraph` stay OUT of this module"
    - "[D-05] In-phase append-only gate = (a) `fi:GovernanceLogShape` SHACL refuses `DELETE`/`UPDATE` on past rows (monotonic position invariant) + (b) `InMemoryGovernanceLog` Protocol contract test asserts no public mutation API exists on past rows"
    - "[D-06] Single write entry `governance_log.append(event: GovernanceEvent)` is the ONLY mutator; assigns monotonic `position`; returns persisted event"
    - "[D-05] SQLite `BEFORE UPDATE/DELETE → RAISE FAIL` trigger TRAVELS FORWARD to Phase 13 (NOT in this plan; documented in module docstring)"
    - "[D-07] On-disk layout `<corpus>/governance.ttl` + `<corpus>/.governance.sqlite` is documented as Phase 13 target; this plan ships ONLY the in-memory Protocol implementation"
  artifacts:
    - path: "src/folio_insights/governance/log.py"
      provides: "GovernanceLog Protocol + InMemoryGovernanceLog (D-04 seam; Phase 13 swap point)"
      contains: "class GovernanceLog(Protocol)"
    - path: "src/folio_insights/governance/shapes/governance_log_shape.ttl"
      provides: "fi:GovernanceLogShape append-only invariant SHACL (D-05 in-phase gate)"
      contains: "fi:GovernanceLogShape"
    - path: "tests/governance/test_governance_log_is_append_only.py"
      provides: "SHACL-level append-only refusal test (positive + negative polarity, mirroring Phase 5 forward-only test)"
      contains: "validate_governance_log_shape"
    - path: "tests/governance/test_governance_log_protocol_contract.py"
      provides: "Protocol contract test — no public mutation API on past rows (D-05 in-phase gate part b)"
      contains: "InMemoryGovernanceLog"
  key_links:
    - from: "src/folio_insights/governance/log.py"
      to: "src/folio_insights/governance/events.py"
      via: "GovernanceEvent type used in append signature"
      pattern: "from folio_insights\\.governance\\.events import GovernanceEvent"
    - from: "src/folio_insights/governance/log.py"
      to: "src/folio_insights/governance/shape_validation.py"
      via: "validate_governance_log_shape called from append()"
      pattern: "validate_governance_log_shape"
    - from: "src/folio_insights/governance/shape_validation.py"
      to: "src/folio_insights/governance/shapes/governance_log_shape.ttl"
      via: "_load_shape_graph at module-init"
      pattern: "governance_log_shape\\.ttl"
---

<objective>
Ship the `GovernanceLog` Protocol seam (D-04) + the `fi:GovernanceLogShape` SHACL guard (D-05 amended in-phase gate) + the `InMemoryGovernanceLog` Protocol contract test that proves the in-memory implementation has no public mutation API on past rows.

Purpose: GOV-02's amended exit bar requires BOTH halves of the append-only guarantee — (a) SHACL refusal of `DELETE`/`UPDATE` on past rows AND (b) the Protocol contract test. The SQLite `BEFORE UPDATE/DELETE → RAISE FAIL` trigger travels forward to Phase 13 (D-05); this plan establishes the substrate Phase 13 swaps a persistent backend behind.

Output: `governance/log.py` (Protocol + InMemoryGovernanceLog) with the genesis carve-out (D-10) hook deferred to 07-04 (signature verification + roles substrate must arrive first); `governance_log_shape.ttl` SHACL with monotonic-position + signed-at constraints; `validate_governance_log_shape()` implementation (replacing the 07-01 NotImplementedError stub); 4 test files including the Hypothesis monotonic-position property test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-governance-model-3-1/07-CONTEXT.md
@.planning/phases/07-governance-model-3-1/07-RESEARCH.md
@.planning/phases/07-governance-model-3-1/07-PATTERNS.md
@.planning/phases/07-governance-model-3-1/07-01-SUMMARY.md
@src/folio_insights/governance/events.py
@src/folio_insights/governance/__init__.py
@src/folio_insights/governance/shape_validation.py

<interfaces>
<!-- The two analog seams Phase 7 mirrors exactly. -->

From src/folio_insights/revision/store.py:1-53 (ShardStore Protocol — Phase 5 D-02; the primary analog):
```python
@runtime_checkable
class ShardStore(Protocol):
    async def get(self, iri: str) -> ShardEnvelope | None: ...
    async def put(self, shard: ShardEnvelope) -> None: ...

class InMemoryShardStore:
    """Process-local in-memory ShardStore. Reset per construction."""
    def __init__(self) -> None:
        self._by_iri: dict[str, ShardEnvelope] = {}
```

From src/folio_insights/identity/cache.py:56-91 (DidDocCache windowed-by-time query pattern):
```python
@runtime_checkable
class DidDocCache(Protocol):
    async def get(self, key: tuple[str, datetime]) -> DidDocSnapshot | None: ...
```

From src/folio_insights/revision/content_edit_shape.ttl:1-50 (Phase 5 SHACL shape — the polarity discipline is load-bearing; pyshacl 0.31.0 sh:sparql proven):
```turtle
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix fi: <https://folio-insights.example/> .

fi:ContentEditShape a sh:NodeShape ;
    sh:targetClass fi:ContentEdit ;
    sh:sparql [
        sh:message "..." ;
        sh:select '''SELECT $this WHERE { ... }''' ;
    ] .
```

From src/folio_insights/revision/shape_validation.py:99-139 (the per-event validator convention — copy this body shape):
```python
def validate_content_edit_shape(...) -> ValidationResult:
    shapes = Graph(); shapes.parse(str(_SHAPE_PATH), format="turtle")
    data_graph = ...  # build local RDF from the event
    conforms, _results_graph, results_text = pyshacl.validate(
        data_graph, shacl_graph=shapes, inference="none", abort_on_first=False,
    )
    violations = [...]
    return ValidationResult(conforms=conforms, violations=violations, results_text=results_text)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship `fi:GovernanceLogShape` SHACL TTL + `validate_governance_log_shape` body + positive/negative polarity tests + PROV-O export round-trip test</name>
  <files>src/folio_insights/governance/shapes/governance_log_shape.ttl, src/folio_insights/governance/shape_validation.py, tests/governance/test_governance_log_shape.py</files>
  <read_first>
    - src/folio_insights/revision/content_edit_shape.ttl (FULL — Phase 5 SHACL shape; copy the namespace declarations verbatim; the `sh:sparql` polarity discipline is load-bearing and was live-spiked against pyshacl 0.31.0 — lines 19-20 of the file cite this)
    - src/folio_insights/revision/shape_validation.py (FULL — copy `validate_content_edit_shape` body structure; especially lines 99-139 the pyshacl.validate + ValidationResult plumbing)
    - tests/revision/test_shacl_forward_only.py (FULL — copy the positive + negative polarity test pair structure at lines 67-87)
    - src/folio_insights/governance/shape_validation.py (from 07-01 — the stub to fill in for `validate_governance_log_shape`)
    - src/folio_insights/governance/log.py — does NOT yet exist; Task 2 ships it. This task lands the validator BEFORE the consumer to break the chicken-and-egg cycle that blocks the Hypothesis property test in Task 2 (Issue #9 fix).
    - .planning/phases/07-governance-model-3-1/07-RESEARCH.md lines 886-925 (`fi:GovernanceLogShape` skeleton TTL — adapt to local namespace)
    - .planning/phases/07-governance-model-3-1/07-PATTERNS.md lines 322-355 (`governance/shapes/*.ttl` pattern + header-comment-block template)
    - .planning/phases/07-governance-model-3-1/07-CONTEXT.md `<decisions>` D-05 (in-phase amended bar — SHACL + Protocol contract; SQLite trigger forwards to Phase 13)
  </read_first>
  <behavior>
    - Test 1 (positive polarity): a list of events with monotonic positions 0, 1, 2 and signed_at strictly increasing → `validate_governance_log_shape(events, new_event_with_pos_3)` returns `ValidationResult(conforms=True, violations=[], ...)`.
    - Test 2 (negative polarity — duplicate position): a list with two events at position 2 → conforms=False, violations mentions "monotonic and gap-free" (matches D-05 SHACL `sh:message`).
    - Test 3 (negative polarity — signed_at goes backward): position 0 signed at 2026-01-01, position 1 signed at 2025-12-31 → conforms=False, violations mentions "monotonically non-decreasing".
    - Test 4 (negative polarity — gap in positions): events at positions 0, 1, 3 (skipping 2) → conforms=False (gap = deletion).
  </behavior>
  <action>
    Create `src/folio_insights/governance/shapes/governance_log_shape.ttl` per RESEARCH lines 886-925 + PATTERNS.md lines 322-355:
    - Header comment block per PATTERNS.md lines 327-341 (cite D-05; cite analog `revision/content_edit_shape.ttl`; document the "SHACL is stateless over a single snapshot" limitation matching Phase 5's L15-17 disclosure).
    - Namespace declarations VERBATIM from PATTERNS.md line 346-349 + add `@prefix prov: <http://www.w3.org/ns/prov#>` (PROV-O).
    - `fi:GovernanceLogShape a sh:NodeShape ; sh:targetClass fi:GovernanceLog ;` (target the log container, not individual events; matches RESEARCH lines 897-898).
    - Two `sh:sparql` constraints:
      1. Monotonic + no-duplicate position: SELECT events sharing the same position (bad case); message `"Governance log positions must be monotonic and gap-free (append-only D-05)"`.
      2. Signed_at monotonically non-decreasing with position: SELECT pairs `(e1, e2)` where `p1 < p2` AND `t2 < t1`; message `"Event signed_at must be monotonically non-decreasing with position"`.
    - Also add a third constraint for gap detection: a `sh:sparql` that counts events and `MAX(position)`; if `MAX(?p) != COUNT(?e) - 1`, signal gap. Message: `"Position gap detected (deletion)"`.
    - **Live polarity spike note** — add a header comment: `# Polarity spiked against pyshacl 0.31.0 + rdflib 7.6.0 — mirrors revision/content_edit_shape.ttl bad-case-matches discipline`.

    Edit `src/folio_insights/governance/shape_validation.py` to:
    - Replace the `validate_governance_log_shape` NotImplementedError stub with a real body.
    - Helper `_build_log_graph(history: list[GovernanceEvent], pending: GovernanceEvent) -> Graph`:
      - Construct an `rdflib.Graph()` representing the log container as `fi:hasEvent ?e` triples; each event gets `fi:position`, `fi:signedAt`, `fi:did`.
      - Include the `pending` event (i.e., the new append being validated) so the SHACL sees the post-append state.
    - Body: `shapes = Graph(); shapes.parse(str(_SHAPES_DIR / "governance_log_shape.ttl"), format="turtle"); data_graph = _build_log_graph(history, pending); conforms, _, results_text = pyshacl.validate(data_graph, shacl_graph=shapes, inference="none", abort_on_first=False); violations = [...]; return ValidationResult(conforms=conforms, violations=violations, results_text=results_text)`.
    - Mirror the violations parsing from `revision/shape_validation.py:123-133`.

    Create `tests/governance/test_governance_log_shape.py` per Tests 1-4 above. Mirror `tests/revision/test_shacl_forward_only.py:67-87` polarity-pair structure. Build a list of synthetic events (use `_BaseEvent` subclasses; ExtractEvent is simplest because it has only one extra field). Use `pytestmark = pytest.mark.governance`.

  </action>
  <verify>
    <automated>uv run pytest tests/governance/test_governance_log_shape.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/folio_insights/governance/shapes/governance_log_shape.ttl` exists and contains `fi:GovernanceLogShape a sh:NodeShape` plus ≥ 2 `sh:sparql` constraints.
    - `rdflib.Graph().parse("src/folio_insights/governance/shapes/governance_log_shape.ttl", format="turtle")` succeeds without parse error (proves Turtle is valid).
    - `python -c "from folio_insights.governance.shape_validation import validate_governance_log_shape; from folio_insights.governance.events import ExtractEvent; from folio_insights.shards.envelope import AttestedSignature; from datetime import datetime, UTC; sig = AttestedSignature(action='extract', signed_at=datetime(2026,1,1,tzinfo=UTC), did='did:fi:a', signature_b64='x', over_content_hash='y'); ev = ExtractEvent(corpus='c1', position=0, signature=sig, shard_iri='fi:shard:abc'); r = validate_governance_log_shape([], ev); print('conforms=', r.conforms)"` prints `conforms= True`.
    - `uv run pytest tests/governance/test_governance_log_shape.py -x` exits 0 with ≥ 4 tests (positive + 3 negative polarity).
    - Behavior (negative polarity): `validate_governance_log_shape(history_with_duplicate_position, new_event)` returns `ValidationResult.conforms == False` and `len(violations) >= 1`.
    - The TTL is **only** consumed via `governance/shape_validation.py` (the exempt module); no direct rdflib import from any other governance/*.py file.
  </acceptance_criteria>
  <done>`fi:GovernanceLogShape` ships as a real pyshacl-validatable SHACL shape with monotonic-position + signed_at + gap-detection constraints, `validate_governance_log_shape` has a real body (replacing the 07-01 NotImplementedError stub), positive + 3 negative polarity tests prove correctness, and a PROV-O round-trip test seeds GOV-02's Turtle-export contract. Combined with Task 2's Protocol contract test (no public mutator on InMemoryGovernanceLog), D-05's amended in-phase gate is fully satisfied: SHACL refusal of `DELETE`/`UPDATE` on past rows + InMemoryGovernanceLog Protocol contract test. The SQLite `BEFORE UPDATE/DELETE → RAISE FAIL` trigger is documented as Phase 13 forward-travel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship `governance/log.py` Protocol + InMemoryGovernanceLog with no-public-mutator surface (D-04/D-06)</name>
  <files>src/folio_insights/governance/log.py, tests/governance/test_governance_log_protocol_contract.py, tests/governance/test_governance_log_is_append_only.py, tests/governance/test_governance_log_exports_as_provo.py, tests/governance/property/__init__.py, tests/governance/property/test_log_append_monotonic.py</files>
  <read_first>
    - src/folio_insights/revision/store.py (FULL — THE primary analog; copy module docstring + Protocol + InMemory class structure)
    - src/folio_insights/identity/cache.py (FULL — windowed-by-time query pattern for `query_active_roles_at`)
    - src/folio_insights/governance/events.py (from 07-01 — the 13 event classes + GovernanceEvent discriminated union)
    - .planning/phases/07-governance-model-3-1/07-PATTERNS.md lines 92-148 — `governance/log.py` pattern assignment + module docstring verbatim template
    - .planning/phases/07-governance-model-3-1/07-RESEARCH.md lines 507-560 (Pattern 2 — `GovernanceLog` Protocol seam) + lines 562-622 (Pattern 3 — append() body structure; NOTE: signature verification + roles + lockout refusal are 07-04 scope — this task stubs them out)
    - .planning/phases/07-governance-model-3-1/07-CONTEXT.md `<decisions>` D-04, D-05, D-06, D-07 (Protocol + amended exit bar + on-disk layout)
    - tests/identity/test_canonical_jcs_properties.py (Hypothesis @given pattern with max_examples=1000)
  </read_first>
  <behavior>
    - Test 1 (Protocol contract — no public mutation API): introspect `InMemoryGovernanceLog` class via `dir()` and `inspect.getmembers()`; assert no public method names match `{"update", "remove", "truncate", "drop", "delete", "pop", "clear", "set_at", "replace"}`. The ONLY public mutator allowed is `append`.
    - Test 2 (Protocol contract — append is async): `inspect.iscoroutinefunction(InMemoryGovernanceLog.append) is True`; same for `query_active_roles_at`, `get_by_position`, `iter_events`, `latest_position`.
    - Test 3 (Protocol contract — runtime_checkable): `isinstance(InMemoryGovernanceLog(), GovernanceLog) is True`.
    - Test 4 (monotonic position — Hypothesis property test, max_examples=1000): for N randomly-generated event sequences (1 ≤ N ≤ 100), after `for e in events: await log.append(e)`, `log.latest_position(corpus) == N - 1` and every appended event's `position` is `[0, 1, ..., N-1]` in order.
    - Test 5 (basic empty log): a fresh `InMemoryGovernanceLog()` returns `await log.latest_position("c1") == -1` (per RESEARCH Pattern 2 docstring — "-1 if empty (next append is position 0 — the genesis row)").
    - Test 6 (append-only end-to-end): InMemoryGovernanceLog.append called twice with monotonic events works; constructing an event list with a tampered position 0 and calling `validate_governance_log_shape(tampered_list, ...)` returns `conforms=False`. The next `append` re-runs validation and rejects. **Note:** this test ONLY proves SHACL-level detection; the Protocol contract test (Test 1) proves there's no public mutator. Both halves together = D-05 in-phase gate.
    - Test 7 (PROV-O export round-trip): append 3 ExtractEvents to InMemoryGovernanceLog; iterate via `iter_events()` and serialize to Turtle in the test body (use `rdflib.Graph()` ASSEMBLE in the TEST, NOT inside `governance/` — tests live outside the boundary); round-trip parse via `rdflib.Graph().parse(format='turtle')` finds at least 3 `prov:Activity` instances with `prov:wasAttributedTo` predicates pointing at signer DIDs. Seeds GOV-02 PROV-O contract; CLI exporter ships in 07-05b.
  </behavior>
  <action>
    Create `src/folio_insights/governance/log.py` with:
    - Module docstring per PATTERNS.md lines 99-115 verbatim (D-04/D-05/D-06 references, analog to `revision/store.py:1-18` + `identity/cache.py`, boundary statement).
    - Imports: stdlib `asyncio`, `datetime`, `typing` (Protocol, runtime_checkable, AsyncIterator); `from folio_insights.governance.events import GovernanceEvent`. **NO** `aiosqlite`, `rdflib`, `pyoxigraph` imports — D-04 boundary.
    - **Imports of validators from `shape_validation`** — Task 1 of this plan already landed the `validate_governance_log_shape` body; this task lazy-imports it inside `append` (don't import at module top if circularity arises). The real body validates the post-append snapshot per the SHACL.
    - `@runtime_checkable class GovernanceLog(Protocol):` with methods exactly per RESEARCH Pattern 2 / PATTERNS.md lines 121-132:
      - `async def append(self, event: GovernanceEvent) -> GovernanceEvent` — validates per-event SHACL shape + `fi:GovernanceLogShape`; assigns monotonic position; returns persisted event with position set. Genesis carve-out (D-10) + signature verification (D-19 belt-and-suspenders) + last-admin lockout (D-11) are STUBBED in this task with a `# TODO(07-04): wire D-10 / D-11 / verify_attestation` comment AND a `NotImplementedError` if the executor mistakenly calls a code path that should fail (specifically: any RoleAssertionEvent or RoleRevocationEvent at this point raises `NotImplementedError("Role event validation lands in 07-04")` — this prevents wiring drift between waves while keeping the Protocol shape final).
      - `async def query_active_roles_at(self, corpus: str, asof: datetime) -> dict[str, set[str]]` — returns `{subject_did → {role, ...}}` active at `asof` (assertions minus revocations, windowed by `signature.signed_at <= asof`). STUB in this task — raises `NotImplementedError("Active-roles query body lands in 07-04 (roles.py)")`.
      - `async def get_by_position(self, corpus: str, position: int) -> GovernanceEvent | None`.
      - `async def iter_events(self, corpus: str) -> AsyncIterator[GovernanceEvent]`.
      - `async def latest_position(self, corpus: str) -> int` — returns -1 if empty.
    - `class InMemoryGovernanceLog:` (NOT inheriting from Protocol — `runtime_checkable` does structural matching). `__init__(self) -> None` initializes `self._by_corpus: dict[str, list[GovernanceEvent]] = {}`. **No public method beyond the Protocol surface**; in particular, NO `update`, `remove`, `truncate`, `clear`, `set_at`, `pop`, `delete`, `replace`. Internal helpers MUST be `_`-prefixed.
    - `append()` body (minimal for this plan; full body lands in 07-04):
      1. Compute next position: `next_pos = await self.latest_position(event.corpus) + 1`.
      2. Reject role events: `if isinstance(event, (RoleAssertionEvent, RoleRevocationEvent)): raise NotImplementedError(...)` (07-04 owns these).
      3. Call `from folio_insights.governance.shape_validation import validate_governance_log_shape, ValidationResult` lazily; pass `(self._by_corpus.get(event.corpus, []), event)`; if `not result.conforms: raise ValueError(f"GovernanceLogShape violation: {result.violations}")`.
      4. Assign position via `event = event.model_copy(update={"position": next_pos})`.
      5. Append to `self._by_corpus.setdefault(event.corpus, []).append(event)`.
      6. Return event.

    Create `tests/governance/test_governance_log_protocol_contract.py` per Tests 1-3 + Test 5 above. Use `inspect.getmembers(InMemoryGovernanceLog, predicate=inspect.isfunction)` to enumerate methods; explicitly assert the forbidden names are absent. Use `pytestmark = pytest.mark.governance`.

    Create `tests/governance/property/__init__.py` (empty marker).

    Create `tests/governance/test_governance_log_is_append_only.py` per Test 6 above. Imports `InMemoryGovernanceLog` + `validate_governance_log_shape`; verifies that `append()` succeeds on monotonic events and that calling `validate_governance_log_shape(history_with_tampered_position_0_value, ...)` returns `conforms=False`. D-05 part (a) acceptance test.

    Create `tests/governance/test_governance_log_exports_as_provo.py` per Test 7 above. Append 3 ExtractEvents through `InMemoryGovernanceLog.append`; iterate via `iter_events()`; build an `rdflib.Graph()` IN THE TEST mapping each event to `<event_uri> a prov:Activity ; prov:wasAttributedTo <signer_did> ; fi:position N`; round-trip serialize → parse → assert ≥ 3 `prov:Activity` triples. Use `rdflib.Graph()` ONLY in the test (boundary unaffected; tests are outside `governance/`). Seeds GOV-02 PROV-O export contract; the CLI exporter ships in 07-05b.

    Create `tests/governance/property/test_log_append_monotonic.py` per Test 4 above. Mirror `tests/identity/test_canonical_jcs_properties.py:44-75` Hypothesis decoration: `@settings(max_examples=1000, deadline=None, suppress_health_check=[HealthCheck.too_slow])`. Use `@given(strategies.lists(strategies.from_type(SomeSimpleEvent), min_size=1, max_size=100))` or a custom strategy that constructs non-role events (e.g., ExtractEvent with random `shard_iri`) since role events raise NotImplementedError in this plan. Use `asyncio.run()` or `pytest.mark.asyncio` (Phase 5/6 precedent).
  </action>
  <verify>
    <automated>uv run pytest tests/governance/test_governance_log_protocol_contract.py tests/governance/test_governance_log_is_append_only.py tests/governance/test_governance_log_exports_as_provo.py tests/governance/property/test_log_append_monotonic.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from folio_insights.governance.log import GovernanceLog, InMemoryGovernanceLog; import asyncio; log = InMemoryGovernanceLog(); assert asyncio.run(log.latest_position('c1')) == -1; print('ok')"` prints `ok`.
    - `python -c "import inspect; from folio_insights.governance.log import InMemoryGovernanceLog; pub = [n for n in dir(InMemoryGovernanceLog) if not n.startswith('_')]; assert set(pub) <= {'append','query_active_roles_at','get_by_position','iter_events','latest_position'}, f'forbidden public method: {set(pub) - {\"append\",\"query_active_roles_at\",\"get_by_position\",\"iter_events\",\"latest_position\"}}'"` exits 0.
    - `python -c "from folio_insights.governance.log import GovernanceLog, InMemoryGovernanceLog; assert isinstance(InMemoryGovernanceLog(), GovernanceLog), 'Protocol structural match failed'"` exits 0.
    - `grep -rn "import aiosqlite\|import rdflib\|import pyoxigraph\|import oxrdflib" src/folio_insights/governance/log.py` returns empty (D-04 boundary).
    - `uv run pytest tests/governance/test_governance_log_protocol_contract.py -x` exits 0 with ≥ 5 tests.
    - `uv run pytest tests/governance/property/test_log_append_monotonic.py -x` exits 0 with the Hypothesis @given run (max_examples=1000; deadline=None).
    - `uv run pytest tests/governance/test_governance_log_is_append_only.py -x` exits 0.
    - `uv run pytest tests/governance/test_governance_log_exports_as_provo.py -x` exits 0 with 1+ tests (round-trip via rdflib parses 3 `prov:Activity` triples).
    - `uv run pytest tests/governance/test_dep_leak_guard.py -x` exits 0 (no regression from 07-01).
    - Behavior: a `RoleAssertionEvent` passed to `append` raises `NotImplementedError` mentioning "07-04" (proves the stub is in place for the next wave).
  </acceptance_criteria>
  <done>`GovernanceLog` Protocol + `InMemoryGovernanceLog` ship with: (a) the exact 5-method async surface (append, query_active_roles_at, get_by_position, iter_events, latest_position), (b) no public mutator beyond `append` (Test 1 enforces — D-05 part b), (c) monotonic position assignment proven by Hypothesis property test (max_examples=1000), (d) role events stubbed pending 07-04. The seam Phase 13 swaps a persistent backend behind is now stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (CLI / future API / future web) → `governance_log.append(event)` | Single write seam (D-06); any direct mutation of `_by_corpus` is a discipline violation. Protocol contract test enforces no public mutator beyond `append`. |
| `governance/log.py` (stdlib + Pydantic only) → `governance/shape_validation.py` (exempt; rdflib + pyshacl) | The SHACL validator is called from `append()` via lazy import. D-04 boundary preserved: log.py contains zero direct rdflib/pyoxigraph references. |
| Phase 7 `InMemoryGovernanceLog` (in-process) → Phase 13 persistent backend (`<corpus>/.governance.sqlite` with `BEFORE UPDATE/DELETE → RAISE FAIL` trigger) | The Protocol surface is the swap point. Defense-in-depth: SHACL guard ships now; SQLite trigger travels to Phase 13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03 | Tampering | `InMemoryGovernanceLog` direct dict mutation bypassing `append` | mitigate | (a) Protocol contract test asserts no public mutation API beyond `append` (test_governance_log_protocol_contract.py); (b) `fi:GovernanceLogShape` SHACL refuses duplicate positions, signed_at backwards, and position gaps (test_governance_log_shape.py with 3 negative-polarity cases); (c) Phase 13 wires SQLite `BEFORE UPDATE/DELETE → RAISE FAIL` trigger (documented in log.py docstring as forward-travel). **HIGH severity** — append-only is the audit-log foundation. |
| T-7-12 | Tampering | direct `aiosqlite`/`rdflib`/`pyoxigraph` import inside `governance/log.py` | mitigate | dep-leak guard (07-01 Task 2) parametrized over the 4 forbidden modules; `governance/log.py` lazily imports `validate_governance_log_shape` from the exempt `shape_validation.py` and contains no direct rdflib/pyoxigraph references. Acceptance criterion includes `grep -rn` confirming no import. |
| T-7-05 | Tampering | Replay of an old role-revocation event at a later position | mitigate (partial — full mitigation in 07-04) | This plan ships the monotonic-position invariant (any replay would attempt to write a position that already exists or signed_at that goes backwards; both refused by SHACL). 07-04 closes the role-side by binding `query_active_roles_at` semantics + the signature verification at `signature.signed_at`. |
</threat_model>

<verification>
- `uv run pytest tests/governance -x -q` exits 0 after this plan (cumulative: 07-01 + 07-03 tests).
- `uv run pytest tests/ -q` exits 0 (no regression in Phases 2/5/6).
- `python -c "from folio_insights.governance.log import GovernanceLog, InMemoryGovernanceLog; print('ok')"` exits 0.
- `grep -rn "import aiosqlite\|import rdflib\|import pyoxigraph" src/folio_insights/governance/log.py` returns empty.
- `ruff check src/folio_insights/governance/log.py src/folio_insights/governance/shape_validation.py` exits 0.
- `rdflib.Graph().parse("src/folio_insights/governance/shapes/governance_log_shape.ttl", format="turtle")` parses (validated in a one-off `python -c` or as part of the SHACL load).
</verification>

<success_criteria>
- D-05 amended in-phase gate satisfied: SHACL `fi:GovernanceLogShape` refuses duplicate positions / backwards signed_at / position gaps (Task 2 tests prove all 3 negative polarities) + Protocol contract test asserts no public mutator beyond `append` (Task 1 Test 1).
- D-06 satisfied: `governance_log.append(event: GovernanceEvent)` is the single write seam; monotonic position assignment proven by Hypothesis property test (max_examples=1000).
- D-04 boundary intact: `governance/log.py` has zero `aiosqlite`/`rdflib`/`pyoxigraph` imports; the only exempt file remains `governance/shape_validation.py`.
- The Protocol seam Phase 13 swaps a persistent backend behind is stable: same 5-method async surface; signature unchanged.
- Role-event handling is stubbed pending 07-04 (RoleAssertionEvent / RoleRevocationEvent appended → NotImplementedError); other event types append cleanly.
</success_criteria>

<output>
Create `.planning/phases/07-governance-model-3-1/07-03-SUMMARY.md` when done with: files created (3 src + 4 test files), test counts, the `fi:GovernanceLogShape` SHACL constraints enumerated, a confirmation that the Protocol surface matches RESEARCH Pattern 2 exactly (5 async methods), and the explicit statement that genesis carve-out (D-10) + role-event validation are forward-traveling to 07-04.
</output>
