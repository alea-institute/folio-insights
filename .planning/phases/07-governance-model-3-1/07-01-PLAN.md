---
phase: 07-governance-model-3-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/folio_insights/shards/envelope.py
  - src/folio_insights/governance/__init__.py
  - src/folio_insights/governance/events.py
  - src/folio_insights/governance/shape_validation.py
  - tests/governance/__init__.py
  - tests/governance/conftest.py
  - tests/governance/test_signed_action_literal_12_values.py
  - tests/governance/test_role_revocation_distinct_event.py
  - tests/governance/test_dep_leak_guard.py
autonomous: true
requirements: [GOV-01, GOV-02]
deferred: []
tags: [governance, pydantic, discriminated-union, dep-leak-guard]

must_haves:
  truths:
    - "[D-13] `SignedAction` Literal in shards/envelope.py has exactly 12 values including `role_revocation`"
    - "[D-06] `GovernanceEvent` Pydantic discriminated union dispatches across 12 event classes on `model_validate` by `action` discriminator"
    - "[D-04] No module under src/folio_insights/governance/ imports aiosqlite, rdflib, pyoxigraph, or oxrdflib (governance/shape_validation.py is the lone exempted module)"
    - "[D-13] `RoleRevocationEvent` is a structurally distinct Pydantic class from `RoleAssertionEvent` (NOT a flag on RoleAssertion)"
  artifacts:
    - path: "src/folio_insights/governance/events.py"
      provides: "12 GovernanceEvent classes + Annotated[Union[...], Field(discriminator='action')]"
      contains: "GovernanceEvent ="
    - path: "src/folio_insights/governance/__init__.py"
      provides: "Barrel re-export of GovernanceEvent + every event class"
      contains: "from folio_insights.governance.events import"
    - path: "src/folio_insights/governance/shape_validation.py"
      provides: "ValidationResult dataclass + per-event validate_*_shape skeleton (filled in by 07-03/07-04/07-05)"
      contains: "class ValidationResult"
    - path: "tests/governance/test_dep_leak_guard.py"
      provides: "Per-file source-scan refusing aiosqlite/rdflib/pyoxigraph/oxrdflib imports under governance/"
      contains: "FORBIDDEN"
  key_links:
    - from: "src/folio_insights/governance/events.py"
      to: "src/folio_insights/shards/envelope.py"
      via: "AttestedSignature import + SignedAction Literal coverage"
      pattern: "from folio_insights\\.shards\\.envelope import AttestedSignature"
    - from: "src/folio_insights/shards/envelope.py:85"
      to: "envelope.py:154 (AttestedSignature.action default)"
      via: "12-value SignedAction Literal — `role_revocation` added at end"
      pattern: "\"role_revocation\""
---

<objective>
Establish the substrate foundation for Phase 7 governance: the 12-value `SignedAction` Literal extension (envelope.py), the 12-class `GovernanceEvent` discriminated union (D-06), the `governance/` package boundary discipline (D-04 dep-leak guard), and the per-event SHACL validator skeleton that 07-03/07-04/07-05 will populate.

Purpose: Every later Phase 7 plan depends on (a) `RoleRevocationEvent` existing as a distinct class (D-13 closes F6 by making revocation structurally distinct), (b) the discriminated union being the single entry-point for `governance_log.append(event)` (D-06), and (c) the `governance/` package being storage-library-free (D-04 — Phase 13 wires `aiosqlite` behind the Protocol).

Output: `src/folio_insights/governance/{events,shape_validation,__init__}.py`, `tests/governance/{conftest,test_signed_action_literal_12_values,test_role_revocation_distinct_event,test_dep_leak_guard}.py`, and a 1-line edit to `shards/envelope.py:85` adding `"role_revocation"` to the `SignedAction` Literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-governance-model-3-1/07-CONTEXT.md
@.planning/phases/07-governance-model-3-1/07-RESEARCH.md
@.planning/phases/07-governance-model-3-1/07-PATTERNS.md

<interfaces>
<!-- Phase 6 primitives this plan consumes verbatim. Executor must NOT reshape AttestedSignature. -->

From src/folio_insights/shards/envelope.py:85-103 (current 11-value Literal — extend to 12):
```python
SignedAction = Literal[
    "extract", "promote", "demote", "contest", "resolve_contest",
    "distinguo", "supersede", "retract", "role_assertion",
    "content_edit", "reparent", "reconcile",
]
# Add: "role_revocation"  (D-13 — extends 11 → 12 values)
```

From src/folio_insights/shards/envelope.py:106-162 (AttestedSignature — UNCHANGED, every event embeds one):
```python
class AttestedSignature(BaseModel):
    model_config = ConfigDict(extra="forbid")
    action: SignedAction = "content_edit"
    signing_key_id: str | None = None
    did_doc_snapshot_at: datetime | None = None
    signed_at: datetime
    did: str
    signature_b64: str
    over_content_hash: str
    cosigners: list["AttestedSignature"] = Field(default_factory=list)
    # ...
```

From src/folio_insights/revision/shape_validation.py:99 (the per-event validator convention — mirror EXACTLY, NOT services/shacl_validator.py):
```python
@dataclass
class ValidationResult:
    conforms: bool
    violations: list[str]
    results_text: str

def validate_content_edit_shape(...) -> ValidationResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `SignedAction` Literal to 12 values + create `governance/events.py` discriminated union</name>
  <files>src/folio_insights/shards/envelope.py, src/folio_insights/governance/__init__.py, src/folio_insights/governance/events.py, tests/governance/__init__.py, tests/governance/conftest.py, tests/governance/test_signed_action_literal_12_values.py, tests/governance/test_role_revocation_distinct_event.py</files>
  <read_first>
    - src/folio_insights/shards/envelope.py (FULL — current SignedAction Literal at L85-103; AttestedSignature at L106-162; the model_rebuild() pattern at L344-350)
    - src/folio_insights/shards/subtypes.py (the discriminated-union analog over `shard_type`; the canonical Annotated[Union[...], Field(discriminator=...)] precedent)
    - .planning/phases/07-governance-model-3-1/07-PATTERNS.md (lines 151-184 — `events.py` pattern assignment + discriminated-union template)
    - .planning/phases/07-governance-model-3-1/07-RESEARCH.md (lines 418-505 — Pattern 1: GovernanceEvent Discriminated Union code block)
    - .planning/phases/07-governance-model-3-1/07-CONTEXT.md `<decisions>` D-06, D-13 (12-event union; role_revocation as distinct event class)
    - tests/shards/test_discriminated_union.py (analog test pattern for Pydantic discriminator dispatch)
  </read_first>
  <behavior>
    - Test 1: `len(get_args(SignedAction)) == 12` and the literal contains `"role_revocation"` as the final value (per D-13 — extends from 11 to 12).
    - Test 2: `RoleRevocationEvent` is a distinct Pydantic class from `RoleAssertionEvent` (different `__mro__` beyond `_BaseEvent`/`BaseModel`; different field set: `revoked_role` vs `role`).
    - Test 3: `GovernanceEvent` discriminated union: `TypeAdapter(GovernanceEvent).validate_python({"action": "role_revocation", "corpus": "c1", "signature": <attested>, "subject_did": "did:fi:bob", "revoked_role": "reviewer"})` returns a `RoleRevocationEvent` instance (NOT `RoleAssertionEvent`).
    - Test 4: Round-trip — every event class supports `model_dump_json` + `TypeAdapter(GovernanceEvent).validate_json(...)` round-trip with `extra="forbid"` violated when an unknown field is added.
  </behavior>
  <action>
    Edit `src/folio_insights/shards/envelope.py:85-103` to add `"role_revocation"` as the 12th `SignedAction` Literal value (after `"reconcile"`). Do NOT touch `AttestedSignature` (D-13 Phase 6 lock); do NOT alter `model_rebuild()` semantics at L344-350.

    Create `src/folio_insights/governance/__init__.py` as a barrel re-export of `GovernanceEvent` + every event class + `ValidationResult` (the shape_validation.py stub from Task 2). Mirror `src/folio_insights/identity/__init__.py` re-export discipline. Module docstring MUST include: `D-04 boundary: stdlib + Pydantic ONLY — NO aiosqlite/rdflib/pyoxigraph imports under governance/ (Phase 13 wires the persistent backend behind the GovernanceLog Protocol). The lone exempt module is governance/shape_validation.py (mirrors revision/shape_validation.py).`

    Create `src/folio_insights/governance/events.py` (per RESEARCH Pattern 1 + PATTERNS.md lines 159-184) with `from __future__ import annotations`. Define `class _BaseEvent(BaseModel)` with `model_config = ConfigDict(extra="forbid")` and three universal slots: `corpus: str`, `position: int = -1` (assigned by GovernanceLog.append), `signature: AttestedSignature` (the only shared primitive per D-16). Then define the 12 event classes — each pins its `action: Literal["<value>"] = "<value>"` discriminator and inherits ONLY from `_BaseEvent`:

    1. `RoleAssertionEvent` — fields: `subject_did: str`, `role: Literal["extractor","reviewer","arbiter","corpus_admin"]`
    2. `RoleRevocationEvent` — fields: `subject_did: str`, `revoked_role: Literal["extractor","reviewer","arbiter","corpus_admin"]`
    3. `PromotionEvent` — fields: `shard_iri: str`, `new_status: Literal["per_se_nota_quoad_nos","demonstrable","authority_only"]`, `cited_iris: list[str] = Field(min_length=1)` (D-20 non-empty)
    4. `DemotionEvent` — fields: `shard_iri: str`, `new_status: Literal["hypothesis"]` (demote-to-hypothesis is the only valid target per PRD §3.1.2)
    5. `ContestEvent` — fields: `shard_iri: str`, `voter_did: str`, `position_text: str` (renamed from `position` because `_BaseEvent.position` already names the log row; avoid shadowing)
    6. `ContestResolutionEvent` — fields: `shard_iri: str`, `resolution_path: Literal["arbiter","distinguo","aporetic"]` (GOV-05 — no majority-vote)
    7. `SupersessionEvent` — fields: `old_shard_iri: str`, `new_shard_iri: str`
    8. `RetractionEvent` — fields: `shard_iri: str`, `cascade_preview_hash: str` (commits the preview confirmed by operator per D-17)
    9. `ExtractEvent` — fields: `shard_iri: str`, `extractor_model: str | None = None`
    10. `DistinguoEvent` — fields: `shard_iri: str`, `prime_analogate_iri: str`, `distinction_kind: str`
    11. `ContentEditEvent` — fields: `shard_iri: str`, `field_path: str`, `rationale: str`
    12. `ReparentEvent` — fields: `shard_iri: str`, `new_parent_iri: str`
    13. `ReconcileEvent` — fields: `shard_iri: str`, `strategy: str`

    (Note: 13 listed because `RoleAssertionEvent` + `RoleRevocationEvent` are both present — corresponds to the 12 `SignedAction` Literal values; the 12 are the action vocabulary, and we ship one event class per action.)

    Define `GovernanceEvent = Annotated[Union[<all 12 classes>], Field(discriminator="action")]` at module bottom. Mirror `shards/subtypes.py` discriminated union pattern.

    Create `tests/governance/__init__.py` (empty marker) and `tests/governance/conftest.py` with shared fixtures (per VALIDATION.md Wave 0):
    - `genesis_did: str` (a fixed did:key for tests)
    - `attested_signature_factory()` callable returning a valid `AttestedSignature` (for embedding in test events; reuse Phase 6 signer fixtures if available — see `tests/identity/conftest.py`)
    - `sample_role_assertion(role="reviewer", subject="did:fi:bob")` factory

    Create `tests/governance/test_signed_action_literal_12_values.py`: assert `len(get_args(SignedAction)) == 12`; assert `"role_revocation" in get_args(SignedAction)`; assert `"role_revocation"` is the last entry (positional D-13 stability).

    Create `tests/governance/test_role_revocation_distinct_event.py`: build a RoleRevocationEvent + a RoleAssertionEvent; assert `type(rev_event) is not type(assert_event)`; assert `rev_event.revoked_role` exists and `assert_event.role` exists (different field names); discriminated-union dispatch test (Test 3 above) using `pydantic.TypeAdapter`.

    Use `pytest.mark.governance` marker; register marker in `pyproject.toml` `[tool.pytest.ini_options.markers]` if missing (mirror Phase 5/6 marker registration).
  </action>
  <verify>
    <automated>uv run pytest tests/governance/test_signed_action_literal_12_values.py tests/governance/test_role_revocation_distinct_event.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"role_revocation"' src/folio_insights/shards/envelope.py` returns ≥ 1 (the Literal extension).
    - `python -c "from folio_insights.shards.envelope import SignedAction; from typing import get_args; assert len(get_args(SignedAction)) == 12; assert 'role_revocation' in get_args(SignedAction)"` exits 0.
    - `python -c "from folio_insights.governance.events import GovernanceEvent, RoleRevocationEvent, RoleAssertionEvent, PromotionEvent, ContestEvent, ContestResolutionEvent, SupersessionEvent, RetractionEvent; print('ok')"` prints `ok`.
    - `python -c "from pydantic import TypeAdapter; from folio_insights.governance.events import GovernanceEvent, RoleRevocationEvent; ta = TypeAdapter(GovernanceEvent)"` exits 0 (discriminated union compiles).
    - `uv run pytest tests/governance/test_signed_action_literal_12_values.py tests/governance/test_role_revocation_distinct_event.py -x` exits 0.
    - `grep -rn "import aiosqlite\|import rdflib\|import pyoxigraph\|import oxrdflib\|from aiosqlite\|from rdflib\|from pyoxigraph\|from oxrdflib" src/folio_insights/governance/events.py src/folio_insights/governance/__init__.py | grep -v "^#"` returns nothing (no forbidden imports).
  </acceptance_criteria>
  <done>The 12-value `SignedAction` Literal ships at `envelope.py:85-103`, the 12-class `GovernanceEvent` discriminated union ships at `governance/events.py` with `Annotated[Union[...], Field(discriminator="action")]`, `governance/__init__.py` barrel re-exports it, and the two test files green prove the discriminator dispatches `role_revocation` → `RoleRevocationEvent` distinct from `role_assertion` → `RoleAssertionEvent` (D-13 closure structurally enforced).</done>
</task>

<task type="auto">
  <name>Task 2: Ship `governance/shape_validation.py` skeleton + `tests/governance/test_dep_leak_guard.py` (D-04 boundary)</name>
  <files>src/folio_insights/governance/shape_validation.py, tests/governance/test_dep_leak_guard.py</files>
  <read_first>
    - src/folio_insights/revision/shape_validation.py (FULL — THE per-event validate_*_shape() convention; copy module structure verbatim and adapt; lines 1-32 module docstring + lines 99-139 validator body)
    - tests/shards/test_dep_leak_guard.py (FULL — THE Phase 13 dep-leak guard template; lines 24-39 are the canonical source-scan pattern)
    - src/folio_insights/governance/events.py (from Task 1 — verifies events are stdlib + Pydantic only, providing the event classes the per-shape validators will consume)
    - .planning/phases/07-governance-model-3-1/07-PATTERNS.md lines 358-399 — `governance/shape_validation.py` pattern assignment (CRITICAL — mirror `revision/shape_validation.py`, NOT `services/shacl_validator.py`)
    - .planning/phases/07-governance-model-3-1/07-RESEARCH.md lines 1097-1131 — dep-leak guard test code skeleton
    - .planning/phases/07-governance-model-3-1/07-CONTEXT.md `<decisions>` D-04, D-05 (boundary + amended exit bar)
  </read_first>
  <action>
    Create `src/folio_insights/governance/shape_validation.py` mirroring `src/folio_insights/revision/shape_validation.py:1-139` structure:
    - Module docstring stating the exemption: "This module is the SHACL validator wrapper; rdflib/pyshacl imports are SAFE here under the same exemption `revision/shape_validation.py` enjoys. The dep stays out of `governance/log.py`, `events.py`, `authorize.py`, etc."
    - Reference `T-7-12 dep-leak grep-guard exemption` in the docstring so the test in this task knows to skip it (the dep-leak guard test below MUST exempt this single filename).
    - Import `rdflib.Graph` + `pyshacl.validate` (these are the ONLY two files allowed to import them inside `governance/`).
    - Define `@dataclass class ValidationResult` with `conforms: bool`, `violations: list[str]`, `results_text: str` — verbatim signature copied from `revision/shape_validation.py`.
    - Define module-level `_SHAPES_DIR = Path(__file__).parent / "shapes"` and helper `_load_shape_graph(filename: str) -> Graph` that parses TTL from `_SHAPES_DIR`. The 8 actual TTL files ship in later plans (07-03 / 07-04 / 07-05); this task ships the directory placeholder (`mkdir governance/shapes/.gitkeep` is fine) + loader.
    - Define helper `_build_event_graph(event: GovernanceEvent) -> Graph` that materializes an event as a minimal local RDF graph for pyshacl to validate (mirror `revision/shape_validation.py:111-138`). Use the `fi:` namespace `https://folio-insights.example/`.
    - Stub the per-event validators as `def validate_<event>_shape(event) -> ValidationResult: raise NotImplementedError("filled by 07-03/07-04/07-05")` for each of the 8 shapes (governance_log, role_assertion, role_revocation, promotion, contest, contest_resolution, supersession, retraction). Real bodies land in 07-03 (log + role + promotion), 07-04 (three-way + contest_resolution).

    Create `tests/governance/test_dep_leak_guard.py` (mirror `tests/shards/test_dep_leak_guard.py:24-39` verbatim with substitution):
    - `FORBIDDEN = ["aiosqlite", "rdflib", "pyoxigraph", "oxrdflib"]`
    - Walk `pathlib.Path(governance.__file__).parent.rglob("*.py")`
    - For each file, refuse `f"import {mod}"` AND `f"from {mod}"` substrings
    - **Exemption:** explicitly skip the file `shape_validation.py` (its filename literally, by basename check `if module_file.name == "shape_validation.py": continue`). This mirrors the precedent that `revision/shape_validation.py` is exempt from the `shards/` boundary (PATTERNS.md line 705).
    - Parametrize on FORBIDDEN modules; assertion message references "D-04 forbids" and points at the Phase 13 GovernanceLog backend swap.
    - Use `pytestmark = pytest.mark.governance`.
  </action>
  <verify>
    <automated>uv run pytest tests/governance/test_dep_leak_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/folio_insights/governance/shape_validation.py` exists and contains `@dataclass\nclass ValidationResult:` and 8 `validate_*_shape` stubs (matching the 8 shape names enumerated in D-04 docs).
    - `src/folio_insights/governance/shapes/` directory exists (with `.gitkeep` or first TTL file).
    - `grep -rn "import aiosqlite\|import rdflib\|import pyoxigraph\|import oxrdflib\|from aiosqlite\|from rdflib\|from pyoxigraph\|from oxrdflib" src/folio_insights/governance/ | grep -v "shape_validation.py" | grep -v "^#"` returns empty (no forbidden imports outside the exempted module).
    - `uv run pytest tests/governance/test_dep_leak_guard.py -x` exits 0 — the test passes against the current `governance/` tree (only `events.py`, `__init__.py`, `shape_validation.py`).
    - `uv run pytest tests/governance/test_dep_leak_guard.py -x` exits 1 if `import aiosqlite` is temporarily added to `governance/events.py` (negative-polarity smoke check — executor can run this once locally then revert; not committed).
    - Behavior assertion: `from folio_insights.governance.shape_validation import ValidationResult, validate_governance_log_shape; ValidationResult(conforms=True, violations=[], results_text="")` instantiates without error; `validate_governance_log_shape(<any>)` raises `NotImplementedError`.
  </acceptance_criteria>
  <done>The `governance/shape_validation.py` module ships with the `ValidationResult` dataclass and 8 `NotImplementedError`-stubbed validators (filled by later plans), `governance/shapes/` directory exists, and `test_dep_leak_guard.py` enforces D-04 by refusing any aiosqlite/rdflib/pyoxigraph/oxrdflib import under `governance/` EXCEPT the single exempted `shape_validation.py` file. Phase 13's backend swap can now slot in behind the Protocol without touching the boundary discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `shards/envelope.py` (canonical vocabulary) → `governance/events.py` (12-class union consumer) | The `SignedAction` Literal is the discriminator vocabulary; any drift between the 12 Literal values and the 13 event class `Literal[action]` pins corrupts dispatch. Mitigation: test_signed_action_literal_12_values.py asserts the 12 values; the discriminated-union test asserts dispatch correctness. |
| `governance/` package (Phase 7 stdlib + Pydantic only) → Phase 13 persistent backend (aiosqlite / rdflib / pyoxigraph) | D-04 keeps storage libs out of `governance/`; violation today silently leaks Phase 13 implementation detail into Phase 7 surface and breaks the seam discipline. Mitigation: test_dep_leak_guard.py parametrized over the 4 forbidden modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-12 | Tampering | `src/folio_insights/governance/*.py` direct aiosqlite/rdflib/pyoxigraph import | mitigate | `tests/governance/test_dep_leak_guard.py` source-scan refuses forbidden imports under `governance/` (single exemption: `shape_validation.py`). Mirrors `tests/shards/test_dep_leak_guard.py` (Phase 2 precedent). Block-on: high — silently breaks Phase 13 seam if it lands. |
| T-7-SC | Tampering | npm/pip/cargo installs in Phase 7 | accept | No new packages this plan (or any Phase 7 plan per RESEARCH §"Package Legitimacy Audit"). Every library is pre-existing from Phases 2/5/6. Disposition: accept — zero install surface. |
</threat_model>

<verification>
- `uv run pytest tests/governance -x -q` exits 0 after this plan.
- `uv run pytest tests/ -q` exits 0 (no regression in upstream Phases 2/5/6 tests; `AttestedSignature` discipline preserved).
- `grep -c "role_revocation" src/folio_insights/shards/envelope.py` ≥ 1.
- `python -c "from folio_insights.governance import GovernanceEvent, RoleRevocationEvent, ValidationResult"` exits 0 (barrel re-export wiring works).
- `ruff check src/folio_insights/governance/ src/folio_insights/shards/envelope.py` exits 0.
</verification>

<success_criteria>
- `SignedAction` Literal has exactly 12 values; `"role_revocation"` is present (D-13 closure structural foundation).
- `GovernanceEvent` discriminated union dispatches across 12 distinct event classes (Pydantic TypeAdapter validates correctly).
- `RoleRevocationEvent` is structurally distinct from `RoleAssertionEvent` (different class, different fields) — F6 closure foundation.
- `governance/` is storage-library-free EXCEPT for the single exempt `shape_validation.py` file.
- All tests in this plan green; no regression in upstream test suite.
- The substrate (events + boundary) is ready for 07-03 (Roles + Authorize + Promote) and 07-04 (Three-Way Disambiguation) to land their per-event SHACL bodies into the `shape_validation.py` stubs.
</success_criteria>

<output>
Create `.planning/phases/07-governance-model-3-1/07-01-SUMMARY.md` when done with: files changed, test counts (added vs total under tests/governance), the `SignedAction` Literal length confirmation, and the 13 event class names enumerated.
</output>
