# Resume Point — Phase 01 polysemy-distinguo-spike

**Paused:** 2026-04-23T22:43:18Z
**Trigger:** `/gsd-pause-work` (mid-`/gsd-discuss-phase 1`)
**Status:** DISCUSSION IN PROGRESS — gray areas identified, not yet asked

---

## Why we paused

Context usage hit 80% (33% remaining) during research-gathering. Pausing before the Q&A to keep the next session clean.

## What happened this session

1. **Resolved phase mismatch.** `gsd-sdk query init.phase-op 1` returned the archived v1.1 `01-deploy-on-railway-as-dev-server` (under `.planning/milestones/v1.1-phases/`). User confirmed v2.0 Phase 1 is the Polysemy / distinguo Spike.
2. **Created the correct directory:** `.planning/phases/01-polysemy-distinguo-spike/` (currently holds only this handoff file).
3. **Gathered prior context:**
   - ROADMAP.md §Phase 1 — goal, dependencies, REQ-IDs, exit criteria (below)
   - PROJECT.md — v2.0 shards-as-axioms scope
   - REQUIREMENTS.md — PRINCIPLE-06 + VOCAB-02
   - PHILOSOPHY.md — scholastic distinguo machinery, analogia entis, known-homonyms problem
   - PRD-v2.0-draft-2.md §8.P6 + §16 R2 — detector spec, 0.6 threshold, mandatory human gate, *consideration* acceptance test
   - research/PITFALLS.md #8 — known-terms-of-art whitelist, N≥3 shards/framework, fork-acceptance-rate dogfooding
4. **Scouted codebase:** v1 assets that Phase 1 will lean on → `src/folio_insights/services/task_clustering.py`, `services/boundary/semantic.py` (sentence-transformers), `pipeline/stages/folio_tagger.py` (4-path FOLIO tagger).
5. **Identified 4 gray areas** (see below) — STOPPED before asking them to preserve context.

## Phase 1 fundamentals (for next session)

**Goal:** Validate §16 Risk 2 (polysemy detector FP rate, human-gate design) via canonical legal *consideration* fixture before committing Phase 9.P6 architecture.

**Depends on:** Phase 0 ✓ (CLOSED 2026-04-23, verdict keep=pyoxigraph)
**Parallel-track:** yes — runs alongside Phase 2 (Shard Envelope)
**REQ-IDs:** PRINCIPLE-06 (scoping only; full impl in 9.P6), VOCAB-02
**Research flag:** yes — no ecosystem precedent for polysemy FP curation + LLM-vs-rule hybrid

**Exit criteria:**
1. *Consideration* fixture set (≥20 shards across 3+ frameworks) classified by prototype detector with ≤10% FP rate
2. Human-gate interaction pattern (accept / reject / modify) documented in spike SUMMARY.md — **no auto-apply path** (§16 R2)
3. Per-framework threshold strategy proposed and open-sourced for 9.P6 planning

## Gray areas — ready to present in next session

1. **Fixture sourcing** — Where do the ≥20 *consideration* shards come from? (v1 advocacy re-extract + FRE + Restatement? Hand-curated seed? LLM-synthesized adversarials too?)
2. **Detector architecture** — Pure-rule (framework tags + axiom-conflict check) / pure-LLM (instructor classification) / hybrid with fallback ordering?
3. **Human-gate UX shape** — CLI-only for the spike (accept/reject/modify), or stub Svelte surface wired to Phase 14 design tokens?
4. **FP "gold set" labeling** — Self-labeled (user), advisory LLM with manual spot-check, or deferred to Phase 15 practitioner sessions?

### User's preferred resumption strategy

Three options were offered at pause time:
- **A.** Pause now, resume fresh with `/gsd-discuss-phase 1` ← **chosen**
- **B.** Batch-mode continue (all 4 in one turn, write CONTEXT.md in one shot)
- **C.** Answer only #1 and #2 (load-bearing decisions), defer #3/#4

## Canonical refs to read at resume

- `ROADMAP.md` §Phase 1 (lines 103-116)
- `PRD-v2.0-draft-2.md` §8.P6 (polysemy detector spec, lines 1090-1114) and §16 Risk 2 (mitigation, lines 1465-1467)
- `PHILOSOPHY.md` lines 108-141, 395, 1247-1252 (scholastic distinguo + homonymy problem)
- `.planning/research/PITFALLS.md` lines 643-656 (Pitfall 8: false-fork explosion + mitigations)
- `.planning/REQUIREMENTS.md` PRINCIPLE-06 (line 104), VOCAB-02 (line 90)
- `.planning/v2.0-MILESTONE-BRIEF.md` line 45 (phase anchor)

## Codebase assets to reuse

- `src/folio_insights/services/task_clustering.py` — sentence-transformers clustering (embed centroids, cluster validation pattern)
- `src/folio_insights/services/boundary/semantic.py` — all-MiniLM-L6-v2 cosine similarity at threshold 0.3
- `src/folio_insights/pipeline/stages/folio_tagger.py` — 4-path FOLIO tagger w/ confidence
- `src/folio_insights/pipeline/discovery/orchestrator.py` — pattern for Phase 9.P6 detector integration

## Git state

- Branch: `master`
- HEAD: `3926637 wip: phase-00 paused at 8/8 (complete)`
- Ahead of origin: **43 commits**
- Working-tree at pause: `M .planning/STATE.md` (session-continuity edits from /gsd-resume-work)

## How to resume

```
/gsd-discuss-phase 1
```

The workflow will detect this `.continue-here.md`, but because the directory already exists AND no CONTEXT.md is present, the flow can proceed directly to `present_gray_areas` using the 4 listed above.

If `gsd-sdk init.phase-op 1` still returns the archived v1.1 path, the fix is to pass `--dir .planning/phases/01-polysemy-distinguo-spike/` or to write CONTEXT.md directly at `.planning/phases/01-polysemy-distinguo-spike/01-CONTEXT.md` and skip the init phase resolution.
