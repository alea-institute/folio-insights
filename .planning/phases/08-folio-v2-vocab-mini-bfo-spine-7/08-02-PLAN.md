---
phase: 08-folio-v2-vocab-mini-bfo-spine-7
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/folio_insights/shards/envelope.py
  - src/folio_insights/bench/generator.py
  - tests/vocab/test_envelope_vocab_pin.py
  - tests/vocab/test_bench_emits_vocab_version.py
  - tests/bench/fixtures/expected_digest.txt
autonomous: true
requirements: [VOCAB-01]

must_haves:
  truths:
    - "ShardEnvelope gains a required field `vocab_version: str` defaulting via `default_factory=lambda: VOCAB_VERSION` [D-03]"
    - "A Pydantic `field_validator(\"vocab_version\")` on ShardEnvelope refuses any value not equal to the module constant `VOCAB_VERSION` [D-03, D-04 Pydantic belt]"
    - "The bench generator emits one extra `Quad(shard_iri, fi:vocabVersion, Literal(VOCAB_VERSION), graph_iri)` per shard [D-04 source-of-truth coverage]"
    - "The N-Quads digest baseline is regenerated and pinned in `tests/bench/fixtures/expected_digest.txt`, and a regression test asserts the new digest is byte-stable across re-runs [Phase 0 D-15 contract]"
    - "First `field_validator` in the codebase is documented as a greenfield idiom; existing `@model_validator(mode='after')` precedent in `shards/envelope.py:313` and `shards/subtypes.py:105` noted in the docstring [D-03 verbatim]"
    - "fi:vocabVersion is the two-belt counterpart to `fi:VocabPinShape` from Plan 08-01 — Pydantic catches at construction, SHACL catches at storage/export [D-04]"
  artifacts:
    - path: src/folio_insights/shards/envelope.py
      provides: "vocab_version field + field_validator + import from folio_insights.vocab"
      contains: "vocab_version"
    - path: src/folio_insights/bench/generator.py
      provides: "per-shard fi:vocabVersion emission + import of VOCAB_VERSION"
      contains: "vocabVersion"
    - path: tests/vocab/test_envelope_vocab_pin.py
      provides: "construction-time pin assertion (3 tests: default, explicit-match, mismatch raises)"
    - path: tests/vocab/test_bench_emits_vocab_version.py
      provides: "per-shard fi:vocabVersion quad emission assertion"
    - path: tests/bench/fixtures/expected_digest.txt
      provides: "byte-stable N-Quads digest baseline (post-Phase-8)"
  key_links:
    - from: src/folio_insights/shards/envelope.py
      to: folio_insights.vocab.VOCAB_VERSION
      via: from folio_insights.vocab import VOCAB_VERSION
      pattern: "from folio_insights.vocab import"
    - from: src/folio_insights/bench/generator.py
      to: folio_insights.vocab.VOCAB_VERSION
      via: import + Literal(VOCAB_VERSION) in _emit_shard_quads
      pattern: "VOCAB_VERSION"
---

<objective>
Land the Pydantic belt half of the D-04 two-belt vocab-pin enforcement: `ShardEnvelope.vocab_version` field + `field_validator` that refuses mismatched values + bench generator emits `fi:vocabVersion` quad on every generated shard, with a regenerated digest baseline so the Phase 0 D-15 byte-identical N-Quads contract stays green.

Purpose: Without this plan, only SHACL catches a smuggled `fi:vocabVersion`. With it, the model rejects at construction (cheaper, earlier, fails-fast) and every generated benchmark shard carries the pin.

Output: 2 source edits + 2 test files + 1 regenerated digest baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md

<interfaces>
<!-- Existing ShardEnvelope field-with-default pattern (lines 301-303 of envelope.py) -->

From src/folio_insights/shards/envelope.py:301-303:
```python
transaction_time: datetime = Field(
    default_factory=lambda: datetime.now(UTC)
)
```

<!-- Existing model_validator pattern (the codebase's "refuse-mismatched-value" precedent) -->

From src/folio_insights/shards/envelope.py:313-346 and src/folio_insights/shards/subtypes.py:105-127:
```python
@model_validator(mode="after")
def _check_invariants(self) -> "ShardEnvelope":
    if condition_violated:
        raise ValueError("...message citing D-XX...")
    return self
```

<!-- Bench generator constants block + per-shard emission point -->

From src/folio_insights/bench/generator.py:49-57:
```python
FI = "https://folio-insights.aleainstitute.ai/vocab/"
CORPUS = "https://folio-insights.aleainstitute.ai/corpus/"
SHARD = "https://folio-insights.aleainstitute.ai/shard/"
CONCEPT = "https://folio-insights.aleainstitute.ai/concept/"
FRAMEWORK = "https://folio-insights.aleainstitute.ai/framework/"
```

From src/folio_insights/bench/generator.py (per-shard quad emission idiom around lines 218-226):
```python
out.append(
    Quad(
        shard_iri,
        NamedNode(f"{FI}framework"),
        NamedNode(f"{FRAMEWORK}{fw}"),
        graph_iri,
    )
)
```

<!-- VOCAB_VERSION constant (lands in Plan 08-01, available before this plan runs since Wave 1 means both Plans run before Wave 2) -->

NOTE: Plans 08-01 and 08-02 are sibling Wave-1 plans. To avoid a Wave-1 ordering dependency, this plan also defines `VOCAB_VERSION = "2026.05.0"` inline if Plan 08-01 has not landed yet — but the executor MUST update `from folio_insights.vocab import VOCAB_VERSION` once Plan 08-01 commits. The acceptance criteria assert the import path matches the canonical one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic vocab_version field + field_validator + envelope tests</name>
  <files>src/folio_insights/shards/envelope.py, tests/vocab/test_envelope_vocab_pin.py</files>
  <read_first>
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md (D-03, D-04, `### Reusable Assets` shard envelope row, `### Established Patterns` two-belt enforcement)
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md (`### MOD src/folio_insights/shards/envelope.py` section, especially the field_validator vs model_validator discretion handle)
    - src/folio_insights/shards/envelope.py (entire file — current 15-field envelope structure; locate the ShardEnvelope class body around line 186 and the existing `transaction_time` field-with-default at line 301)
    - src/folio_insights/shards/subtypes.py (model_validator precedent around lines 105-127)
    - tests/shards/test_envelope_roundtrip.py (existing round-trip pattern Plan 08-02 must not break)
    - tests/shards/conftest.py (`_SUBTYPE_DEFAULTS` fixture pattern — must continue to round-trip)
  </read_first>
  <behavior>
    - `from folio_insights.shards.envelope import ShardEnvelope; e = ShardEnvelope(<minimum required fields>); assert e.vocab_version == "2026.05.0"` — default factory pins the constant.
    - Passing `vocab_version="2026.05.0"` explicitly to the constructor succeeds and the field round-trips through `model_dump()`.
    - Passing `vocab_version="2026.04.0"` raises `pydantic.ValidationError` whose error message contains both `vocab_version` and `Phase 8` (or `VOCAB_VERSION`) and the offending value `2026.04.0`.
    - Passing `vocab_version=""` raises `ValidationError`.
    - Passing `vocab_version="2026.05.0"` (string) but with the surrounding fields valid: `ShardEnvelope(**e.model_dump()).vocab_version == "2026.05.0"` (round-trip stable).
    - Every existing `tests/shards/test_envelope_roundtrip.py` test continues to pass — the new default factory means existing fixtures auto-inherit the pin.
    - Every existing `tests/shards/test_subtype_*.py` test continues to pass.
    - The new `tests/vocab/test_envelope_vocab_pin.py` ships ≥4 tests covering the four behaviors above.
  </behavior>
  <action>
    Edit `src/folio_insights/shards/envelope.py`. At the top of the file (after the existing imports), add `from folio_insights.vocab import VOCAB_VERSION` and `from pydantic import field_validator` (add `field_validator` to the existing pydantic import line; PATTERNS.md notes this is the first `field_validator` in the codebase). Inside the `ShardEnvelope` class body, alongside the existing bitemporal fields (near line 301), add `vocab_version: str = Field(default_factory=lambda: VOCAB_VERSION, description="Phase 8 D-03 vocab pin — Pydantic belt of the two-belt D-04 enforcement; SHACL belt lives in src/folio_insights/vocab/shapes.ttl::fi:VocabPinShape.")`. Below the existing model validators (around line 313+), add `@field_validator("vocab_version") @classmethod def _check_vocab_pin(cls, v: str) -> str: ...` raising `ValueError` if `v != VOCAB_VERSION` with the message `f"vocab_version must equal module constant {VOCAB_VERSION!r}; got {v!r} (Phase 8 D-03 / D-04 two-belt enforcement)."`. The validator returns `v` on success.

    Add a code comment block above the field_validator citing PATTERNS.md note that this is the first `field_validator` in the repo and that the established model_validator precedent at `envelope.py:313` and `subtypes.py:105` was considered but D-03 calls for field_validator verbatim.

    Create `tests/vocab/test_envelope_vocab_pin.py` with the 4 behavior assertions above. Use the existing `_SUBTYPE_DEFAULTS` from `tests/shards/conftest.py` (or replicate the minimum kwargs to construct a `ShardEnvelope`/`SimpleAssertionShard`). Tests are pure-Pydantic — no rdflib/pyshacl.

    Do NOT touch `_SUBTYPE_DEFAULTS` — the new field has a default factory, so every existing fixture auto-inherits the pin without code changes. If a fixture passes `vocab_version` explicitly somewhere, leave that as documentation; the validator accepts the matching value.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" &amp;&amp; uv run pytest tests/vocab/test_envelope_vocab_pin.py tests/shards/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/vocab/test_envelope_vocab_pin.py -x` exits 0 with ≥4 tests collected.
    - `uv run pytest tests/shards/ -x` exits 0 (no regressions in the 47-test shard suite).
    - `grep -n 'vocab_version' src/folio_insights/shards/envelope.py | wc -l` ≥ 3 (field declaration + validator + import comment).
    - `grep -n 'field_validator' src/folio_insights/shards/envelope.py | wc -l` ≥ 1.
    - `grep -n 'from folio_insights.vocab import VOCAB_VERSION' src/folio_insights/shards/envelope.py | wc -l` == 1.
    - `python -c "from folio_insights.shards.envelope import ShardEnvelope; import pytest; import pydantic; from folio_insights.vocab import VOCAB_VERSION; assert VOCAB_VERSION == '2026.05.0'"` exits 0.
  </acceptance_criteria>
  <done>
    Pydantic belt active; existing tests green; mismatched value raises; field round-trips.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bench generator fi:vocabVersion emission + digest regeneration test</name>
  <files>src/folio_insights/bench/generator.py, tests/vocab/test_bench_emits_vocab_version.py, tests/bench/fixtures/expected_digest.txt</files>
  <read_first>
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md (`## Integration Points` § bench generator, `### Reusable Assets` § promotion of constants to NAMESPACES)
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md (`### MOD src/folio_insights/bench/generator.py` section, especially the determinism note about the Phase 0 D-15 contract)
    - src/folio_insights/bench/generator.py (entire file — locate the constants block at lines 49-57 and the `_emit_shard_quads` function around lines 200-261)
    - tests/bench/ (existing bench tests — locate any digest-pinning test and the fixture path; the regeneration must update whichever file currently pins the digest)
    - .planning/phases/00-foundations-hard-gate/00-08-DECISION.md (search for Phase 0 D-15 byte-identical N-Quads contract reference for context)
  </read_first>
  <behavior>
    - Running `python -m folio_insights.bench.cli generate --seed <fixed> --shards <N>` emits exactly one `<shard_iri> <https://folio-insights.aleainstitute.ai/vocab/vocabVersion> "2026.05.0" <graph_iri> .` quad per shard.
    - The new digest baseline file `tests/bench/fixtures/expected_digest.txt` contains the post-Phase-8 SHA-256 of the deterministic N-Quads output.
    - The digest is byte-stable across two back-to-back generator runs with the same seed (re-run produces identical bytes; the Phase 0 D-15 contract).
    - `tests/vocab/test_bench_emits_vocab_version.py` exercises the generator with a deterministic seed and asserts: the output contains the `fi:vocabVersion "2026.05.0"` substring; the count of `vocabVersion` lines equals the shard count; the SHA-256 digest of the sorted-and-canonicalized output matches the pinned baseline.
    - All existing bench tests that pinned the previous digest are updated to point at the new baseline (NOT deleted; the assertion structure is preserved).
  </behavior>
  <action>
    Edit `src/folio_insights/bench/generator.py`. Near the top, after the existing imports, add `from folio_insights.vocab import VOCAB_VERSION` (do NOT yet replace the local `FI` / `CORPUS` / `SHARD` / `CONCEPT` / `FRAMEWORK` string constants — that re-export consolidation is deferred to Phase 11 or a future polish phase per CONTEXT.md `### Reusable Assets`; this plan only ADDS the version emission, keeping the local constants byte-identical so the only digest delta is the one new quad per shard).

    Inside `_emit_shard_quads` (around line 200-261), at the deterministic insertion point AFTER the existing `framework` quad emission and BEFORE the next per-shard quad block, append one new `Quad(shard_iri, NamedNode(f"{FI}vocabVersion"), Literal(VOCAB_VERSION), graph_iri)` per shard. The insertion position is deterministic — same seed continues to produce the same byte sequence, just with one extra quad per shard inserted at the same offset.

    Locate the existing bench-digest test (search `tests/bench/` for `expected_digest` or `sha256`) and the fixture file path it reads. If the fixture path is `tests/bench/fixtures/expected_digest.txt`, regenerate that file. If the fixture lives elsewhere, regenerate AT THAT PATH and DO NOT create a duplicate at `tests/bench/fixtures/expected_digest.txt`. Document the located path in the SUMMARY. The regeneration command (document in code comment): `python -m folio_insights.bench.cli generate --seed <baseline-seed> --shards <baseline-shards> | sha256sum > tests/bench/fixtures/expected_digest.txt` (adapt to the actual CLI surface).

    Create `tests/vocab/test_bench_emits_vocab_version.py`. Test 1: generate N shards with seed S, parse output, assert exactly N lines contain `vocabVersion "2026.05.0"`. Test 2: generate twice with identical seed, assert bytes are identical (byte-stable). Test 3: assert SHA-256 of generator output matches the pinned baseline in `tests/bench/fixtures/expected_digest.txt`.

    Do NOT modify the `_emit_*_quads` helpers in any way other than appending the one new quad per shard. The Phase 0 D-15 byte-identical contract requires the deterministic emission order; changing it elsewhere is out of scope per D-13.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" &amp;&amp; uv run pytest tests/vocab/test_bench_emits_vocab_version.py tests/bench/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/vocab/test_bench_emits_vocab_version.py -x` exits 0.
    - `uv run pytest tests/bench/ -x` exits 0 (the regenerated digest baseline is consistent with whatever existing bench test pinned it).
    - `grep -n 'vocabVersion' src/folio_insights/bench/generator.py | wc -l` ≥ 1.
    - `grep -n 'VOCAB_VERSION' src/folio_insights/bench/generator.py | wc -l` ≥ 2 (one import + one emit-site reference).
    - Running the generator twice with the same seed produces byte-identical output (assertion inside the test).
    - `python -c "from folio_insights.bench import generator; import inspect; src = inspect.getsource(generator); assert 'vocabVersion' in src and 'VOCAB_VERSION' in src"` exits 0.
  </acceptance_criteria>
  <done>
    Bench generator emits `fi:vocabVersion` quad per shard; digest baseline regenerated and pinned; byte-stability test green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pydantic construction | `ShardEnvelope(vocab_version=...)` accepts user input; field_validator is the gate |
| Bench generator output | Generator writes N-Quads consumed by downstream Phase 0 gates; byte-stability is a contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | `vocab_version` field at construction | mitigate | `field_validator` raises ValidationError on any value != VOCAB_VERSION (Pydantic belt; SHACL belt is the redundancy via fi:VocabPinShape from Plan 08-01) |
| T-08-07 | Tampering | Bench digest determinism | mitigate | Byte-stability test asserts two back-to-back runs produce identical output; pinned digest baseline catches future drift in CI |
| T-08-08 | Repudiation | Shard claims it was minted against an older vocab | mitigate | Pydantic + SHACL two-belt rejects unpinned/wrong-pin shards; signed shards include vocab_version in the JCS canonical hash via Phase 6 wiring (no change here — vocab_version is in `model_dump()` output) |
| T-08-09 | Information Disclosure | vocab_version leaks (vocab version is non-secret spec metadata) | accept | Vocab version is intended public spec metadata; no PII surface |
</threat_model>

<verification>
- `uv run pytest tests/vocab/ tests/shards/ tests/bench/ -x` all green.
- `git log --oneline -2` shows two atomic commits: (1) envelope pin, (2) bench emission + digest regen.
- The digest baseline file diff documents the byte-delta count is exactly N (one new quad per shard).
</verification>

<success_criteria>
The Pydantic belt of D-04 ships; ShardEnvelope rejects mismatched `vocab_version`; the bench generator emits `fi:vocabVersion` per shard; the byte-identical N-Quads contract holds with the regenerated digest baseline.
</success_criteria>

<output>
Create `.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-02-SUMMARY.md` when done, documenting:
- The path of the bench digest fixture (located during task 2)
- The byte-delta count between pre-Phase-8 and post-Phase-8 digests
- Test count delta (+4 envelope-pin, +3 bench-emit minimum)
- Confirmation the existing 47-test shard suite still passes
</output>
