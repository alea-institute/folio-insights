---
phase: 08-folio-v2-vocab-mini-bfo-spine-7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/folio_insights/vocab/__init__.py
  - src/folio_insights/vocab/predicates.ttl
  - src/folio_insights/vocab/classes.ttl
  - src/folio_insights/vocab/bfo_spine.ttl
  - src/folio_insights/vocab/bfo_mapping.ttl
  - src/folio_insights/vocab/shapes.ttl
  - src/folio_insights/services/owl_serializer.py
  - src/folio_insights/export/shapes.ttl
  - tests/vocab/__init__.py
  - tests/vocab/test_vocab_load_smoke.py
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03, VOCAB-04]

must_haves:
  truths:
    - "Canonical fi: prefix is exactly https://folio-insights.aleainstitute.ai/vocab/ across all 5 vocab TTL files and the vocab package [D-01]"
    - "FOLIO upstream canonical IRIs (https://folio.openlegalstandard.org/) and v1 frozen extension prefix (.../modules/folio-insights/) are untouched in services/owl_serializer.py and export/shapes.ttl except for a header marker comment [D-01a, D-01b]"
    - "VOCAB_VERSION = \"2026.05.0\" ships as a module constant in folio_insights.vocab and each of the 5 TTL files carries its own owl:versionIRI containing 2026.05.0 [D-02, D-05]"
    - "TTL split lives at src/folio_insights/vocab/{predicates,classes,bfo_spine,bfo_mapping,shapes}.ttl with minimal per-file @prefix blocks [D-08]"
    - "All 4 analogia predicates (fi:primeAnalogate, fi:proportionalRelation, fi:distinguishes, fi:distinctionKind) and 4 distinction kinds (realis, rationis, rationis_cum_fundamento_in_re, analogica) defined in predicates.ttl [VOCAB-02]"
    - "Tractarian fi:elaborates, Spinozan fi:dependsOnAxiom + fi:dependsOnDefinition, Russellian fi:closureMarker, Carnap fi:Framework, Aristotelian fi:predicationMode defined in predicates.ttl [VOCAB-03]"
    - "fi:supersedes and fi:supersededBy declared as an owl:inverseOf pair in predicates.ttl [VOCAB-05, D-10]"
    - "9-class mini-BFO spine (Continuant, IndependentContinuant, SpecificallyDependentContinuant, GenericallyDependentContinuant, Role, Occurrent, Process, Quality, Disposition) in bfo_spine.ttl — drops PRD §7.2 fi:Event, adds SDC/GDC/Quality/Disposition [D-06]"
    - "bfo_mapping.ttl provides owl:equivalentClass to BFO 2020 for all 9 mini-BFO classes [D-07, VOCAB-04]"
    - "vocab/shapes.ttl ships fi:VocabPinShape requiring fi:vocabVersion equals VOCAB_VERSION [D-04]"
    - "folio_insights.vocab.load_graph(*, include_bfo_mapping=False) returns an rdflib.Graph; load_pyoxigraph_store() returns a pyoxigraph in-memory Store; both parse all 5 TTL files via importlib.resources [D-09]"
    - "tests/vocab/test_vocab_load_smoke.py asserts rdflib and pyoxigraph both parse the package without errors and that owl:versionIRI strings contain '2026.05.0'"
  artifacts:
    - path: src/folio_insights/vocab/__init__.py
      provides: "VOCAB_VERSION constant, FI_PREFIX, NAMESPACES, load_graph, load_pyoxigraph_store"
      contains: "VOCAB_VERSION = \"2026.05.0\""
    - path: src/folio_insights/vocab/predicates.ttl
      provides: "PRD §7.1 fi:* properties — analogia, distinguo, elaboration, dependencies, closure, framework, predication_mode, epistemic_status, valid-time, contest, attestations, role, content-edit, provenance-hash, supersession"
      contains: "fi:supersedes"
    - path: src/folio_insights/vocab/classes.ttl
      provides: "PRD §7.2 non-BFO classes + governance classes (CommonAxiom, Postulate, Definition, MeaningPostulate, Framework, RoleAssertion, ContentEdit, GovernanceLog, AttestedSignature, ContestVote)"
      contains: "fi:GovernanceLog"
    - path: src/folio_insights/vocab/bfo_spine.ttl
      provides: "9-class mini-BFO spine"
      contains: "fi:SpecificallyDependentContinuant"
    - path: src/folio_insights/vocab/bfo_mapping.ttl
      provides: "owl:equivalentClass to BFO 2020"
      contains: "owl:equivalentClass"
    - path: src/folio_insights/vocab/shapes.ttl
      provides: "fi:VocabPinShape + per-predicate range/domain SHACL belts"
      contains: "fi:VocabPinShape"
    - path: tests/vocab/test_vocab_load_smoke.py
      provides: "rdflib + pyoxigraph parse smoke + owl:versionIRI presence assertions"
  key_links:
    - from: src/folio_insights/vocab/__init__.py
      to: src/folio_insights/vocab/*.ttl
      via: importlib.resources.files
      pattern: "importlib\\.resources"
    - from: src/folio_insights/vocab/bfo_mapping.ttl
      to: BFO 2020 PURL
      via: owl:equivalentClass
      pattern: "obo/BFO_"
---

<objective>
Ship the FOLIO Insights v2 TTL vocabulary as a self-contained `src/folio_insights/vocab/` package: 5 TTL files (predicates, classes, bfo_spine, bfo_mapping, shapes), a `VOCAB_VERSION` module constant, `importlib.resources`-backed loaders for both rdflib and pyoxigraph, and a smoke test that proves both parse paths work end-to-end. Annotate the v1 OWL export pipeline files with `DO NOT migrate` marker comments per D-01b — header-only, no content changes.

Purpose: Phase 8 anchor. Every downstream `fi:*` predicate in Phase 9-16 dereferences against this package. Two-belt enforcement (Pydantic + SHACL) starts here with `fi:VocabPinShape`; the Pydantic belt lands in Plan 08-02.

Output: A new Python sub-package + 5 OWL/SHACL TTL files + 1 smoke test + 2 marker-only edits to legacy files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The canonical fi: prefix string used across the codebase today -->

From src/folio_insights/polysemy/distinguo.py:32-33:
```python
# Canonical folio-insights vocabulary IRI (PRD §7.1, VOCAB-02).
FI_VOCAB = "https://folio-insights.aleainstitute.ai/vocab/"
```

From src/folio_insights/bench/generator.py:51-55:
```python
FI = "https://folio-insights.aleainstitute.ai/vocab/"
CORPUS = "https://folio-insights.aleainstitute.ai/corpus/"
SHARD = "https://folio-insights.aleainstitute.ai/shard/"
CONCEPT = "https://folio-insights.aleainstitute.ai/concept/"
FRAMEWORK = "https://folio-insights.aleainstitute.ai/framework/"
```

From src/folio_insights/governance/shape_validation.py:32-62 (TTL loader idiom — adapt to importlib.resources):
```python
import pyshacl
from rdflib import RDF, BNode, Graph, Literal, Namespace, URIRef
from rdflib.namespace import XSD

_SHAPES_DIR = Path(__file__).parent / "shapes"

def _load_shape_graph(filename: str) -> Graph:
    path = _SHAPES_DIR / filename
    g = Graph()
    g.parse(str(path), format="turtle")
    return g
```

From src/folio_insights/store/pyoxigraph_store.py:95-98 (bulk_load pattern):
```python
def bulk_load_nquads(self, path: Path) -> None:
    with open(path, "rb") as f:
        self._store.bulk_load(f, format=RdfFormat.N_QUADS)
```

Use `pyoxigraph.Store()` (no path → in-memory) and `store.load(data, format=RdfFormat.TURTLE)` per file for the vocab loader.

BFO 2020 IRIs (from CONTEXT.md `<canonical_refs>`):
- bfo:BFO_0000002 Continuant
- bfo:BFO_0000003 Occurrent
- bfo:BFO_0000004 IndependentContinuant
- bfo:BFO_0000015 Process
- bfo:BFO_0000016 Disposition
- bfo:BFO_0000019 Quality
- bfo:BFO_0000020 SpecificallyDependentContinuant
- bfo:BFO_0000023 Role
- bfo:BFO_0000031 GenericallyDependentContinuant

Recommended bfo namespace declaration: `@prefix bfo: <http://purl.obolibrary.org/obo/> .`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create vocab package scaffold + 5 TTL files + smoke test</name>
  <files>src/folio_insights/vocab/__init__.py, src/folio_insights/vocab/predicates.ttl, src/folio_insights/vocab/classes.ttl, src/folio_insights/vocab/bfo_spine.ttl, src/folio_insights/vocab/bfo_mapping.ttl, src/folio_insights/vocab/shapes.ttl, tests/vocab/__init__.py, tests/vocab/test_vocab_load_smoke.py</files>
  <read_first>
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md (D-01..D-09 + canonical_refs §BFO + specifics)
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md (vocab/__init__.py + TTL files + bfo_spine sections)
    - src/folio_insights/polysemy/distinguo.py (canonical FI_VOCAB constant, line 32-33)
    - src/folio_insights/governance/shape_validation.py (TTL loader idiom + rdflib-only module pattern, lines 1-100)
    - src/folio_insights/store/pyoxigraph_store.py (bulk_load API, lines 80-110)
    - src/folio_insights/governance/shapes/role_assertion_shape.ttl (SHACL prefix block + NodeShape header analog)
    - src/folio_insights/governance/shapes/supersession_shape.ttl (sh:sparql polarity discipline + supersession-domain near-twin)
    - src/folio_insights/governance/shapes/governance_log_shape.ttl (sh:sparql bad-case pattern)
    - src/folio_insights/bench/generator.py:49-57 (constants block to be promoted to NAMESPACES)
    - src/folio_insights/export/shapes.ttl (Turtle prefix-block analog ONLY — content is v1 frozen)
  </read_first>
  <behavior>
    - `from folio_insights.vocab import VOCAB_VERSION` returns the literal string `"2026.05.0"`.
    - `from folio_insights.vocab import FI_PREFIX` returns `"https://folio-insights.aleainstitute.ai/vocab/"`.
    - `folio_insights.vocab.NAMESPACES` is a `Mapping[str, rdflib.Namespace]` containing at least keys `fi`, `corpus`, `shard`, `concept`, `framework`.
    - `folio_insights.vocab.load_graph()` returns an `rdflib.Graph` populated from `predicates.ttl + classes.ttl + bfo_spine.ttl + shapes.ttl` (NOT `bfo_mapping.ttl` by default).
    - `folio_insights.vocab.load_graph(include_bfo_mapping=True)` additionally parses `bfo_mapping.ttl` into the same graph.
    - `folio_insights.vocab.load_pyoxigraph_store()` returns a pyoxigraph in-memory `Store` containing the parsed Turtle from all 5 TTL files; round-trips a `SELECT ?s WHERE { ?s a <http://www.w3.org/2002/07/owl#Ontology> }` with ≥4 rows (one ontology IRI per file).
    - `tests/vocab/test_vocab_load_smoke.py` asserts:
      - `load_graph()` returns a non-empty rdflib Graph.
      - `load_graph(include_bfo_mapping=True)` triple count is strictly greater than `load_graph()`.
      - `load_pyoxigraph_store()` parses without raising.
      - Every TTL file contains the literal substring `2026.05.0` in an `owl:versionIRI` line (read raw file text, assert presence).
      - The canonical `fi:` prefix `https://folio-insights.aleainstitute.ai/vocab/` appears in every TTL file's `@prefix fi:` declaration.
      - `bfo_mapping.ttl` contains at least 9 distinct `owl:equivalentClass` triples whose objects start with `http://purl.obolibrary.org/obo/BFO_` (one per mini-BFO class).
      - `bfo_spine.ttl` declares 9 distinct `fi:*` classes with `a owl:Class`: Continuant, IndependentContinuant, SpecificallyDependentContinuant, GenericallyDependentContinuant, Role, Occurrent, Process, Quality, Disposition.
      - `predicates.ttl` declares `fi:supersedes` and `fi:supersededBy` and one of them carries `owl:inverseOf` referencing the other.
      - `shapes.ttl` declares `fi:VocabPinShape` as a `sh:NodeShape` with `sh:path fi:vocabVersion` and `sh:hasValue "2026.05.0"`.
  </behavior>
  <action>
    Create `src/folio_insights/vocab/__init__.py` with module docstring citing Phase 8 D-01/D-02/D-05/D-08/D-09. Define module-level constants `VOCAB_VERSION = "2026.05.0"`, `FI_PREFIX = "https://folio-insights.aleainstitute.ai/vocab/"`, `NAMESPACES: Mapping[str, Namespace]` containing `fi`, `corpus`, `shard`, `concept`, `framework` keys (promote from `bench/generator.py:51-55` strings into `rdflib.Namespace` objects). Implement `load_graph(*, include_bfo_mapping: bool = False) -> rdflib.Graph` by iterating `importlib.resources.files("folio_insights.vocab")` for `predicates.ttl, classes.ttl, bfo_spine.ttl, shapes.ttl` (+ `bfo_mapping.ttl` when the flag is true) and calling `Graph.parse(data=..., format="turtle")` on the bytes. Implement `load_pyoxigraph_store() -> pyoxigraph.Store` by constructing an in-memory `Store()` and calling `store.load(input=<bytes>, format=RdfFormat.TURTLE)` for each of the 5 files. Per D-09, `bfo_mapping.ttl` is included in `load_pyoxigraph_store()` (the cost-amortisation argument only applies to the rdflib path used by SHACL validation).

    Create `predicates.ttl` with minimal `@prefix` block (`fi`, `owl`, `rdfs`, `xsd`, `dc`) and an `owl:Ontology` header at IRI `<https://folio-insights.aleainstitute.ai/vocab/predicates>` carrying `owl:versionIRI <https://folio-insights.aleainstitute.ai/vocab/2026.05.0/predicates>`. Define every PRD §7.1 predicate: analogia 4 (`fi:primeAnalogate`, `fi:proportionalRelation`, `fi:distinguishes`, `fi:distinctionKind`); elaboration (`fi:elaborates`); dependencies (`fi:dependsOnAxiom`, `fi:dependsOnDefinition`); closure (`fi:closureMarker`); framework (`fi:framework`, `fi:inFramework`); epistemic status (`fi:epistemicStatus`); predication mode (`fi:predicationMode`); supersession (`fi:supersedes`, `fi:supersededBy` declared as an `owl:inverseOf` pair per D-10/D-12); valid-time (`fi:validTimeStart`, `fi:validTimeEnd`, `fi:transactionTime`); contest (`fi:contests`, `fi:contestVote`, `fi:positionText`, `fi:voterDid`, `fi:resolutionPath`); attestations (`fi:signedAction` declared as `owl:DatatypeProperty` with `sh:in` enum NOT here — sh:in lives in shapes.ttl per D-12; just declare the property + rdfs:range xsd:string here); role (`fi:hasRole` declared as `owl:DatatypeProperty` with literal-range per D-12, `fi:hasActiveRoleAt`, `fi:role`, `fi:revokedRole`); content-edit (`fi:editedAt`, `fi:editedBy`, `fi:fieldPath`, `fi:rationale`); provenance-hash (`fi:provenanceHash`, `fi:canonicalContentHash`, `fi:overContentHash`, `fi:signingKeyId`); plus the version-pin predicate (`fi:vocabVersion` as `owl:DatatypeProperty` with `rdfs:range xsd:string`). Use `owl:ObjectProperty` for predicates whose ranges are IRIs and `owl:DatatypeProperty` for literal-range predicates. Every predicate carries `rdfs:label` and `rdfs:comment`.

    Create `classes.ttl` with minimal `@prefix` block and `owl:Ontology` header at IRI `<https://folio-insights.aleainstitute.ai/vocab/classes>` + `owl:versionIRI <https://folio-insights.aleainstitute.ai/vocab/2026.05.0/classes>`. Define PRD §7.2 non-BFO classes (`fi:CommonAxiom`, `fi:Postulate`, `fi:Definition`, `fi:MeaningPostulate`, `fi:Framework`) and the governance classes Phase 7 SHACL shapes target (`fi:RoleAssertion`, `fi:RoleRevocation`, `fi:Promotion`, `fi:Demotion`, `fi:Contest`, `fi:ContestResolution`, `fi:Distinguo`, `fi:Supersession`, `fi:Retraction`, `fi:ContentEdit`, `fi:GovernanceLog`, `fi:AttestedSignature`, `fi:ContestVote`, `fi:Shard`). Per D-12, this is where the drift audit (Plan 08-04) will check that the classes Phase 7's shape files target now exist as `owl:Class` declarations.

    Create `bfo_spine.ttl` with minimal `@prefix` block and `owl:Ontology` header at IRI `<https://folio-insights.aleainstitute.ai/vocab/bfo_spine>` + `owl:versionIRI <https://folio-insights.aleainstitute.ai/vocab/2026.05.0/bfo_spine>`. Declare 9 `owl:Class` entries per D-06: `fi:Continuant`, `fi:IndependentContinuant`, `fi:SpecificallyDependentContinuant`, `fi:GenericallyDependentContinuant`, `fi:Role`, `fi:Occurrent`, `fi:Process`, `fi:Quality`, `fi:Disposition`. Include `rdfs:subClassOf` relationships matching BFO 2020 hierarchy (IndependentContinuant/SDC/GDC are sub-classes of Continuant; Process is sub-class of Occurrent; Role is sub-class of SDC; Quality is sub-class of SDC; Disposition is sub-class of SDC). Each class carries `rdfs:label` and `rdfs:comment` documenting the divergence from PRD §7.2 (drops `fi:Event`; adds SDC/GDC/Quality/Disposition).

    Create `bfo_mapping.ttl` with minimal `@prefix` block including `bfo: <http://purl.obolibrary.org/obo/>` and an `owl:Ontology` header at IRI `<https://folio-insights.aleainstitute.ai/vocab/bfo_mapping>` + `owl:versionIRI <https://folio-insights.aleainstitute.ai/vocab/2026.05.0/bfo_mapping>`. Per D-07, emit 9 exhaustive `owl:equivalentClass` rows mapping each `fi:*` mini-BFO class to its `bfo:BFO_xxxxxxx` counterpart using the 9 IRIs listed in the `<interfaces>` block. Each row carries an `rdfs:comment` citing BFO 2020 PURL.

    Create `shapes.ttl` with minimal `@prefix` block (`fi`, `sh`, `rdf`, `xsd`) and an `owl:Ontology` header at IRI `<https://folio-insights.aleainstitute.ai/vocab/shapes>` + `owl:versionIRI <https://folio-insights.aleainstitute.ai/vocab/2026.05.0/shapes>`. Per D-04 emit `fi:VocabPinShape` targeting `fi:Shard` with `sh:path fi:vocabVersion`, `sh:datatype xsd:string`, `sh:minCount 1`, `sh:maxCount 1`, `sh:hasValue "2026.05.0"`, and `sh:message "fi:vocabVersion must equal VOCAB_VERSION constant (Phase 8 D-04 two-belt enforcement)"`. Also emit `fi:SignedActionEnumShape` providing the 13-value `sh:in` enumeration for `fi:signedAction` per D-12 (values: role_assertion, role_revocation, extract, promote, demote, contest, resolve_contest, distinguo, supersede, retract, content_edit, reparent, reconcile — exact match to the `SignedAction` Literal in `src/folio_insights/shards/envelope.py:85`). Also emit `fi:RoleEnumShape` providing `sh:in` enumeration for `fi:hasRole` (values: extractor, reviewer, arbiter, corpus_admin). The `fi:SupersessionAlignmentShape` sh:sparql gate (CONTEXT D-10) ships in Plan 08-03 alongside `as_of.py`; THIS plan defines only the three node shapes named above.

    Create `tests/vocab/__init__.py` (empty package marker) and `tests/vocab/test_vocab_load_smoke.py` implementing the assertions in `<behavior>`. Use `rdflib.Graph` for the rdflib path and `pyoxigraph.Store` directly for the pyoxigraph path. Read raw TTL file text via `importlib.resources.files("folio_insights.vocab") / "<name>.ttl"` for the substring assertions.

    Apply D-01b marker comments (header-only, NO content changes) to `src/folio_insights/services/owl_serializer.py` (insert a Python comment block after the module docstring per PATTERNS.md §`MOD owl_serializer.py`) and `src/folio_insights/export/shapes.ttl` (insert a Turtle `#` comment block at the very top before any `@prefix` line per PATTERNS.md §`MOD export/shapes.ttl`). The marker text cites D-01a + D-01b and warns against migration.

    Use `pyoxigraph` import path `from pyoxigraph import Store, RdfFormat` (mirrors `store/pyoxigraph_store.py`). Use `rdflib.Graph` and `rdflib.Namespace`. Do NOT introduce any pyshacl import in `vocab/__init__.py`; the shape file is data, not a validator (validators live in Plan 08-02 and Phase 11).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" &amp;&amp; uv run pytest tests/vocab/test_vocab_load_smoke.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/vocab/test_vocab_load_smoke.py -x` exits 0.
    - `grep -c 'https://folio-insights.aleainstitute.ai/vocab/' src/folio_insights/vocab/predicates.ttl src/folio_insights/vocab/classes.ttl src/folio_insights/vocab/bfo_spine.ttl src/folio_insights/vocab/bfo_mapping.ttl src/folio_insights/vocab/shapes.ttl` reports ≥1 for every file.
    - `grep -c '2026.05.0' src/folio_insights/vocab/predicates.ttl src/folio_insights/vocab/classes.ttl src/folio_insights/vocab/bfo_spine.ttl src/folio_insights/vocab/bfo_mapping.ttl src/folio_insights/vocab/shapes.ttl` reports ≥1 for every file (the `owl:versionIRI` line).
    - `grep -E 'fi:(Continuant|IndependentContinuant|SpecificallyDependentContinuant|GenericallyDependentContinuant|Role|Occurrent|Process|Quality|Disposition)\b' src/folio_insights/vocab/bfo_spine.ttl | grep -v '^#' | wc -l` ≥ 9.
    - `grep -c 'obo/BFO_' src/folio_insights/vocab/bfo_mapping.ttl` ≥ 9.
    - `grep -E 'fi:(supersedes|supersededBy)\b' src/folio_insights/vocab/predicates.ttl | grep -v '^#' | wc -l` ≥ 2, and `grep -c 'owl:inverseOf' src/folio_insights/vocab/predicates.ttl` ≥ 1.
    - `grep -c 'fi:VocabPinShape' src/folio_insights/vocab/shapes.ttl` ≥ 1, and `grep -c '"2026.05.0"' src/folio_insights/vocab/shapes.ttl` ≥ 1 (sh:hasValue line).
    - `python -c "from folio_insights.vocab import VOCAB_VERSION, FI_PREFIX, load_graph, load_pyoxigraph_store; assert VOCAB_VERSION == '2026.05.0'; assert FI_PREFIX == 'https://folio-insights.aleainstitute.ai/vocab/'; g = load_graph(include_bfo_mapping=True); assert len(g) > 0; s = load_pyoxigraph_store(); print('OK', len(g))"` prints `OK` followed by a positive integer.
    - `grep -E 'v1-legacy.*DO NOT migrate' src/folio_insights/services/owl_serializer.py src/folio_insights/export/shapes.ttl | wc -l` ≥ 2.
    - `grep -c 'https://folio.openlegalstandard.org/' src/folio_insights/services/owl_serializer.py` is unchanged from the pre-Phase-8 baseline (sacred IRI assertion — D-01a).
  </acceptance_criteria>
  <done>
    All assertions pass; the vocab package imports cleanly; smoke test green; legacy marker comments visible in both v1-frozen files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TTL parse-time | rdflib + pyoxigraph parse Turtle authored in this plan; malformed TTL is a parse-error not a code-exec vector |
| BFO IRI supply chain | bfo_mapping.ttl references external PURL `http://purl.obolibrary.org/obo/BFO_*`; no network fetch occurs at parse — IRIs are opaque strings |
| Prefix-collision squatting | The canonical `fi:` namespace is `https://folio-insights.aleainstitute.ai/vocab/`; legacy v1 prefix `.../modules/folio-insights/` MUST stay frozen so no downstream consumer confuses the two |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | shapes.ttl `sh:hasValue "2026.05.0"` | mitigate | Substring assertion in smoke test pins the exact value; downstream Plan 08-02 Pydantic belt rejects mismatches at construction (two-belt enforcement per CONTEXT D-04) |
| T-08-02 | Spoofing | Canonical `fi:` prefix | mitigate | Plan-wide `@prefix fi: <https://folio-insights.aleainstitute.ai/vocab/>` discipline enforced by grep gate in `<acceptance_criteria>`; v1 frozen prefix gets explicit `DO NOT migrate` marker per D-01b |
| T-08-03 | Information Disclosure | None (TTL is public spec) | accept | Vocab files ship in source tree, no secrets; intended to be world-readable |
| T-08-04 | Denial of Service | `load_graph()` / `load_pyoxigraph_store()` at module import | accept | All 5 TTL files are static, bounded in size (target <50KB total); no network I/O; bench/generator analog has equivalent surface |
| T-08-05 | Tampering | BFO 2020 PURL IRIs in bfo_mapping.ttl | accept | IRIs are opaque strings; no network resolution at parse-time; D-07 documents the PURL source so a downstream consumer can audit |
</threat_model>

<verification>
- `uv run pytest tests/vocab/ -v` green.
- Smoke test green; all grep gates green per `<acceptance_criteria>`.
- `git diff src/folio_insights/services/owl_serializer.py` shows only a comment-block addition (no code line changes).
- `git diff src/folio_insights/export/shapes.ttl` shows only a Turtle comment-block addition (no `@prefix` or shape line changes).
</verification>

<success_criteria>
The vocab package + 5 TTL files exist; rdflib and pyoxigraph both parse them; `VOCAB_VERSION = "2026.05.0"` is the single source of truth; v1-legacy files carry the marker comment; all decision IDs D-01..D-09 cited in this plan are physically present in the artifacts.
</success_criteria>

<output>
Create `.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-01-SUMMARY.md` when done, documenting:
- Each of the 5 TTL files' triple count + owl:versionIRI value
- The `VOCAB_VERSION` value
- Any divergence from PRD §7.2 (the D-06 9-class spine vs the PRD's 7) so Plan 08-04 / a PRD amendment doc can pick it up
- The two v1-legacy marker comments
</output>
