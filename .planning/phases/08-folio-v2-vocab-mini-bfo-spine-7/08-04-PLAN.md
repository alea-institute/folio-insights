---
phase: 08-folio-v2-vocab-mini-bfo-spine-7
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - tests/vocab/test_predicate_drift_audit.py
  - tests/vocab/test_pyoxigraph_roundtrip.py
  - tests/vocab/test_owl_version_iri.py
  - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-DRIFT-AUDIT.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03, VOCAB-04, VOCAB-05]

must_haves:
  truths:
    - "Every `fi:*` predicate emitted by Phase 2–7 source code is cross-checked against `src/folio_insights/vocab/predicates.ttl` and `classes.ttl` from Plan 08-01 [D-12]"
    - "The 4 named suspects from CONTEXT D-12 are explicitly audited: `fi:hasRole` (DatatypeProperty literal-range), `fi:signedAction` (DatatypeProperty + sh:in 13-value enum), `fi:GovernanceLog` (class in classes.ttl), `fi:supersedes`/`fi:supersededBy` (owl:inverseOf pair) [D-12]"
    - "Each in-scope mismatch is fixed via an atomic commit `fix(08): align fi:X with PRD §7.1` — renames, broader refactors, new features are deferred [D-13]"
    - "An audit report `08-DRIFT-AUDIT.md` enumerates every audited predicate, its PRD §7.1 expected shape, the source-code observed shape, and the disposition (no-change / in-scope-fix / deferred)"
    - "Both rdflib AND pyoxigraph parse all 5 vocab TTL files without errors [VOCAB-01 acceptance gate 1]"
    - "Every TTL file's `owl:versionIRI` resolves to a string containing `2026.05.0` [D-05]"
    - "The vocab graph loaded by `folio_insights.vocab.load_graph(include_bfo_mapping=True)` contains every Phase 7 governance class that Phase 7 SHACL shapes reference (the `targetClass` set is a subset of the vocab class declarations) [drift closure for VOCAB-01]"
  artifacts:
    - path: tests/vocab/test_predicate_drift_audit.py
      provides: "Enumeration of Phase 2–7 fi:* predicate usage + cross-check against vocab/predicates.ttl + classes.ttl"
    - path: tests/vocab/test_pyoxigraph_roundtrip.py
      provides: "Asserts pyoxigraph.Store.load() succeeds for all 5 TTL files + a SPARQL query returns ≥5 ontology rows"
    - path: tests/vocab/test_owl_version_iri.py
      provides: "Asserts each TTL file's owl:versionIRI contains '2026.05.0' (D-05)"
    - path: .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-DRIFT-AUDIT.md
      provides: "Per-predicate audit table + disposition + atomic-commit log"
  key_links:
    - from: tests/vocab/test_predicate_drift_audit.py
      to: src/folio_insights/governance/ + src/folio_insights/shards/
      via: source scan (grep + AST) for fi:* predicates
      pattern: "fi:[a-zA-Z]"
---

<objective>
Close the D-12 drift audit: every `fi:*` predicate that Phase 2–7 source code emits is cross-checked against the canonical vocab TTL files Plan 08-01 ships; mismatches are fixed atomically per D-13 scope discipline; and a written audit report enumerates the verdict for every audited predicate. Also ship the rdflib + pyoxigraph round-trip parity test and the `owl:versionIRI` verification per VOCAB-01 acceptance gate 1 and D-05.

Purpose: Without this audit, Phase 9–16 inherits silent definition drift between Python code and the TTL spec. With it, every `fi:*` predicate has one authoritative definition + a regression test that catches future drift.

Output: 3 test files + 1 written audit report + N atomic drift-fix commits (N determined during execution; 0 ≤ N ≤ ~5 based on CONTEXT D-12 suspect list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-01-PLAN.md
@.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-02-PLAN.md

<interfaces>
<!-- Phase 7 SignedAction Literal (the 13-value enum that fi:signedAction must mirror) -->

From src/folio_insights/shards/envelope.py:85 (the 13-value Literal):
```python
SignedAction = Literal[
    "role_assertion", "role_revocation", "extract", "promote", "demote",
    "contest", "resolve_contest", "distinguo", "supersede", "retract",
    "content_edit", "reparent", "reconcile",
]
```

<!-- Phase 7 hasRole / hasActiveRoleAt pattern (the suspect D-12 named) -->

From src/folio_insights/governance/shapes/role_revocation_shape.ttl:69-81 (relevant grep hits):
```turtle
$this fi:revokedRole "corpus_admin" .
$this fi:subjectDid ?subject .
?subjectAdmin fi:hasActiveRoleAt ?subjRoleNode .
?subjRoleNode fi:role "corpus_admin" .
```

<!-- Phase 7 SupersessionEvent (one direction only — fi:supersededBy is inferred via inverseOf per D-12) -->

From src/folio_insights/governance/events.py:230-237:
```python
class SupersessionEvent(_BaseEvent):
    action: Literal["supersede"] = "supersede"
    old_shard_iri: str
    new_shard_iri: str
```

<!-- PRD §7.1 expected shapes for the 4 D-12 suspects -->

PRD §7.1 (from CONTEXT.md D-12):
- fi:hasRole: owl:DatatypeProperty, rdfs:range xsd:string, sh:in ("extractor" "reviewer" "arbiter" "corpus_admin")
- fi:signedAction: owl:DatatypeProperty, rdfs:range xsd:string, sh:in (13 values matching the SignedAction Literal)
- fi:GovernanceLog: owl:Class declared in classes.ttl
- fi:supersedes / fi:supersededBy: owl:ObjectProperty pair connected via owl:inverseOf
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drift audit test + per-predicate cross-check + drift report</name>
  <files>tests/vocab/test_predicate_drift_audit.py, .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-DRIFT-AUDIT.md</files>
  <read_first>
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md (D-12 suspect list, D-13 scope discipline)
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-01-PLAN.md (what predicates.ttl + classes.ttl + shapes.ttl declared)
    - src/folio_insights/vocab/predicates.ttl (from Plan 08-01)
    - src/folio_insights/vocab/classes.ttl (from Plan 08-01)
    - src/folio_insights/vocab/shapes.ttl (from Plan 08-01 + Plan 08-03)
    - src/folio_insights/governance/events.py (all 13 event classes — the source of every emitted fi:* action predicate)
    - src/folio_insights/governance/supersede.py (fi:supersedes site)
    - src/folio_insights/governance/contest.py (fi:contests / fi:contestVote sites)
    - src/folio_insights/governance/retract.py (fi:retracts / fi:cascadePreviewHash sites)
    - src/folio_insights/governance/authorize.py (fi:hasRole / fi:hasActiveRoleAt sites)
    - src/folio_insights/governance/shapes/ (entire directory — 8 shape files that reference fi:* targetClass + sh:path predicates)
    - src/folio_insights/shards/envelope.py (the 13-value SignedAction Literal at line 85; the 6 frozen identity fields)
    - src/folio_insights/bench/generator.py (the per-shard predicate emissions: fi:framework, fi:transactionTime, fi:vocabVersion, etc.)
  </read_first>
  <behavior>
    - The test enumerates every `fi:*` predicate found in `src/folio_insights/governance/`, `src/folio_insights/shards/`, `src/folio_insights/polysemy/`, `src/folio_insights/bench/`, `src/folio_insights/revision/`, `src/folio_insights/temporal/` by scanning Python and TTL files for the regex `fi:[a-zA-Z][a-zA-Z0-9_]*`. The set is deduplicated.
    - For every emitted predicate, the test asserts at least one of: (a) the predicate is declared in `vocab/predicates.ttl`, OR (b) the predicate is the local name of a class declared in `vocab/classes.ttl` (e.g., `fi:GovernanceLog` is a class), OR (c) the predicate is a SHACL shape name declared in `vocab/shapes.ttl` (e.g., `fi:VocabPinShape`), OR (d) the predicate is a recognized blank-node placeholder (e.g., `fi:hasActiveRoleAt` may be a bnode-marker construct that the audit explicitly waives with a documented rationale).
    - The test asserts the 4 named D-12 suspects specifically:
      - `fi:hasRole` is declared in `predicates.ttl` AND has a corresponding `sh:in` enumeration in `shapes.ttl` matching `["extractor", "reviewer", "arbiter", "corpus_admin"]` (the `fi:RoleEnumShape` from Plan 08-01).
      - `fi:signedAction` is declared in `predicates.ttl` AND has a corresponding `sh:in` enumeration in `shapes.ttl` matching the 13-value `SignedAction` Literal from `src/folio_insights/shards/envelope.py:85` (the `fi:SignedActionEnumShape` from Plan 08-01).
      - `fi:GovernanceLog` is declared as `owl:Class` in `classes.ttl`.
      - `fi:supersedes` and `fi:supersededBy` are both declared in `predicates.ttl` AND one of them carries `owl:inverseOf` referencing the other.
    - The audit report `08-DRIFT-AUDIT.md` lists every emitted predicate with: name | source file | PRD §7.1 expected shape | observed shape | disposition (no-change / in-scope-fix-applied-in-commit-<sha> / deferred-with-rationale).
    - If the test discovers a mismatch that is in-scope per D-13 (changes TTL or envelope/Pydantic field declaration only), the executor fixes it in an atomic commit `fix(08): align fi:X with PRD §7.1` BEFORE the test passes, and the audit report records the commit SHA.
    - If the test discovers a mismatch that is out-of-scope per D-13 (rename / broader refactor / new feature), the executor records the gap in the audit report under "Deferred mismatches" with a citation and the test waives that predicate via an explicit allow-list documented inline.
  </behavior>
  <action>
    Create `tests/vocab/test_predicate_drift_audit.py`. At the top, declare two sets:
      1. `EMITTED_PREDICATES` — populated by recursively scanning `src/folio_insights/{governance,shards,polysemy,bench,revision,temporal}/*.py` and `src/folio_insights/governance/shapes/*.ttl` for the regex `fi:[a-zA-Z][a-zA-Z0-9_]*`. Use Python `re` + `pathlib.Path.rglob`. Strip the `fi:` prefix; the resulting set is the local-name set.
      2. `DECLARED_PREDICATES` — populated by parsing the 5 vocab TTL files via `folio_insights.vocab.load_graph(include_bfo_mapping=True)`, iterating all subjects with `rdf:type` in `{owl:ObjectProperty, owl:DatatypeProperty, owl:AnnotationProperty, owl:Class, sh:NodeShape}`. Strip the canonical `fi:` namespace; the resulting set is the local-name set.

    The first assertion: `assert EMITTED_PREDICATES - DECLARED_PREDICATES - DOCUMENTED_WAIVERS == set()` — every emitted name is either declared or explicitly waived. `DOCUMENTED_WAIVERS` is a hardcoded allow-list at the top of the test file with one entry per waived predicate citing the audit report row.

    Add 4 explicit-suspect tests per the `<behavior>` block:
      - `test_hasRole_datatype_with_sh_in_enum()`: load vocab graph; assert `fi:hasRole` has `rdf:type owl:DatatypeProperty` AND `vocab/shapes.ttl` declares a NodeShape that targets `fi:hasRole` with `sh:in` enumerating exactly the 4 role names.
      - `test_signedAction_datatype_with_13_value_enum()`: assert `fi:signedAction` is `owl:DatatypeProperty` AND `shapes.ttl` declares a NodeShape with `sh:in` enumerating exactly the 13 `SignedAction` Literal values (import the Literal from `shards.envelope` to derive the expected set dynamically).
      - `test_GovernanceLog_class_declared()`: assert `fi:GovernanceLog` has `rdf:type owl:Class` in the vocab graph.
      - `test_supersedes_inverseOf_supersededBy()`: assert both `fi:supersedes` and `fi:supersededBy` are declared AND `(fi:supersedes, owl:inverseOf, fi:supersededBy)` OR `(fi:supersededBy, owl:inverseOf, fi:supersedes)` is in the graph.

    Run the test. For each failure that represents an in-scope D-13 fix:
      - Edit ONLY the relevant TTL file (predicates.ttl, classes.ttl, or shapes.ttl) to align with the PRD §7.1 shape. (If the source code side is wrong — e.g., `governance/events.py` uses a Python field name that mismatches the TTL — the Python side is editable too, but ONLY if it's a one-line declaration; renames/refactors are deferred per D-13.)
      - Commit each fix atomically: `git commit -m "fix(08): align fi:X with PRD §7.1"`.
      - Re-run the test until green.

    For each deferred mismatch, add the predicate to `DOCUMENTED_WAIVERS` with an inline comment citing the audit report row + rationale.

    Author `08-DRIFT-AUDIT.md` with:
      - H1 + intro citing D-12 + D-13 + the test file.
      - H2 `## Audited Predicates` — a table: | predicate | source file:line | PRD §7.1 expected | observed | disposition |.
      - H2 `## In-scope fixes applied` — one row per atomic commit with the commit SHA + message + 1-sentence description.
      - H2 `## Deferred mismatches` — one row per waiver with citation + rationale + tracking note for a future polish phase.
      - H2 `## Summary` — total emitted, total declared, total waived, total fixed.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" &amp;&amp; uv run pytest tests/vocab/test_predicate_drift_audit.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/vocab/test_predicate_drift_audit.py -x` exits 0 with ≥5 tests (1 enumeration + 4 explicit suspects).
    - `grep -c '|' .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-DRIFT-AUDIT.md` ≥ 10 (table rows).
    - `git log --oneline --all -- src/folio_insights/vocab/ src/folio_insights/governance/ src/folio_insights/shards/ | grep -E '^[a-f0-9]+ fix\(08\): align fi:'` lists 0 or more atomic fix commits (count documented in audit report).
    - `python -c "from folio_insights.vocab import load_graph; from rdflib import URIRef; g = load_graph(include_bfo_mapping=True); fi = 'https://folio-insights.aleainstitute.ai/vocab/'; assert (URIRef(fi+'supersedes'), URIRef('http://www.w3.org/2002/07/owl#inverseOf'), URIRef(fi+'supersededBy')) in g or (URIRef(fi+'supersededBy'), URIRef('http://www.w3.org/2002/07/owl#inverseOf'), URIRef(fi+'supersedes')) in g"` exits 0.
  </acceptance_criteria>
  <done>
    Drift audit test green; report enumerates every emitted predicate; atomic fixes committed; deferred items documented with rationale.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rdflib + pyoxigraph round-trip + owl:versionIRI verification</name>
  <files>tests/vocab/test_pyoxigraph_roundtrip.py, tests/vocab/test_owl_version_iri.py</files>
  <read_first>
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-CONTEXT.md (VOCAB-01 acceptance gate 1: "TTL parses with both rdflib and pyoxigraph"; D-05 owl:versionIRI per file)
    - .planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-PATTERNS.md (`### src/folio_insights/vocab/__init__.py` § pyoxigraph round-trip pattern)
    - src/folio_insights/vocab/__init__.py (from Plan 08-01 — load_graph + load_pyoxigraph_store)
    - src/folio_insights/vocab/{predicates,classes,bfo_spine,bfo_mapping,shapes}.ttl (from Plan 08-01 — owl:versionIRI in each)
    - src/folio_insights/store/pyoxigraph_store.py (pyoxigraph Store API; lines 80-110)
    - .planning/research/PITFALLS.md (Pitfall D7: pyoxigraph 0.5.7 dropped rdf-star; relevant if any vocab TTL uses RDF-star)
  </read_first>
  <behavior>
    - `tests/vocab/test_pyoxigraph_roundtrip.py` asserts that `folio_insights.vocab.load_pyoxigraph_store()` parses all 5 TTL files without raising, that the resulting `Store` answers a SPARQL `SELECT ?ont WHERE { ?ont a owl:Ontology }` query with ≥5 rows (one ontology IRI per TTL file), and that `folio_insights.vocab.load_graph(include_bfo_mapping=True)` returns an `rdflib.Graph` whose triple count is greater than `load_graph(include_bfo_mapping=False)`.
    - The same query executed against the rdflib graph and the pyoxigraph store returns the same set of ontology IRIs (parity).
    - `tests/vocab/test_owl_version_iri.py` asserts that for each of the 5 TTL files, an `owl:versionIRI` triple exists in the rdflib graph whose object IRI contains the substring `2026.05.0`. Specifically: the `predicates`, `classes`, `bfo_spine`, `bfo_mapping`, `shapes` ontology IRIs all have an `owl:versionIRI` row pinning the current `VOCAB_VERSION`.
    - The pyoxigraph SELECT uses the `prefix owl: <http://www.w3.org/2002/07/owl#>` declaration via `Store.query(...)`.
    - Both tests confirm PITFALLS D7 mitigation: NO RDF-star syntax in any of the 5 vocab TTL files (grep `<<` / `>>` triple-term syntax; zero hits).
  </behavior>
  <action>
    Create `tests/vocab/test_pyoxigraph_roundtrip.py`. Three tests:
      1. `test_pyoxigraph_loads_all_five_ttl_files()`: call `from folio_insights.vocab import load_pyoxigraph_store; s = load_pyoxigraph_store()`. Assert `len(s) > 0` (triple count). Run `results = list(s.query("PREFIX owl: <http://www.w3.org/2002/07/owl#> SELECT ?ont WHERE { ?ont a owl:Ontology }"))`. Assert `len(results) >= 5` (one ontology IRI per TTL file).
      2. `test_rdflib_pyoxigraph_parity_on_ontology_iris()`: load both via `load_graph(include_bfo_mapping=True)` and `load_pyoxigraph_store()`. Extract the set of `?ont` IRIs from each. Assert the two sets are equal.
      3. `test_no_rdf_star_syntax_in_vocab_ttls()`: read each of the 5 TTL files via `importlib.resources.files("folio_insights.vocab")`. Assert no file contains `<<` or `>>` (PITFALLS D7 mitigation — pyoxigraph 0.5.7 silently returns empty against RDF-star patterns).

    Create `tests/vocab/test_owl_version_iri.py`. Single test asserting D-05 + VOCAB-01:
      - Load vocab graph via `load_graph(include_bfo_mapping=True)`.
      - For each expected ontology IRI in `["predicates", "classes", "bfo_spine", "bfo_mapping", "shapes"]`:
        - Construct `ont_iri = URIRef(f"https://folio-insights.aleainstitute.ai/vocab/{name}")`.
        - Query `graph.objects(ont_iri, OWL.versionIRI)`.
        - Assert at least one object exists.
        - Assert the str of that object contains `2026.05.0`.
        - Assert the str of that object follows the pattern `https://folio-insights.aleainstitute.ai/vocab/2026.05.0/{name}`.

    Use `from folio_insights.vocab import load_graph, load_pyoxigraph_store, VOCAB_VERSION`. Use `from rdflib import URIRef, OWL` (or `Graph().namespace_manager`). Use `from pyoxigraph import Store, NamedNode, QueryResults`.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" &amp;&amp; uv run pytest tests/vocab/test_pyoxigraph_roundtrip.py tests/vocab/test_owl_version_iri.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/vocab/test_pyoxigraph_roundtrip.py -x` exits 0 with 3 tests.
    - `uv run pytest tests/vocab/test_owl_version_iri.py -x` exits 0 with ≥1 test (the loop counts as 1 parameterized test or 5 separate cases — either is acceptable).
    - `python -c "from folio_insights.vocab import load_pyoxigraph_store; s = load_pyoxigraph_store(); rows = list(s.query('PREFIX owl: <http://www.w3.org/2002/07/owl#> SELECT ?o WHERE { ?o a owl:Ontology }')); print(len(rows))"` prints `5` (or more if any TTL declares multiple ontologies).
    - `grep -E '<<|>>' src/folio_insights/vocab/*.ttl | wc -l` == 0 (PITFALLS D7 mitigation).
    - `uv run pytest tests/vocab/ -x` exits 0 (all 4 test files from Plans 08-01..08-04 green together).
  </acceptance_criteria>
  <done>
    rdflib + pyoxigraph parity verified; every owl:versionIRI pinned to 2026.05.0; PITFALLS D7 mitigation in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit scan | The audit reads source files and TTL files; no external execution or network |
| Pyoxigraph parse | Untrusted TTL is parsed; parse errors are caught and reported, not silently swallowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering | A new `fi:*` predicate added in a future phase without TTL declaration | mitigate | `test_predicate_drift_audit.py` recurses the source tree on every CI run; new undeclared predicates fail the test |
| T-08-16 | Tampering | An RDF-star pattern smuggled into a vocab TTL (pyoxigraph 0.5.7 silently returns empty against `<<` / `>>` per PITFALLS D7) | mitigate | `test_no_rdf_star_syntax_in_vocab_ttls()` grep-asserts zero hits across the 5 files |
| T-08-17 | Tampering | `owl:versionIRI` drift between TTL file and `VOCAB_VERSION` constant | mitigate | `test_owl_version_iri.py` asserts the substring `2026.05.0` appears in every TTL's versionIRI object |
| T-08-18 | Repudiation | A drift fix applied without an atomic commit reference | mitigate | D-13 discipline: each fix gets its own `fix(08): align fi:X with PRD §7.1` commit; audit report records SHAs |
</threat_model>

<verification>
- `uv run pytest tests/vocab/ -x` green across all 4 test files (smoke + envelope-pin + bench-emit + drift-audit + roundtrip + versionIRI).
- `08-DRIFT-AUDIT.md` complete with table + commits + deferred items + summary.
- All 4 D-12 named suspects pass their dedicated tests.
- No RDF-star syntax anywhere in `src/folio_insights/vocab/*.ttl`.
</verification>

<success_criteria>
The drift between Phase 2–7 source code and Phase 8 vocab TTL is fully audited, all in-scope mismatches are fixed atomically per D-13, deferred items have documented rationale, rdflib + pyoxigraph parity is verified, and every owl:versionIRI is pinned to `2026.05.0`. VOCAB-01 acceptance gate 1 ("TTL parses with both rdflib and pyoxigraph; owl:versionIRI stable") is satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-folio-v2-vocab-mini-bfo-spine-7/08-04-SUMMARY.md` when done, documenting:
- Total predicates emitted by Phase 2–7 (the size of `EMITTED_PREDICATES`)
- Total predicates declared in the vocab (`DECLARED_PREDICATES`)
- Total in-scope fixes applied (with commit SHA list)
- Total deferred items (with rationale-pointers into `08-DRIFT-AUDIT.md`)
- Confirmation of rdflib + pyoxigraph parity on the ontology IRI set
- Confirmation every owl:versionIRI contains `2026.05.0`
- Cross-reference: any necessary amendment to PRD §7.2 (specifically the 9-class vs 7-class mini-BFO divergence per D-06) — flag for a separate PR or future doc-fix
</output>
