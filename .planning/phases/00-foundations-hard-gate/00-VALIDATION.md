---
phase: 0
slug: foundations-hard-gate
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-22
---

# Phase 0 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md §Validation Architecture. Planner fills the per-task table in Step 8.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (existing v1 harness) + pytest-benchmark (new, Wave 0) |
| **Config file** | `pyproject.toml` (existing `[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest -x --ff -q` |
| **Full suite command** | `uv run pytest --benchmark-skip` |
| **Benchmark run command** | `uv run pytest -m benchmark --benchmark-only` |
| **Estimated runtime** | ~45s quick, ~3m full (excluding 1M-triple bench) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest -x --ff -q`
- **After every plan wave:** Run `uv run pytest --benchmark-skip`
- **Before `/gsd-verify-work`:** Full suite green + all 5 gate benchmarks recorded in `00-DECISION.md`
- **Max feedback latency:** 60 seconds (excluding 1M-triple gates which are inherently slow)

---

## Per-Task Verification Map

*Populated by `/gsd-plan-phase` (Step 8) per plan. Each task receives a row.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | TBD         | TBD        | TBD             | TBD       | TBD               | TBD         | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Derived from RESEARCH.md Environment Availability section. Planner MUST include a Wave 0 plan that installs and verifies:

- [ ] `pyproject.toml` — add `pyoxigraph==0.5.7`, `oxrdflib`, `owlready2`, `pytest-benchmark`, `hyperfine` (dev) deps
- [ ] `tests/conftest.py` — shared fixtures for pyoxigraph Store, temp dirs, seeded RNG (`--seed N`)
- [ ] `tests/bench/` — directory + `__init__.py` + `conftest.py` for benchmark-only fixtures
- [ ] `tests/bench/test_gate1_rdf12.py` — RDF-12 annotation round-trip stubs (REQ-STORAGE-04)
- [ ] `tests/bench/test_gate2_sparql.py` — P95 SPARQL harness stub (REQ-QUALITY-01)
- [ ] `tests/bench/test_gate3_image.py` — worker image size assertion (REQ-QUALITY-03)
- [ ] `tests/bench/test_gate4_ssr.py` — cold-page latency stub (REQ-QUALITY-04)
- [ ] `tests/bench/test_gate5_digest.py` — bit-identical digest comparison (REQ-OBS-04)
- [ ] `fixtures/bench/` — seeded 1k / 10k / 100k / 1M corpus artifacts (gitignored; generated by `folio-insights bench gen`)
- [ ] JVM 17 + HermiT jar availability check (`uv run python -c "import owlready2; owlready2.default_world.get_ontology('http://test.org/').load()"`)
- [ ] Dagger CLI available (`dagger version` in CI image)
- [ ] `docker buildx` with BuildKit reproducibility support (`docker buildx create --use`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Gate 5 bit-identical digest local vs Railway | OBS-04 | Requires Railway deploy — not reproducible in CI sandbox | Build locally → note digest → push → compare `docker inspect` output on Railway-produced image |
| DECISION.md `keep=pyoxigraph` vs `pivot=fuseki` verdict | STORAGE-04 | Human judgment on gate-tuning results; not automatable | Review `00-DECISION.md` measurement table; verify verdict matches D-04/D-05/D-06/D-07/D-08 rules |
| PHILOSOPHY.md rename preserves intent | (deliverable) | Visual/editorial review | Diff `docs/2026-04-19_Philosophy.md` → `PHILOSOPHY.md`; verify all internal links still resolve |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references above
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s (non-benchmark tasks)
- [ ] Each of the 5 HARD GATES maps to exactly one `tests/bench/test_gate{N}_*.py` assertion
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
