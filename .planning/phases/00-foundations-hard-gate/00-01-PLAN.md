---
phase: 0
plan: 1
plan_number: 1
plan_name: "prep-deps-rename-wave0-scaffold"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .dockerignore
  - PHILOSOPHY.md
  - 2026-04-19_Philosophy.md
  - tests/bench/__init__.py
  - tests/bench/conftest.py
  - fixtures/bench/.gitkeep
  - fixtures/gold_queries/.gitkeep
  - fixtures/gold_queries/adversarial/.gitkeep
autonomous: true
requirements: [SEC-02, QUALITY-03]
requirements_addressed: [SEC-02, QUALITY-03]
user_setup: []
must_haves:
  truths:
    - "PHILOSOPHY.md exists at repo root (D-18)"
    - "pyproject.toml declares pinned Phase 0 deps (pyoxigraph==0.5.7, oxrdflib, owlready2==0.50)"
    - "pyproject.toml `requires-python = \">=3.11,<3.13\"` (Pitfall 8)"
    - "tests/bench/ scaffold exists with session-scoped bench_store fixture stub"
    - "fixtures/bench/ and fixtures/gold_queries/ directories tracked in git"
    - "uv sync exits 0 on the new dep set"
  artifacts:
    - path: "PHILOSOPHY.md"
      provides: "Philosophy doc at repo root (D-18)"
      min_lines: 100
    - path: "pyproject.toml"
      provides: "Pinned Phase 0 deps, `requires-python` tightened to <3.13"
      contains: "pyoxigraph==0.5.7"
    - path: "tests/bench/conftest.py"
      provides: "bench_1m_corpus + bench_store fixtures (session-scoped), seeded_rng fixture"
      exports: ["bench_1m_corpus", "bench_store", "seeded_rng"]
    - path: ".dockerignore"
      provides: "Excludes .git, output/, .planning/, node_modules, __pycache__ per Gate 5 step 10"
      contains: ".planning/"
  key_links:
    - from: "pyproject.toml"
      to: "uv sync / pip install"
      via: "hatchling build-system + [project.dependencies]"
      pattern: "pyoxigraph==0\\.5\\.7"
    - from: "tests/bench/conftest.py"
      to: "pyoxigraph.Store"
      via: "bench_store fixture calls Store + bulk_load + optimize"
      pattern: "from pyoxigraph import Store"
    - from: "PHILOSOPHY.md"
      to: "repo root"
      via: "git mv from 2026-04-19_Philosophy.md (preserves blame)"
      pattern: "test -f PHILOSOPHY.md"
---

<objective>
Prepare Phase 0 ground: add the locked deps (pyoxigraph, oxrdflib, owlready2, pytest-benchmark, dagger-io), tighten Python version per Pitfall 8, rename `2026-04-19_Philosophy.md` → `PHILOSOPHY.md` (D-18), and scaffold the `tests/bench/` Wave 0 test tree so downstream plans can drop implementations into ready-made hooks.

Purpose: Close the VALIDATION.md Wave 0 gaps before any gate-measurement plan runs. Every downstream plan depends on the fixtures created here.

Output:
- `PHILOSOPHY.md` at repo root (D-18)
- Updated `pyproject.toml` with pinned deps + `requires-python=">=3.11,<3.13"`
- `.dockerignore` updated with Gate 5 exclusions
- `tests/bench/` with `conftest.py` exposing `bench_1m_corpus`, `bench_store`, `seeded_rng` fixtures
- Empty `fixtures/bench/` and `fixtures/gold_queries/` directories committed via `.gitkeep`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundations-hard-gate/00-CONTEXT.md
@.planning/phases/00-foundations-hard-gate/00-RESEARCH.md
@.planning/phases/00-foundations-hard-gate/00-PATTERNS.md
@.planning/phases/00-foundations-hard-gate/00-VALIDATION.md
@pyproject.toml
@Dockerfile
@.dockerignore
@tests/conftest.py

<interfaces>
<!-- Current baseline state from repo scan. -->

Current `pyproject.toml` dependencies (lines 6-19):
```toml
dependencies = [
    "pydantic>=2.7.0",
    "pydantic-settings>=2.7.0",
    "httpx>=0.28.0",
    "rdflib>=7.6.0",
    "lxml>=5.0",
    "sentence-transformers>=3.0.0",
    "instructor>=1.14.0",
    "click>=8.0.0",
    "aiosqlite>=0.20.0",
    "folio-python>=0.1.5",
    "sse-starlette>=3.3.0",
    "pyshacl>=0.31.0",
]
```

Current `requires-python = ">=3.11"` on line 5 (must tighten to `>=3.11,<3.13` per Pitfall 8).

Current `[project.optional-dependencies]` (lines 21-26):
```toml
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.25.0",
    "pytest-timeout>=2.3",
]
```

Philosophy doc confirmed at repo root (NOT `docs/2026-04-19_Philosophy.md` as CONTEXT.md Open Question #1 noted): `/home/damienriehl/Coding Projects/folio-insights/2026-04-19_Philosophy.md` (76782 bytes). CONTEXT.md D-18 is correct in spirit; the path has no `docs/` prefix. This plan resolves the open question by confirming the actual path via `ls`.

Existing `tests/conftest.py` fixture pattern to replicate:
```python
"""Shared fixtures for folio-insights."""
from __future__ import annotations
from pathlib import Path
import pytest
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pinned Phase 0 deps, tighten Python version, update .dockerignore, git-mv PHILOSOPHY.md</name>
  <files>pyproject.toml, .dockerignore, PHILOSOPHY.md (from 2026-04-19_Philosophy.md)</files>
  <read_first>
    - pyproject.toml (current state — see &lt;interfaces&gt; above)
    - .dockerignore (verify current exclusions before appending)
    - .planning/phases/00-foundations-hard-gate/00-PATTERNS.md §Plan P0-B #1 (pyproject pattern to append) + §Shared 2 (Gate 5 reproducibility discipline)
    - .planning/phases/00-foundations-hard-gate/00-RESEARCH.md §Common Pitfalls Pitfall 8 (Python 3.11-3.12 pin) + §Environment Availability (Python 3.11 row)
    - .planning/phases/00-foundations-hard-gate/00-CONTEXT.md D-18 (rename scope — pure git mv, no content change)
  </read_first>
  <action>
  **Step 1 — Verify philosophy file location:**
  Run `ls "2026-04-19_Philosophy.md"` to confirm the file is at repo root (NOT in `docs/`). This resolves CONTEXT.md Open Question 1.

  **Step 2 — Rename philosophy file (preserve blame via git mv):**
  ```bash
  git mv "2026-04-19_Philosophy.md" "PHILOSOPHY.md"
  ```
  Do NOT edit content. D-18 explicitly scopes to rename only; restructure is Phase 18.

  **Step 3 — Update `pyproject.toml`** with the following EXACT edits (use the Edit tool per-line; do NOT rewrite the file):

  3a. Change line 5:
  FROM: `requires-python = ">=3.11"`
  TO:   `requires-python = ">=3.11,<3.13"`  # Pitfall 8: instructor+pydantic structural-match regressions on 3.13

  3b. After line 18 (`"pyshacl>=0.31.0",`) and before line 19 (`]`), append these 3 entries INSIDE the `dependencies` list:
  ```toml
      "pyoxigraph==0.5.7",      # RDF 1.2 store; locked per RISK-3 + D-04 (STORAGE-04)
      "oxrdflib",                # rdflib Store bridge; one-way for non-RDF-12 paths (STORAGE-02)
      "owlready2==0.50",         # HermiT JVM wrapper; worker-tier only per RISK-1 (PRINCIPLE-01)
  ```

  3c. Replace the entire `[project.optional-dependencies]` block (lines 21-26) with:
  ```toml
  [project.optional-dependencies]
  dev = [
      "pytest>=9.0",
      "pytest-asyncio>=0.25.0",
      "pytest-timeout>=2.3",
      "pytest-benchmark>=5.1",   # Gate 2 P95 harness (QUALITY-01)
      "dagger-io",                # Gate 5 CI pipeline Python SDK (OBS-04); P0-05 uses this
  ]
  ```

  **Step 4 — Update `.dockerignore`** with Gate 5 step 10 + Pitfall 5 exclusions. Read the current file first, then add any missing entries from this list (one per line; do NOT duplicate):
  ```
  .git
  .planning
  .pytest_cache
  .venv
  node_modules
  output
  __pycache__
  *.pyc
  .env
  ```
  Rationale: `output/` is excluded per Pitfall 5 (digest drift from SQLite WAL). `.planning/` and `.git/` per Gate 5 step 10. `__pycache__`/`*.pyc` per `PYTHONDONTWRITEBYTECODE=1` principle.

  **Step 5 — Install + validate:**
  ```bash
  uv sync --all-extras
  ```
  This must exit 0. If it fails on `pyoxigraph==0.5.7` wheel availability, that is a blocker — surface immediately (do NOT fall back to an unpinned version).
  </action>
  <acceptance_criteria>
    - `test -f PHILOSOPHY.md` exits 0 AND `test ! -e "2026-04-19_Philosophy.md"` exits 0 (file moved, not copied)
    - `git log --follow PHILOSOPHY.md | grep -c "^commit"` returns &gt;= 2 (blame preserved; git mv not cp+add)
    - `grep -c 'pyoxigraph==0.5.7' pyproject.toml` returns 1
    - `grep -c 'oxrdflib' pyproject.toml` returns 1
    - `grep -c 'owlready2==0.50' pyproject.toml` returns 1
    - `grep -c 'pytest-benchmark>=5.1' pyproject.toml` returns 1
    - `grep -c 'dagger-io' pyproject.toml` returns 1
    - `grep -c 'requires-python = ">=3.11,<3.13"' pyproject.toml` returns 1
    - `grep -q '^.planning' .dockerignore` AND `grep -q '^output' .dockerignore` AND `grep -q '^.git' .dockerignore` AND `grep -q '^node_modules' .dockerignore`
    - `uv sync --all-extras` exits 0
    - `uv run python -c "import pyoxigraph; print(pyoxigraph.__version__)"` prints `0.5.7`
    - `uv run python -c "import owlready2; print(owlready2.__version__)"` prints `0.50` or higher
    - `uv run python -c "import pytest_benchmark"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f PHILOSOPHY.md &amp;&amp; test ! -e "2026-04-19_Philosophy.md" &amp;&amp; grep -q 'pyoxigraph==0.5.7' pyproject.toml &amp;&amp; grep -q 'requires-python = "&gt;=3.11,&lt;3.13"' pyproject.toml &amp;&amp; uv sync --all-extras &amp;&amp; uv run python -c "import pyoxigraph, owlready2, pytest_benchmark; print(pyoxigraph.__version__)"</automated>
  </verify>
  <done>Philosophy file renamed with blame preserved; pyproject.toml declares all Phase 0 deps with exact pins; uv sync succeeds; all three Python imports succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold tests/bench/ tree with session-scoped fixtures + fixtures/ directories</name>
  <files>tests/bench/__init__.py, tests/bench/conftest.py, fixtures/bench/.gitkeep, fixtures/gold_queries/.gitkeep, fixtures/gold_queries/adversarial/.gitkeep</files>
  <read_first>
    - tests/conftest.py (existing project fixture idiom — from __future__ import annotations, pytest import style)
    - .planning/phases/00-foundations-hard-gate/00-PATTERNS.md §Plan P0-B #2 (conftest.py pattern — lines 111-170 of PATTERNS.md) + §Shared 3 (Determinism discipline Pitfall 7)
    - .planning/phases/00-foundations-hard-gate/00-RESEARCH.md §Code Examples Example 4 (lines 807-825 — bench_1m_corpus fixture pattern)
    - .planning/phases/00-foundations-hard-gate/00-VALIDATION.md §Wave 0 Requirements (checklist of required fixtures)
  </read_first>
  <behavior>
    - Test 1: `bench_1m_corpus` fixture returns `Path("fixtures/bench.nq")` when that path exists, else fails with instructive message pointing to `folio-insights bench gen --seed 42 --target 1000000` (fixture consumer does not exist yet in Wave 1; assert existence logic only)
    - Test 2: `bench_store` fixture (session-scoped) creates in-memory `pyoxigraph.Store()`, calls `.bulk_load()` + `.optimize()` only when `bench_1m_corpus` exists; skips with informative message otherwise
    - Test 3: `seeded_rng` fixture returns a `random.Random(42)` instance (NOT module-level `random`; NOT numpy.random) per Pitfall 7
    - Test 4: `tests/bench/__init__.py` exists so pytest discovers this as a package
    - Test 5: `fixtures/bench/`, `fixtures/gold_queries/`, `fixtures/gold_queries/adversarial/` exist with `.gitkeep` files (tracked by git)
  </behavior>
  <action>
  **Step 1 — Create `tests/bench/__init__.py`** (empty file; one-line header OK):
  ```python
  """Phase 0 benchmark tests (Gates 1-5 measurement harness)."""
  ```

  **Step 2 — Create `tests/bench/conftest.py`** verbatim (copy from PATTERNS.md §Plan P0-B #2 + RESEARCH.md Example 4):
  ```python
  """Shared fixtures for folio-insights Phase 0 bench tests.

  Scope: Gate 1-5 measurement harness. Fixtures are session-scoped where
  1M-triple load is expensive; per-test fixtures for determinism helpers.

  Pitfall 7 discipline: single `random.Random(seed)` instance; never
  module-level `random.choice()`; never `numpy.random`.
  """
  from __future__ import annotations

  import random
  from pathlib import Path

  import pytest

  FIXTURES = Path(__file__).resolve().parents[2] / "fixtures"
  BENCH_CORPUS = FIXTURES / "bench.nq"


  @pytest.fixture(scope="session")
  def bench_1m_corpus() -> Path:
      """D-14 pytest fixture — deterministic 1M-triple corpus (committed artifact).

      Generated via `folio-insights bench gen --seed 42 --target 1000000` (Plan 00-02).
      If missing, skip the test with actionable instructions.
      """
      if not BENCH_CORPUS.exists():
          pytest.skip(
              f"Missing {BENCH_CORPUS}. Run: "
              "uv run folio-insights bench gen --seed 42 --target 1000000 "
              "--out fixtures/bench.nq"
          )
      return BENCH_CORPUS


  @pytest.fixture(scope="session")
  def bench_store(bench_1m_corpus: Path):
      """pyoxigraph.Store pre-loaded + optimized (Gate 2 tuning steps 1-2 from
      RESEARCH.md Gate-Tuning Playbook Gate 2).

      session-scoped: bulk_load at 1M is expensive; reuse across all Gate 2
      assertions in a single pytest run.
      """
      from pyoxigraph import RdfFormat, Store

      store = Store(path=None)  # in-memory for tests (RocksDB temp for Phase 13)
      with open(bench_1m_corpus, "rb") as f:
          store.bulk_load(f, format=RdfFormat.N_QUADS)
      store.optimize()  # RocksDB compaction + index merge
      return store


  @pytest.fixture
  def seeded_rng() -> random.Random:
      """Per-test seeded RNG (D-15 determinism per Pitfall 7).

      Always an explicit Random(seed) instance. Never use module-level
      random.choice() or numpy.random in tests or the generator.
      """
      return random.Random(42)
  ```

  **Step 3 — Create fixture directories tracked by git:**
  ```bash
  mkdir -p fixtures/bench fixtures/gold_queries/adversarial
  touch fixtures/bench/.gitkeep fixtures/gold_queries/.gitkeep fixtures/gold_queries/adversarial/.gitkeep
  ```

  **Step 4 — Run pytest collection** to prove the scaffold works:
  ```bash
  uv run pytest tests/bench/ --collect-only -q
  ```
  Expect: exits 0; lists `conftest.py` fixtures as discoverable (no test files yet is fine).
  </action>
  <acceptance_criteria>
    - `test -f tests/bench/__init__.py` AND `test -f tests/bench/conftest.py`
    - `test -f fixtures/bench/.gitkeep` AND `test -f fixtures/gold_queries/.gitkeep` AND `test -f fixtures/gold_queries/adversarial/.gitkeep`
    - `grep -c "from pyoxigraph import RdfFormat, Store" tests/bench/conftest.py` returns 1
    - `grep -c "random.Random(42)" tests/bench/conftest.py` returns 1
    - `grep -c "scope=\"session\"" tests/bench/conftest.py` returns 2  (bench_1m_corpus and bench_store)
    - `grep -c "pytest.skip" tests/bench/conftest.py` returns 1  (fixture-missing skip path)
    - `grep -c "numpy" tests/bench/conftest.py` returns 0  (Pitfall 7: never numpy.random)
    - `uv run pytest tests/bench/ --collect-only -q` exits 0
    - `git ls-files fixtures/` shows the three .gitkeep files
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/bench/conftest.py &amp;&amp; test -f fixtures/bench/.gitkeep &amp;&amp; test -f fixtures/gold_queries/adversarial/.gitkeep &amp;&amp; grep -q "random.Random(42)" tests/bench/conftest.py &amp;&amp; ! grep -q "numpy" tests/bench/conftest.py &amp;&amp; uv run pytest tests/bench/ --collect-only -q</automated>
  </verify>
  <done>tests/bench/ scaffold in place; fixtures expose bench_1m_corpus + bench_store + seeded_rng with session scope where needed; fixture directories tracked in git; pytest collects without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → local disk | Untrusted code from PyPI writes to venv; wheel hashes must match |
| git mv → repo | Untrusted nothing; local filesystem only |
| Dockerfile → image layers | Gate 5 mitigates digest drift (handled in Plan 00-04/00-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering (supply-chain) | pyoxigraph==0.5.7 wheel from PyPI | mitigate | Exact version pin (`==0.5.7`, not `>=`) prevents surprise upgrade; hash-pinned `requirements.lock` generated in Plan 00-05 (`uv pip compile --generate-hashes`) per SEC-01. Phase 0 accepts unhashed `uv sync` for Wave 1 setup; Plan 00-05 enforces `--require-hashes` at Dockerfile install. |
| T-00-02 | Tampering | owlready2==0.50 wheel | mitigate | Version pin; no post-install hooks in owlready2; bundled HermiT.jar is a known artifact hash (verified in Plan 00-04 Docker build) |
| T-00-03 | Information Disclosure | .env accidentally in Docker build context | mitigate | `.dockerignore` includes `.env` (Step 4) — never shipped in layers |
| T-00-04 | Information Disclosure | `.planning/` leaked into image | accept | `.dockerignore` excludes; no PII in `.planning/` anyway (design docs only) |
| T-00-05 | DoS | 3.13 Python regression breaks instructor at runtime | mitigate | `requires-python = ">=3.11,<3.13"` — fails fast at install, not at runtime |
</threat_model>

<verification>
Validation beyond per-task acceptance:
- `git log --oneline -1` shows the commit for this plan; `git diff HEAD~1 -- pyproject.toml` shows the three dep additions + requires-python tightening.
- `uv run python -c "import sys; assert sys.version_info &lt; (3, 13), sys.version"` passes.
- `docker build -f Dockerfile -t fi:wave1-smoke .` can still build using the existing single-stage Dockerfile (no regression on v1 Docker path; Plans 00-04/00-05 do the split).
</verification>

<success_criteria>
1. `PHILOSOPHY.md` exists at repo root; `2026-04-19_Philosophy.md` is gone; git blame preserved via `git mv`.
2. `pyproject.toml` declares pinned `pyoxigraph==0.5.7`, `oxrdflib`, `owlready2==0.50`; dev deps include `pytest>=9.0`, `pytest-benchmark>=5.1`, `dagger-io`.
3. `requires-python` is `>=3.11,<3.13` (not `>=3.11`).
4. `.dockerignore` excludes `.git`, `.planning/`, `output/`, `node_modules`, `__pycache__`, `.env`.
5. `tests/bench/` scaffold discoverable by pytest; `bench_1m_corpus`, `bench_store`, `seeded_rng` fixtures exposed.
6. `fixtures/bench/`, `fixtures/gold_queries/`, `fixtures/gold_queries/adversarial/` directories committed (via `.gitkeep`).
7. `uv sync --all-extras` exits 0; all new deps import successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundations-hard-gate/00-01-SUMMARY.md` documenting:
- Git sha of the rename commit (for blame-chain audit)
- Exact `uv.lock` delta (new packages pulled in transitively — particularly watch for `pyoxigraph`'s C extensions and any owlready2 JDK helper packages)
- Any deviation from the planned dep list with justification (e.g., `oxrdflib` version selected by resolver)
- Resolution of CONTEXT.md Open Question 1 (philosophy file path): document that the file lives at repo root, NOT `docs/`
</output>

<open_questions>
Resolved in this plan:
- **OQ 1 (CONTEXT.md philosophy path mismatch):** Confirmed at `/home/damienriehl/Coding Projects/folio-insights/2026-04-19_Philosophy.md`. D-18 remains correct in intent; `docs/` prefix in CONTEXT.md was a typo.

Deferred to later plans:
- **OQ 2 (1M-triple P95 baseline):** Plan 00-06 measures and budgets ≥1 tune-loop iteration per D-05.
- **OQ 3 (Railway Node 22 cold-page baseline):** Plan 00-07 measures before tuning.
- **OQ 4 (Fuseki pivot trigger numeric threshold):** Plan 00-08 proposes numeric (D-06 is qualitative).
- **OQ 5 (Adversarial SPARQL set composition):** Plan 00-03 defines the 5-query adversarial set from PRD §16/§21.
</open_questions>
