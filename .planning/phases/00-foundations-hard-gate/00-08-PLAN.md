---
phase: 0
plan: 8
plan_number: 8
plan_name: "decision-md-synthesis-and-fuseki-pivot-scaffold"
type: execute
wave: 5
depends_on: [3, 5, 6, 7]
files_modified:
  - .planning/phases/00-foundations-hard-gate/00-DECISION.md
  - fuseki/config.ttl
  - fuseki/README.md
  - fuseki/.gitkeep
  - .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md
autonomous: false
requirements: [SEC-01, SEC-02]
requirements_addressed: [SEC-01, SEC-02]
user_setup: []
must_haves:
  truths:
    - "`.planning/phases/00-foundations-hard-gate/00-DECISION.md` exists and declares ONE of two verdicts: `keep=pyoxigraph` OR `pivot=fuseki` (D-17)"
    - "DECISION.md records measured values for all 5 gates (Gate 1 binary, Gates 2-5 with numbers) — no N/A rows"
    - "DECISION.md tuning-passes section lists every pass run for Gates 2/3/4 with before/after numbers (sourced from 00-06-TUNING-LOG.md and 00-07-MEASUREMENTS.md)"
    - "DECISION.md downstream-phase branch guidance table covers Phases 6, 10, 11, 13, 16 (the 5 phases whose plans must branch on the verdict)"
    - "DECISION.md explicitly defers DID signature to Phase 6+ (D-17 chicken-and-egg) with a signed-after-Phase-6 backfill procedure"
    - "If verdict = pivot, `fuseki/config.ttl` exists and `docker run` invocation is documented"
    - "If verdict = keep, `fuseki/` directory still exists with README stating 'scaffold not required; kept for v2.1 re-evaluation'"
    - "Checkpoint: user approves verdict and signs off on downstream branch-guidance implications before the plan commits"
  artifacts:
    - path: ".planning/phases/00-foundations-hard-gate/00-DECISION.md"
      provides: "Binding Phase 0 verdict per D-17 schema (binary verdict + per-gate table + tuning log + branch guidance + signature slot)"
      min_lines: 120
      contains: "Verdict:"
    - path: ".planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md"
      provides: "Standalone downstream-phase guidance doc that Phases 6/10/11/13/16 planners MUST read before writing their plans"
      contains: "Phase 6"
    - path: "fuseki/config.ttl"
      provides: "Fuseki 5.x TDB2 assembler scaffold (only materialized on pivot; committed empty-with-README on keep)"
      exports: []
    - path: "fuseki/README.md"
      provides: "Docker-run invocation for local Fuseki + Phase 0 pivot procedure"
      contains: "docker run"
  key_links:
    - from: ".planning/phases/00-foundations-hard-gate/00-DECISION.md"
      to: "00-03-SUMMARY.md, 00-05-SUMMARY.md, 00-06-TUNING-LOG.md, 00-07-MEASUREMENTS.md"
      via: "DECISION.md verbatim-quotes the gate measurements from prior SUMMARYs/logs"
      pattern: "Source:.*SUMMARY"
    - from: ".planning/phases/00-foundations-hard-gate/00-DECISION.md"
      to: "ROADMAP.md Phases 6/10/11/13/16"
      via: "Branch-guidance table tells each downstream planner what to adjust"
      pattern: "Phase (6|10|11|13|16)"
    - from: "fuseki/config.ttl"
      to: "fuseki/README.md"
      via: "README documents the docker run command that consumes config.ttl"
      pattern: "config.ttl"
---

<objective>
Synthesize all Gate 1-5 measurements into the binding Phase 0 verdict artifact per D-17, produce a standalone branch-guidance document downstream planners will read, and — conditionally on Gate 1 failure — materialize the Fuseki pivot scaffold so the pivot is executable without a second research pass.

Purpose: Phase 0's ONLY ship-critical output is the binary verdict `keep=pyoxigraph` vs `pivot=fuseki`. Every one of Phases 6/10/11/13/16 has a planned branch on this verdict. The DECISION.md artifact is the single source of truth those downstream planners will read to pick their branch. It also resolves CONTEXT.md Open Question 4 (numeric threshold proposal — D-06 is qualitative; this plan proposes a concrete number).

Output:
- `.planning/phases/00-foundations-hard-gate/00-DECISION.md` per D-17 schema (verdict + per-gate table + tuning passes + branch guidance + signature slot deferred to Phase 6)
- `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` — standalone doc for downstream planners (duplicates DECISION.md branch table but lives at predictable path for `@file` references from other phase plans)
- `fuseki/config.ttl` — Fuseki 5.x TDB2 scaffold (RESEARCH.md §Fuseki Pivot Scaffold lines 609-633), materialized ONLY on pivot verdict
- `fuseki/README.md` — docker-run invocation + procedure for triggering pivot
- Checkpoint: user confirms verdict + branch guidance before commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundations-hard-gate/00-CONTEXT.md
@.planning/phases/00-foundations-hard-gate/00-RESEARCH.md
@.planning/phases/00-foundations-hard-gate/00-PATTERNS.md
@.planning/phases/00-foundations-hard-gate/00-VALIDATION.md
@.planning/phases/00-foundations-hard-gate/00-03-SUMMARY.md
@.planning/phases/00-foundations-hard-gate/00-05-SUMMARY.md
@.planning/phases/00-foundations-hard-gate/00-06-SUMMARY.md
@.planning/phases/00-foundations-hard-gate/00-06-TUNING-LOG.md
@.planning/phases/00-foundations-hard-gate/00-07-SUMMARY.md
@.planning/phases/00-foundations-hard-gate/00-07-MEASUREMENTS.md

<interfaces>
<!-- D-17 DECISION.md schema (CONTEXT.md lines 52-54): -->
<!--   (a) binary verdict `keep=pyoxigraph` OR `pivot=fuseki` -->
<!--   (b) per-gate measurement table with actual values -->
<!--   (c) tuning passes performed and results -->
<!--   (d) downstream-phase branch guidance -->
<!--   (signature deferred to Phase 6+ — DID substrate doesn't exist yet) -->

<!-- Fuseki 5.x TDB2 pivot scaffold (from 00-RESEARCH.md §Fuseki Pivot Scaffold, lines 609-633): -->

Fuseki config.ttl skeleton (expected pattern per RESEARCH.md):
```turtle
@prefix :        <#> .
@prefix fuseki:  <http://jena.apache.org/fuseki#> .
@prefix rdf:     <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix ja:      <http://jena.hpl.hp.com/2005/11/Assembler#> .
@prefix tdb2:    <http://jena.apache.org/2016/tdb#> .

<#service_fi> rdf:type fuseki:Service ;
    fuseki:name              "fi" ;
    fuseki:serviceQuery      "query" , "sparql" ;
    fuseki:serviceReadGraphStore "get" ;
    fuseki:dataset           <#dataset_tdb2> .

<#dataset_tdb2> rdf:type tdb2:DatasetTDB2 ;
    tdb2:location            "/fuseki/databases/fi" ;
    ja:context [ ja:cxtName "arq:queryTimeout" ; ja:cxtValue "30000" ] .
```

Docker invocation (standard Fuseki 5.x image):
```bash
docker run --rm -p 3030:3030 \
  -v "$(pwd)/fuseki/config.ttl:/fuseki/config.ttl:ro" \
  -v "$(pwd)/fuseki/databases:/fuseki/databases" \
  -e FUSEKI_BASE=/fuseki \
  stain/jena-fuseki:5.1.0 \
  /jena-fuseki/fuseki-server --config=/fuseki/config.ttl
```

<!-- Downstream branch-guidance matrix (5 phases whose plans branch on verdict): -->

| Phase | Keep branch (pyoxigraph) | Pivot branch (fuseki) |
|-------|--------------------------|-----------------------|
| 6 (DID + Auth) | Sign DECISION.md retroactively once DID substrate ships; no storage changes | Same signature story; worker-tier reasoning reads from Fuseki HTTP endpoint instead of direct Store |
| 10 (Arq Worker + Redis) | Worker tier embeds pyoxigraph in-process (read-only snapshot) | Worker tier issues HTTP queries to Fuseki sidecar; Redis still for job queue |
| 11 (SHACL @ scale) | pyshacl over pyoxigraph.Store (direct Graph API) | pyshacl over Fuseki HTTP SPARQL endpoint OR switch to Jena SHACL (Java-side, worker-tier) |
| 13 (Storage + RocksDB) | pyoxigraph path-backed Store with RocksDB tuning (Pass 3 of Gate 2 tuning log) | Drop entire RocksDB branch; use Fuseki TDB2 (native Jena tuning instead) |
| 16 (Public SPARQL endpoint) | FastAPI wrapper over pyoxigraph Store with SPARQL-injection hardening | Direct Fuseki proxy with Fuseki-side ACLs + rate limiting; FastAPI as auth shim only |

<!-- CONTEXT.md Open Question 4 resolution: -->
<!-- D-05 says "≤800ms post-tune → accept with SLO adjustment; >800ms → pivot". -->
<!-- D-06/D-07 use qualitative language. This plan proposes these numeric thresholds for DECISION.md: -->
<!--   Gate 1: binary (pass/fail) — D-04 STRICT -->
<!--   Gate 2: <500 hard-pass / 500-800 accept-with-SLO / >800 pivot (D-05) -->
<!--   Gate 3: <500 hard-pass / 500-700 accept / >700 split-reasoner-to-microservice (D-06, NOT pivot) -->
<!--   Gate 4: <200 hard-pass / 200-400 accept-with-SLO / >400 deferred-hydration-per-surface (D-07, NOT full-adapter-swap) -->
<!--   Gate 5: binary (bit-identical digest or not) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthesize DECISION.md from all gate SUMMARYs + logs</name>
  <files>.planning/phases/00-foundations-hard-gate/00-DECISION.md</files>
  <read_first>
    - .planning/phases/00-foundations-hard-gate/00-CONTEXT.md D-04, D-05, D-06, D-07, D-08, D-17 (gate rules + artifact schema)
    - .planning/phases/00-foundations-hard-gate/00-RESEARCH.md §Fuseki Pivot Scaffold (lines 609-633)
    - .planning/phases/00-foundations-hard-gate/00-03-SUMMARY.md (Gate 1 result — binary pass/fail from 13 gold queries)
    - .planning/phases/00-foundations-hard-gate/00-05-SUMMARY.md (Gate 5 result — Mode 1 local-determinism digest + Mode 2 Railway digest if run)
    - .planning/phases/00-foundations-hard-gate/00-06-SUMMARY.md + 00-06-TUNING-LOG.md (Gate 2 warm P95 + cold P95 + all tuning passes performed)
    - .planning/phases/00-foundations-hard-gate/00-07-SUMMARY.md + 00-07-MEASUREMENTS.md (Gate 3 image size, Gate 4 per-surface P95, D-11 HermiT full-1M timing)
    - ROADMAP.md §Phase 0 (exit criteria verbatim) + §Phase 6/10/11/13/16 details (to verify branch-guidance correctness)
  </read_first>
  <action>
  **Step 1 — Read every upstream result file.** Use the Read tool on each `.md` in `<read_first>`. If any file is missing, STOP and surface the gap — DECISION.md cannot be written with incomplete evidence. Specifically:
  - `00-03-SUMMARY.md` MUST report per-query pass/fail for q01..q13 and all adversarial queries
  - `00-06-SUMMARY.md` + `00-06-TUNING-LOG.md` MUST report warm P95 per query + which tuning passes ran + before/after numbers
  - `00-07-MEASUREMENTS.md` MUST report Gate 3 (final image size + layer-by-layer table if over budget) + Gate 4 (per-surface P95 for 3 surfaces × 3 shard IDs) + D-11 (HermiT elapsed + Xmx, OR "deferred to verify session" if not run)
  - `00-05-SUMMARY.md` MUST report Gate 5 Mode 1 (local determinism: two back-to-back builds → identical digest?) and Mode 2 (Railway digest match if RAILWAY_TOKEN was available)

  **Step 2 — Compute the verdict.** Apply the decision rules in this exact order:

  ```
  if Gate 1 has ANY failure (per D-04 STRICT):
      verdict = "pivot=fuseki"
      verdict_driver = "Gate 1 (RDF-12 gold query failure)"
  elif Gate 2 warm P95 > 800ms (per D-05 hard ceiling, after tuning):
      verdict = "pivot=fuseki"
      verdict_driver = "Gate 2 (P95 SPARQL > 800ms post-tune)"
  elif Gate 5 Mode 1 local-determinism = FAIL:
      verdict = "pivot=fuseki"
      verdict_driver = "Gate 5 (Dagger reproducibility failure) — reproducibility is a first-class v2.0 invariant; a non-reproducible pyoxigraph path is worse than a reproducible Fuseki path"
  else:
      verdict = "keep=pyoxigraph"
      verdict_driver = "All gates pass OR degrade within accept-with-SLO bands"
  ```

  Gates 3 and 4 do NOT trigger pivot on their own (D-06 → split reasoner microservice; D-07 → per-surface deferred hydration). Record them as escape-hatch triggers in the branch-guidance table instead.

  **Step 3 — Write `.planning/phases/00-foundations-hard-gate/00-DECISION.md`** using this EXACT skeleton. Fill in bracketed placeholders from Step 1's reads. Preserve section order and header levels verbatim so downstream planners can grep/anchor predictably:

  ```markdown
  # Phase 0 DECISION — Foundations HARD GATE

  **Phase:** 00-foundations-hard-gate
  **Authored:** {YYYY-MM-DD from `date -u +%Y-%m-%d`}
  **Status:** BINDING (per D-17) — all downstream phases MUST branch on the Verdict below
  **Signature:** *Deferred to Phase 6+ (D-17 chicken-and-egg: DID substrate doesn't exist yet). Backfill procedure in §Signature Deferral.*

  ---

  ## Verdict

  **{keep=pyoxigraph | pivot=fuseki}**

  *Driver:* {verdict_driver from Step 2}

  {If keep:} The locked v2.0 stack validates against reality. pyoxigraph 0.5.7 + rdflib 7.6 bridge + owlready2/HermiT + SvelteKit 5 adapter-node + Dagger CI is the substrate for Phases 1-20.
  {If pivot:} The locked v2.0 stack fails HARD GATE {N}. Substitute Apache Jena Fuseki 5.x (TDB2) for pyoxigraph as the RDF store. All other picks (owlready2, adapter-node, Dagger) remain. Fuseki scaffold is in `fuseki/` (Task 2).

  ---

  ## Per-Gate Measurement Table

  | Gate | REQ | Target | Measured | Verdict |
  |------|-----|--------|----------|---------|
  | 1. RDF-12 annotation pattern | STORAGE-04 | 100% of 13 gold queries return expected rows (D-04 STRICT) | {X/13 passing; list failing query IDs if any; source: 00-03-SUMMARY.md} | {PASS \| FAIL (pivot)} |
  | 2. P95 SPARQL latency @ 1M triples (warm) | QUALITY-01 | <500ms hard / 500-800ms accept / >800ms pivot (D-05) | {P95 value in ms; source: 00-06-TUNING-LOG.md final pass row} | {PASS \| ACCEPT-WITH-SLO \| FAIL (pivot)} |
  | 3. Worker Docker image size | QUALITY-03 | <500MB hard / 500-700MB accept / >700MB split-reasoner (D-06) | {size in MB; source: 00-07-MEASUREMENTS.md §Gate 3} | {PASS \| ACCEPT \| ESCAPE-HATCH (split reasoner; NOT pivot)} |
  | 4. SSR cold page latency (P95, 3 surfaces × 3 IDs) | QUALITY-04 | <200ms hard / 200-400ms accept / >400ms deferred-hydration-per-surface (D-07) | {worst-case P95 in ms + which surface; source: 00-07-MEASUREMENTS.md §Gate 4} | {PASS \| ACCEPT-WITH-SLO \| ESCAPE-HATCH (deferred hydration; NOT pivot)} |
  | 5. Dagger reproducibility (bit-identical digest) | OBS-04 | Mode 1 local = PASS required; Mode 2 Railway = PASS when RAILWAY_TOKEN available (D-08) | Mode 1: {sha256 digest match? yes/no}; Mode 2: {yes/no/skipped}; source: 00-05-SUMMARY.md | {PASS \| FAIL (pivot)} |

  ### Cross-cut measurements (not gates, recorded for posterity)

  - **D-11 HermiT full 1M reasoning:** {elapsed_seconds s, Xmx={mb}MB, World=fresh | "deferred to verify session" if FOLIO_RUN_D11 was not set}. Source: 00-07-MEASUREMENTS.md §D-11.
  - **Gate 2 cold P95 (informational):** {value in ms if reported}. Source: 00-06-TUNING-LOG.md §Cold.

  ---

  ## Tuning Passes Performed

  *Chronological. Each row = one pass. Empty section if verdict = pivot and tuning was skipped past first failure.*

  ### Gate 2 (SPARQL P95)

  | Pass | Change | Before (P95 ms) | After (P95 ms) | Source |
  |------|--------|-----------------|----------------|--------|
  | 0 | Baseline (bulk_load + optimize, no hints) | — | {n} | TUNING-LOG Pass 0 |
  | 1 | Named-graph pruning via `named_graphs=[...]` per-query | {n} | {n} | TUNING-LOG Pass 1 |
  | 2 | SPARQL rewrites (FILTER-push, LIMIT-push, predicate-reorder) | {n} | {n} | TUNING-LOG Pass 2 |
  | 3 | RocksDB tuning (if Store was path-backed) | {n or N/A if in-memory} | {n or N/A} | TUNING-LOG Pass 3 |

  ### Gate 3 (Worker image size)

  | Pass | Change | Before (MB) | After (MB) | Source |
  |------|--------|-------------|------------|--------|
  | 0 | Baseline (python:3.11-slim + OpenJDK 17 full) | — | {n} | 00-07-MEASUREMENTS.md |
  | 1 | jlink custom JRE (`--add-modules java.base,java.logging,java.xml,java.naming,java.sql,jdk.crypto.ec --strip-debug --no-man-pages --no-header-files --compress=2`) | {n} | {n} | 00-07-MEASUREMENTS.md |
  | 2 | A1 musllinux spike (Alpine vs slim fallback) | {n} | {n or "spike confirmed slim"} | 00-07-MEASUREMENTS.md |

  ### Gate 4 (SSR P95)

  | Pass | Change | Before (ms, worst surface) | After (ms, worst surface) | Source |
  |------|--------|----------------------------|----------------------------|--------|
  | 0 | Baseline (adapter-node stream, warm cache) | — | {n} | 00-07-MEASUREMENTS.md |
  | 1 | `setHeaders` cache-control + unawaited streaming fetches | {n} | {n} | 00-07-MEASUREMENTS.md |
  | 2 | @polka/compression custom server (only if Pass 1 >400ms) | {n or N/A} | {n or N/A} | 00-07-MEASUREMENTS.md |

  ---

  ## Downstream Phase Branch Guidance

  *Every downstream phase listed here MUST read this table before writing its PLAN.*

  | Phase | Keep branch (pyoxigraph) | Pivot branch (fuseki) | Note |
  |-------|--------------------------|-----------------------|------|
  | 6 (DID + Auth) | Storage-agnostic; no changes on verdict | Storage-agnostic; no changes on verdict | DID middleware is HTTP-layer; indifferent to store |
  | 10 (Arq Worker + Redis) | Worker embeds pyoxigraph in-process (read-only snapshot path = shared volume) | Worker issues HTTP SPARQL to Fuseki sidecar; Redis still for job queue | On keep, must resolve snapshot-staleness vs lock-contention; on pivot, must add Fuseki-HTTP-retry policy |
  | 11 (SHACL @ scale) | pyshacl over pyoxigraph.Store direct Graph API (rdflib bridge per 00-03-SUMMARY.md) | pyshacl over Fuseki HTTP SPARQL endpoint, OR switch to native Jena SHACL (Java-side, worker-tier) | Keep branch inherits Pitfall 2 bridge drop guard; pivot branch inherits Jena SHACL profile |
  | 13 (Storage + RocksDB) | pyoxigraph path-backed Store with RocksDB tuning (Pass 3 of Gate 2 TUNING-LOG) | Drop RocksDB; use Fuseki TDB2 native tuning (Jena `tdb2:location` + `arq:queryTimeout`) | On pivot, Plan 13 scope shrinks significantly; Fuseki TDB2 is preconfigured for most workloads |
  | 16 (Public SPARQL) | FastAPI wrapper over pyoxigraph Store with SPARQL-injection hardening (SERVICE-block, query-timeout, prefix allow-list) | Direct Fuseki proxy with Fuseki-side ACLs + rate limiting; FastAPI becomes auth shim only | SEC-01 adjacency: pivot branch inherits Jena's SPARQL sandbox; keep branch must hand-roll equivalent |

  ### Escape-hatch triggers (NOT pivot; DO adjust downstream plans)

  | Trigger | Affects | Adjustment |
  |---------|---------|------------|
  | Gate 3 final size > 700MB post-tune (D-06) | Phase 10 | Split HermiT reasoner into a separate microservice (not a container layer in the main worker); Plan 10 must budget for this |
  | Gate 4 worst surface P95 > 400ms post-tune (D-07) | Phase 15 | Per-surface deferred-hydration fallback (SPA shell + client fetch); NOT a full `adapter-node → adapter-static` swap |
  | D-11 HermiT 1M reasoning > 2h OR OOM at 4GB Xmx | Phase 9.P7 | Stream TBox inferences incrementally (not whole-World reasoning); Plan 9.P7 must scope this |

  ---

  ## Open Question Resolutions

  - **OQ 1 (philosophy file path):** Resolved in 00-01-SUMMARY.md — file was at repo root (not `docs/`); `git mv` to `PHILOSOPHY.md` preserved blame.
  - **OQ 2 (1M P95 baseline):** Measured — see §Per-Gate Measurement Table row 2.
  - **OQ 3 (Railway Node 22 cold-page baseline):** Measured — see §Per-Gate Measurement Table row 4.
  - **OQ 4 (Fuseki pivot trigger numeric threshold):** **Resolved here.** The D-05 hard ceiling of 800ms stands; Gates 3/4 do NOT trigger pivot (they trigger escape-hatches per D-06/D-07). Therefore pivot requires ONE of: (a) Gate 1 failure (D-04 STRICT), (b) Gate 2 warm P95 > 800ms post-tune, (c) Gate 5 Mode 1 local-determinism failure. This is the canonical numeric policy.
  - **OQ 5 (adversarial SPARQL set composition):** Resolved in 00-03-SUMMARY.md — 5 queries per PRD §16/§21 (deep_graph_traversal, large_construct, service_blocked `evil.example`, service_ssrf `169.254.169.254`, supersession_as_of).

  ---

  ## Signature Deferral (per D-17)

  The DID substrate this artifact would be signed against is delivered in Phase 6. Until Phase 6 ships, this DECISION.md is authenticated only by:
  - Git commit sha of the commit that introduces this file
  - Git signature (if the author has a configured GPG key)

  **Backfill procedure (to be executed in Phase 6+):**
  1. Phase 6 ships the `did:folio:...` DID method and signing tooling.
  2. Phase 6 planner adds a post-ship task: "Sign Phase 0 DECISION.md with the project's minted DID."
  3. The signature (detached JWS or W3C VC proof) is appended below this section and committed as an addendum; the DECISION.md body is NOT re-written.
  4. If any gate measurement is later invalidated (e.g., corpus grows), a new DECISION.md is authored (never edited in-place); this one is marked SUPERSEDED-BY.

  ```
  # Signature slot (empty until Phase 6 backfill)
  # DID: <did:folio:...>
  # Proof: <JWS or VC proof appended here>
  # Signed-at: <ISO-8601 UTC>
  ```

  ---

  ## Provenance

  | Source | File | Section |
  |--------|------|---------|
  | Gate 1 | 00-03-SUMMARY.md | All |
  | Gate 2 | 00-06-SUMMARY.md + 00-06-TUNING-LOG.md | All passes |
  | Gate 3 | 00-07-SUMMARY.md + 00-07-MEASUREMENTS.md §Gate 3 | Image-size table |
  | Gate 4 | 00-07-SUMMARY.md + 00-07-MEASUREMENTS.md §Gate 4 | Per-surface P95 |
  | Gate 5 | 00-05-SUMMARY.md | Mode 1 + Mode 2 digest match |
  | D-11 | 00-07-MEASUREMENTS.md §D-11 | HermiT elapsed + Xmx |
  | Decision rules | 00-CONTEXT.md §Decisions D-04 through D-08, D-17 | Verbatim |

  ---

  *DECISION.md authored 2026-04-22 per D-17; signature deferred to Phase 6+; see §Signature Deferral for backfill procedure.*
  ```

  **Step 4 — Self-audit checklist** (run mentally before Task 3 checkpoint):
  - [ ] No `{placeholder}` markers remain in the written file
  - [ ] Verdict section declares exactly ONE of `keep=pyoxigraph` or `pivot=fuseki` (not both, not `tbd`)
  - [ ] Per-gate table has 5 rows (Gates 1-5), no "N/A" in Measured column — every cell has a real value
  - [ ] Tuning Passes section has at least Pass 0 (baseline) for Gates 2/3/4 even if no further passes ran
  - [ ] Branch-guidance table covers Phases 6, 10, 11, 13, 16 (exactly these 5)
  - [ ] Escape-hatch table distinguishes "NOT pivot" triggers (Gates 3/4, D-11) from pivot triggers
  - [ ] OQ 4 resolution paragraph states numeric pivot policy explicitly
  - [ ] Signature Deferral section exists with the 4-step backfill procedure
  - [ ] Provenance table cites every upstream SUMMARY/LOG file
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/00-foundations-hard-gate/00-DECISION.md` exits 0
    - `grep -cE '^## Verdict' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns 1
    - `grep -cE '^\*\*(keep=pyoxigraph|pivot=fuseki)\*\*' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns 1 (exactly one verdict, not both)
    - `grep -cE '^## Per-Gate Measurement Table' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns 1
    - `grep -cE '^\| [1-5]\.' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns at least 5 (five gate rows)
    - `grep -cE 'Phase (6|10|11|13|16)' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns at least 5 (each phase cited in branch guidance)
    - `grep -c 'Signature Deferral' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns 1
    - `grep -c 'OQ 4' .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns 1
    - `! grep -E '\{[a-z_]+\}' .planning/phases/00-foundations-hard-gate/00-DECISION.md` (no unfilled placeholder markers — exits 0 if none found)
    - `wc -l .planning/phases/00-foundations-hard-gate/00-DECISION.md` returns >= 120 lines
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE '^\*\*(keep=pyoxigraph|pivot=fuseki)\*\*' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE 'Phase 6' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE 'Phase 10' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE 'Phase 11' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE 'Phase 13' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -qE 'Phase 16' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; grep -q 'OQ 4' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; ! grep -qE '\{[a-z_]+\}' .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; [ "$(wc -l &lt; .planning/phases/00-foundations-hard-gate/00-DECISION.md)" -ge 120 ]</automated>
  </verify>
  <done>DECISION.md exists with a single binary verdict, all 5 gate rows populated with measured values, tuning-passes section references every upstream pass, downstream branch-guidance table covers Phases 6/10/11/13/16, OQ 4 is resolved with numeric policy, Signature Deferral section documents Phase 6+ backfill procedure, and no unfilled `{placeholder}` markers remain.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize fuseki/ scaffold + BRANCH-GUIDANCE.md (conditional behavior based on verdict)</name>
  <files>fuseki/config.ttl, fuseki/README.md, fuseki/.gitkeep, .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md</files>
  <read_first>
    - .planning/phases/00-foundations-hard-gate/00-DECISION.md (written in Task 1 — read the Verdict section to branch behavior)
    - .planning/phases/00-foundations-hard-gate/00-RESEARCH.md §Fuseki Pivot Scaffold (lines 609-633)
    - 00-CONTEXT.md D-17 (DECISION.md schema — BRANCH-GUIDANCE.md duplicates the branch table for downstream-planner `@file` use)
  </read_first>
  <action>
  **Step 1 — Read the verdict from DECISION.md** (written in Task 1):
  ```bash
  grep -oE '\*\*(keep=pyoxigraph|pivot=fuseki)\*\*' .planning/phases/00-foundations-hard-gate/00-DECISION.md | head -1
  ```
  Parse to `VERDICT` = `keep` or `pivot`. If grep returns nothing, Task 1 is broken — STOP and re-run Task 1.

  **Step 2 — Create `fuseki/` directory with ALWAYS-present files** (both verdicts):

  2a. `fuseki/.gitkeep` — empty file so the dir is tracked even on keep verdict.

  2b. `fuseki/README.md` verbatim (unchanged regardless of verdict; content differs only in the status banner):
  ```markdown
  # fuseki/ — Apache Jena Fuseki pivot scaffold

  **Status (Phase 0 DECISION):** {keep=pyoxigraph (scaffold not required; retained for v2.1 re-evaluation) | pivot=fuseki (ACTIVE — Phase 0 triggered pivot)}

  *See `.planning/phases/00-foundations-hard-gate/00-DECISION.md` §Verdict for context.*

  ## Purpose

  This directory contains the Apache Jena Fuseki 5.x TDB2 scaffold specified as the Phase 0 pre-committed pivot target. If Phase 0 Gate 1 failed (RDF-12 annotation pattern did not work on pyoxigraph 0.5.7), this scaffold becomes the production RDF substrate.

  ## Local Fuseki run (for developers exploring the pivot branch)

  ```bash
  mkdir -p fuseki/databases
  docker run --rm -p 3030:3030 \
    -v "$(pwd)/fuseki/config.ttl:/fuseki/config.ttl:ro" \
    -v "$(pwd)/fuseki/databases:/fuseki/databases" \
    -e FUSEKI_BASE=/fuseki \
    stain/jena-fuseki:5.1.0 \
    /jena-fuseki/fuseki-server --config=/fuseki/config.ttl
  ```

  Fuseki UI + SPARQL endpoint: `http://localhost:3030/fi/sparql`.

  ## Config

  `config.ttl` defines one service (`/fi`) backed by one TDB2 dataset at `/fuseki/databases/fi`. Query timeout is 30s (matches Phase 0 gate-2 test harness).

  ## What must change downstream on pivot

  See `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` for the full table. TL;DR:
  - Phase 10 worker issues HTTP SPARQL to Fuseki sidecar (not in-process pyoxigraph)
  - Phase 11 SHACL uses Jena native SHACL OR pyshacl over HTTP
  - Phase 13 drops RocksDB entirely; tunes Fuseki TDB2 instead
  - Phase 16 public endpoint is a thin auth proxy to Fuseki

  ## v2.1 re-evaluation (keep verdict only)

  If Phase 0 verdict is `keep=pyoxigraph`, this scaffold is NOT removed. It is retained so that a v2.1 re-evaluation (e.g., triple count growing past 10M, or RDF-12 spec changes) can re-test Fuseki without re-researching the scaffold.
  ```

  2c. `fuseki/config.ttl` — Fuseki 5.x TDB2 assembler config verbatim:
  ```turtle
  @prefix :        <#> .
  @prefix fuseki:  <http://jena.apache.org/fuseki#> .
  @prefix rdf:     <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
  @prefix ja:      <http://jena.hpl.hp.com/2005/11/Assembler#> .
  @prefix tdb2:    <http://jena.apache.org/2016/tdb#> .

  <#service_fi> rdf:type fuseki:Service ;
      fuseki:name                  "fi" ;
      fuseki:serviceQuery          "query" , "sparql" ;
      fuseki:serviceReadGraphStore "get" ;
      fuseki:dataset               <#dataset_tdb2> .

  <#dataset_tdb2> rdf:type tdb2:DatasetTDB2 ;
      tdb2:location                "/fuseki/databases/fi" ;
      ja:context [ ja:cxtName  "arq:queryTimeout" ; ja:cxtValue "30000" ] .
  ```

  The `config.ttl` file is materialized regardless of verdict. On `keep`, it is dead config-as-code; on `pivot`, it is the production scaffold.

  **Step 3 — Substitute the README status banner** based on `VERDICT` parsed in Step 1:
  - If `VERDICT` = `keep`: write `**Status (Phase 0 DECISION):** keep=pyoxigraph (scaffold not required; retained for v2.1 re-evaluation)`
  - If `VERDICT` = `pivot`: write `**Status (Phase 0 DECISION):** pivot=fuseki (ACTIVE — Phase 0 triggered pivot)`

  **Step 4 — Write `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md`** — this is the standalone doc downstream planners will `@file`-reference. It verbatim-duplicates the DECISION.md branch-guidance tables at a predictable path (so downstream plans can anchor without re-parsing DECISION.md).

  ```markdown
  # Phase 0 Branch Guidance for Downstream Planners

  **Source of truth:** `.planning/phases/00-foundations-hard-gate/00-DECISION.md` §Downstream Phase Branch Guidance.

  This file duplicates that table at a stable, predictable path so downstream PLAN.md files can `@`-reference it without re-parsing DECISION.md.

  ## Verdict

  **{keep=pyoxigraph | pivot=fuseki}** (sourced from DECISION.md; if DECISION.md updates, update this file)

  ## Branch table

  | Phase | Keep branch (pyoxigraph) | Pivot branch (fuseki) | Note |
  |-------|--------------------------|-----------------------|------|
  | 6 (DID + Auth) | Storage-agnostic; no changes on verdict | Storage-agnostic; no changes on verdict | DID middleware is HTTP-layer; indifferent to store |
  | 10 (Arq Worker + Redis) | Worker embeds pyoxigraph in-process | Worker issues HTTP SPARQL to Fuseki sidecar; Redis still for job queue | On keep: resolve snapshot-staleness vs lock-contention. On pivot: add Fuseki-HTTP retry policy |
  | 11 (SHACL @ scale) | pyshacl over pyoxigraph.Store direct Graph API (rdflib bridge per 00-03-SUMMARY.md; inherits Pitfall 2 drop guard) | pyshacl over Fuseki HTTP SPARQL endpoint, OR switch to native Jena SHACL (Java-side, worker-tier) | Keep branch reuses bridge guard; pivot inherits Jena SHACL profile |
  | 13 (Storage + RocksDB) | pyoxigraph path-backed Store with RocksDB tuning | Drop RocksDB; Fuseki TDB2 native tuning | On pivot, Plan 13 scope shrinks significantly |
  | 16 (Public SPARQL) | FastAPI wrapper over pyoxigraph Store with SPARQL-injection hardening | Fuseki proxy with Fuseki-side ACLs + rate limits | SEC-01 adjacency: pivot inherits Jena's SPARQL sandbox |

  ## Escape hatches (NOT pivot — apply to keep verdict only)

  | Trigger | Affects | Adjustment |
  |---------|---------|------------|
  | Gate 3 final size > 700MB post-tune (D-06) | Phase 10 | Split HermiT reasoner into separate microservice |
  | Gate 4 worst surface P95 > 400ms post-tune (D-07) | Phase 15 | Per-surface deferred-hydration fallback (SPA shell + client fetch) |
  | D-11 HermiT 1M > 2h OR OOM at 4GB Xmx | Phase 9.P7 | Stream TBox inferences incrementally; not whole-World reasoning |

  ## How to use this file

  Downstream PLAN.md files add one line to `<context>`:

  ```
  @.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md
  ```

  Then branch frontmatter/tasks on `Verdict`.
  ```

  Substitute `Verdict` header the same way as the README banner in Step 3.
  </action>
  <acceptance_criteria>
    - `test -f fuseki/config.ttl` AND `test -f fuseki/README.md` AND `test -f fuseki/.gitkeep`
    - `grep -q 'tdb2:DatasetTDB2' fuseki/config.ttl`
    - `grep -q 'fuseki:Service' fuseki/config.ttl`
    - `grep -q 'arq:queryTimeout' fuseki/config.ttl`
    - `grep -q 'stain/jena-fuseki:5.1.0' fuseki/README.md`
    - `grep -qE 'Status \(Phase 0 DECISION\):\*\* (keep=pyoxigraph|pivot=fuseki)' fuseki/README.md` (banner populated, no placeholder `{...}`)
    - `test -f .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md`
    - `grep -c 'Phase 10' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` returns at least 1
    - `grep -c 'Phase 16' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` returns at least 1
    - `grep -qE '\*\*(keep=pyoxigraph|pivot=fuseki)\*\*' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` (verdict populated, no placeholder)
    - `! grep -E '\{[a-z_]+\}' fuseki/README.md` (no unfilled placeholders)
    - `! grep -E '\{[a-z_]+\}' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f fuseki/config.ttl &amp;&amp; test -f fuseki/README.md &amp;&amp; test -f .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md &amp;&amp; grep -q 'tdb2:DatasetTDB2' fuseki/config.ttl &amp;&amp; grep -qE 'Status \(Phase 0 DECISION\):\*\* (keep=pyoxigraph|pivot=fuseki)' fuseki/README.md &amp;&amp; grep -qE '\*\*(keep=pyoxigraph|pivot=fuseki)\*\*' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md &amp;&amp; ! grep -qE '\{[a-z_]+\}' fuseki/README.md &amp;&amp; ! grep -qE '\{[a-z_]+\}' .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md</automated>
  </verify>
  <done>fuseki/ scaffold exists with valid Fuseki 5.x TDB2 config.ttl + docker-run README (banner reflects actual verdict); BRANCH-GUIDANCE.md duplicates the DECISION.md branch table at a stable path; both files have placeholder substitution completed (no `{...}` markers).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — User approves DECISION.md verdict + branch guidance</name>
  <files>.planning/phases/00-foundations-hard-gate/00-DECISION.md, .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md, fuseki/config.ttl, fuseki/README.md</files>
  <action>Present the artifacts from Tasks 1 and 2 to the user. Do NOT commit until approval. On approval, proceed to the commit step documented in <output>. On rejection, re-run Task 1 and/or Task 2 with the user's correction instructions. See &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; below for the checkpoint protocol.</action>
  <verify><automated>test -f .planning/phases/00-foundations-hard-gate/00-DECISION.md &amp;&amp; test -f .planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md &amp;&amp; test -f fuseki/config.ttl &amp;&amp; test -f fuseki/README.md</automated></verify>
  <what-built>
    Tasks 1 and 2 produced:
    - `.planning/phases/00-foundations-hard-gate/00-DECISION.md` — the binding Phase 0 verdict per D-17, synthesized from all 5 gate SUMMARYs and tuning logs.
    - `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` — standalone branch-guidance doc for downstream-phase planners.
    - `fuseki/config.ttl` + `fuseki/README.md` — Fuseki 5.x TDB2 pivot scaffold (materialized on both verdicts; status banner reflects actual verdict).

    DECISION.md resolves OQ 4 (numeric pivot threshold policy) and defers DID signature to Phase 6+ with a documented backfill procedure.
  </what-built>
  <how-to-verify>
    1. **Read the verdict.** Open `.planning/phases/00-foundations-hard-gate/00-DECISION.md` §Verdict. Confirm it matches your expectation from the gate measurements.
       - Sanity check: does §Per-Gate Measurement Table contain real numbers for Gates 2-5, with the Verdict column using the right bucket (PASS / ACCEPT-WITH-SLO / FAIL / ESCAPE-HATCH)?
       - Sanity check: if verdict = pivot, is §Verdict "Driver" column citing the exact failing gate?

    2. **Spot-check provenance.** Pick one gate row (e.g., Gate 2). Open the cited source file (e.g., 00-06-TUNING-LOG.md final pass row). Confirm the number in DECISION.md matches the source.

    3. **Review the branch-guidance table.** DECISION.md §Downstream Phase Branch Guidance. For each of Phases 6/10/11/13/16: does the Keep/Pivot column describe a concrete, implementable change? Flag any row that's too vague to act on.

    4. **Review escape hatches.** §Escape-hatch triggers should list Gates 3, 4, and D-11 — NOT as pivots, but as adjustments to specific downstream phases. Confirm the trigger thresholds match D-06/D-07 verbatim.

    5. **Review OQ 4 resolution.** §Open Question Resolutions item 4 should state: pivot requires ONE of (a) Gate 1 fail, (b) Gate 2 warm P95 > 800ms, or (c) Gate 5 Mode 1 determinism fail. Gates 3/4 do NOT trigger pivot.

    6. **Review Signature Deferral.** §Signature Deferral should have the 4-step backfill procedure — NOT a blocker for Phase 0 shipping.

    7. **Inspect the Fuseki scaffold.** `fuseki/config.ttl` should parse as valid Turtle (optional sanity: `docker run --rm -v "$(pwd)/fuseki:/f" stain/jena-fuseki:5.1.0 riot --validate /f/config.ttl` — only if you want to go that deep). `fuseki/README.md` status banner should reflect the actual verdict.

    8. **Inspect BRANCH-GUIDANCE.md.** Should duplicate the DECISION.md branch table at `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md`. Downstream plans will `@`-reference this path.

    **If anything is wrong:** describe the issue — Task 1 or 2 will be re-run with corrections. Do NOT commit yet.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the commit, or describe issues for correction.</resume-signal>
  <done>User has verified and approved the DECISION.md verdict, branch guidance, Fuseki scaffold, OQ 4 resolution, and Signature Deferral procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream SUMMARYs → DECISION.md | DECISION.md verbatim-quotes numbers from 00-03/05/06/07 SUMMARYs; a tampered SUMMARY would propagate to the binding verdict |
| Git commit → DECISION.md authenticity | Until Phase 6 DID signing lands, authenticity is git-sha-only (commit signature if author has GPG; else bare commit) |
| fuseki/config.ttl → Fuseki process | On pivot, Fuseki reads this config; a tampered config could change the query-timeout or dataset location and affect Phase 16 security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-34 | Tampering | DECISION.md (pre-DID) | accept | Phase 0 accepts git-sha-only authenticity per D-17 chicken-and-egg. Backfill via DID sig in Phase 6+ (documented procedure in §Signature Deferral). Risk window: Phase 0-5. Mitigation of the window: DECISION.md is committed immediately after Task 3 approval; any edit AFTER the commit requires a new DECISION.md with SUPERSEDED-BY marker (not in-place edit) per §Signature Deferral point 4. |
| T-00-35 | Repudiation | DECISION.md authorship dispute | mitigate | Git author on the DECISION.md commit becomes the signatory until Phase 6 DID backfill. Commit message must include "authored by <name> on behalf of Phase 0 synthesis" and NOT squash-merge (preserve full commit chain). |
| T-00-36 | Information Disclosure | DECISION.md may leak pyoxigraph version-specific perf numbers to adversaries targeting SPARQL injection | accept | SEC-01 is a Phase 16 hardening concern. DECISION.md numbers are already public (this is an open-source project); leaking them now vs Phase 16 makes no difference. Phase 16 must not rely on security-by-obscurity. |
| T-00-37 | Information Disclosure | Branch-guidance leaks internal architecture | accept | Architecture is public (ROADMAP.md already lists phase dependencies). No new disclosure. |
| T-00-38 | Denial of Service | fuseki/config.ttl could set `arq:queryTimeout` too high, enabling DoS on the pivot branch | mitigate | Config sets `arq:queryTimeout` = 30000ms (30s) per RESEARCH.md §Fuseki Pivot Scaffold. If Phase 16 hardening needs a shorter window, Phase 16 can override via runtime params without editing config.ttl. |
| T-00-39 | Elevation of Privilege | Signature Deferral backfill could be gamed to sign a DIFFERENT DECISION.md in Phase 6+ (substitute-then-sign attack) | mitigate | §Signature Deferral point 4: ANY change to gate measurements post-commit requires a NEW DECISION.md with SUPERSEDED-BY marker (not in-place edit). Phase 6 DID-signing task MUST verify the DECISION.md body matches the git-sha'd commit before signing. |
| T-00-40 | Tampering (pivot scaffold) | fuseki/config.ttl edited post-Phase-0 to weaken security posture (e.g., remove query timeout) | mitigate | Phase 16 planner MUST include a config-as-code diff review against this file as a Phase 16 gate. Noted in BRANCH-GUIDANCE.md Phase 16 row SEC-01 adjacency language. |
</threat_model>

<verification>
Validation beyond per-task acceptance:

- DECISION.md verdict is reachable from the actual measurements (no hand-waving): every number in §Per-Gate Measurement Table has a `Source:` pointer that grep can resolve to a file on disk.
- DECISION.md §Downstream Phase Branch Guidance contents exactly match BRANCH-GUIDANCE.md branch table (both are committed from the same data; drift between them is a bug).
- `fuseki/config.ttl` parses as valid Turtle:
  ```bash
  uv run python -c "from rdflib import Graph; Graph().parse('fuseki/config.ttl', format='turtle'); print('ok')"
  ```
  Must print `ok`.
- Phase 0 is now committable as complete: DECISION.md is the last artifact, and it binds downstream phases.
- No downstream PLAN.md references this file until the commit lands. After the commit, Phases 1-20 planners can `@` the path.
</verification>

<success_criteria>
1. `.planning/phases/00-foundations-hard-gate/00-DECISION.md` exists with a single binary verdict (`keep=pyoxigraph` OR `pivot=fuseki`), populated per-gate measurement table for all 5 gates, tuning-passes section, downstream branch-guidance table covering Phases 6/10/11/13/16, OQ 4 resolved with numeric policy, Signature Deferral procedure, and a Provenance table citing every upstream SUMMARY/LOG.
2. `.planning/phases/00-foundations-hard-gate/00-BRANCH-GUIDANCE.md` exists at the predictable path downstream planners will `@`-reference, verbatim-duplicating the DECISION.md branch table.
3. `fuseki/config.ttl` exists with valid Fuseki 5.x TDB2 assembler config (parses as Turtle); `fuseki/README.md` documents the `docker run` invocation and the status banner reflects the actual verdict.
4. User has approved the verdict, branch guidance, and OQ 4 resolution via the Task 3 checkpoint.
5. No unfilled `{placeholder}` markers in any of the 3 output files.
6. Phase 0 is declared DONE — downstream phases can now plan against a binding substrate verdict.
</success_criteria>

<output>
After Task 3 approval, create `.planning/phases/00-foundations-hard-gate/00-08-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. MUST record:
- (a) Final verdict (keep=pyoxigraph OR pivot=fuseki) and its driver
- (b) Count of PASS / ACCEPT-WITH-SLO / ESCAPE-HATCH / FAIL rows in Per-Gate Measurement Table
- (c) Whether DID-signing procedure was invoked (NO in Phase 0; deferred to Phase 6+)
- (d) Whether fuseki/ scaffold status banner reads `keep` or `pivot`
- (e) Git sha of the DECISION.md commit (for authenticity chain until Phase 6 DID backfill)
- (f) Explicit "Phase 0 DONE" declaration — signals to ROADMAP.md updater that Phase 0 checkbox can flip
- (g) Resolution of CONTEXT.md OQ 4 (quote the numeric-policy paragraph verbatim)
- (h) List of downstream phases (6, 10, 11, 13, 16) whose planners MUST read DECISION.md + BRANCH-GUIDANCE.md before their first plan
</output>

<open_questions>
Resolved in this plan:
- **OQ 4 (Fuseki pivot trigger numeric threshold):** Resolved — pivot requires ONE of (a) Gate 1 STRICT fail (D-04), (b) Gate 2 warm P95 > 800ms post-tune (D-05 hard ceiling), (c) Gate 5 Mode 1 local-determinism fail. Gates 3/4 and D-11 do NOT trigger pivot (they trigger escape-hatches per D-06/D-07).

Deferred out of Phase 0:
- **DID signature on DECISION.md:** Deferred to Phase 6+ per D-17 chicken-and-egg. Backfill procedure is documented in DECISION.md §Signature Deferral.
- **Phase 15 overlap scope re-evaluation:** D-09 full nav SSR prototype (Plan 07) may pre-build Phase 15 deliverables. Phase 15 planner should check for redundancy when they write Phase 15's first plan.
- **Phase 17 overlap:** D-10 full Dagger CI (Plan 05) means Phase 17 inherits pipeline. Phase 17 planner scopes to test-consolidation only.
- **Phase 20 overlap:** D-10 Railway trigger + D-08 bit-identical digest solve most Phase 20 deploy mechanics. Phase 20 narrows to CalVer cut + announce.

Surfaced for downstream phases:
- **Phase 10 architecture branch on Gate 3 escape-hatch:** If Gate 3 final size > 700MB post-tune, Phase 10 must budget for splitting HermiT reasoner into a separate microservice.
- **Phase 15 deferred-hydration fallback on Gate 4 escape-hatch:** If Gate 4 worst surface P95 > 400ms post-tune, Phase 15 implements per-surface SPA shell (NOT full adapter swap).
- **Phase 9.P7 streaming-inference on D-11 escape-hatch:** If HermiT 1M > 2h OR OOM at 4GB, Phase 9.P7 streams TBox inferences incrementally.
</open_questions>
