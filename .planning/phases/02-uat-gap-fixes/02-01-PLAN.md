---
phase: 02-uat-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewer/vite.config.ts
autonomous: true
requirements:
  - I-3
gap_closure: true

must_haves:
  truths:
    - "Viewer dev server at :9926 proxies every /api/* request to the API at :9925"
    - "viewer/vite.config.ts contains no reference to port 8700"
    - "The proxy config carries an inline comment pointing at the feedback_api-client-proxy.md auto-memory rule so the port is not reintroduced"
  artifacts:
    - path: "viewer/vite.config.ts"
      provides: "Vite dev server config with /api proxy"
      contains: "http://localhost:9925"
  key_links:
    - from: "viewer (dev server :9926)"
      to: "API (uvicorn :9925)"
      via: "vite proxy /api -> http://localhost:9925"
      pattern: "target:\\s*'http://localhost:9925'"
---

<objective>
Fix UAT Issue I-3: Vite dev-mode proxy is hardcoded to `http://localhost:8700` but the API runs on 9925. Every `/api/*` call from the dev viewer returns 500, blocking 12 downstream UAT tests (24-28, 30, 33-36). This is a one-line fix in `viewer/vite.config.ts`.

Purpose: Unblock end-to-end viewer dev-mode testing so the follow-up UAT sweep can progress past "skipped" on those 12 tests.
Output: `viewer/vite.config.ts` with correct proxy target + an inline comment referencing the auto-memory rule so it is not reintroduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-uat-gap-fixes/02-CONTEXT.md
@.planning/phases/COMPREHENSIVE-UAT.md

@/home/damienriehl/.claude/projects/-home-damienriehl-Coding-Projects-folio-insights/memory/feedback_api-client-proxy.md

@viewer/vite.config.ts

<interfaces>
<!-- Current state of viewer/vite.config.ts (from Read tool at planning time) -->
```ts
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	plugins: [sveltekit()],
	server: {
		port: 5173,
		proxy: {
			'/api': {
				target: 'http://localhost:8700',  // WRONG — API is on 9925
				changeOrigin: true,
			},
		},
	},
});
```

Auto-memory rule (MUST comply): `feedback_api-client-proxy.md`
- Frontend API calls must use relative URLs routed through the Vite proxy
- Never hardcode localhost ports in **client code** (`viewer/src/lib/api/client.ts` — API_BASE must stay `''`)
- `viewer/vite.config.ts` is the one place the backend port is named; it is infrastructure, not client code

Bootup port assignments (from `.planning/STATE.md` and recent commits):
- API (uvicorn): `:9925`
- Viewer (vite dev server): `:9926`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change vite proxy target from 8700 to 9925 and add auto-memory-rule comment</name>
  <files>viewer/vite.config.ts</files>
  <read_first>
    - viewer/vite.config.ts (current 15-line file — confirm the exact `target: 'http://localhost:8700'` literal is still present before editing)
    - /home/damienriehl/.claude/projects/-home-damienriehl-Coding-Projects-folio-insights/memory/feedback_api-client-proxy.md (auto-memory rule being enforced)
    - .planning/phases/02-uat-gap-fixes/02-CONTEXT.md lines 26 and 100-103 (I-3 fix sketch + verification expectations)
  </read_first>
  <action>
    Replace the `target: 'http://localhost:8700',` line in `viewer/vite.config.ts` with:

    ```ts
    // API dev port — must match uvicorn port from bootup config (9925).
    // Per user auto-memory rule `feedback_api-client-proxy.md`: client code
    // (viewer/src/lib/api/client.ts) keeps API_BASE = '' and routes through
    // this proxy. Never hardcode a localhost port in client code — only
    // the proxy target names the backend port, and it lives here.
    target: 'http://localhost:9925',
    ```

    Keep the surrounding structure (`'/api': { ... changeOrigin: true }`) unchanged. Do NOT introduce `VITE_API_URL` or any env-var indirection in this phase — that was explicitly deferred in CONTEXT.md `<deferred>` ("Migrating vite config to env-var pattern app-wide — only change the proxy target here"). Hardcoded 9925 is the chosen fix per CONTEXT decision I-3.

    After editing, do NOT modify `viewer/src/lib/api/client.ts` — per the auto-memory rule, client code must keep `API_BASE = ''`.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-insights" && grep -c "localhost:8700" viewer/vite.config.ts | grep -qx 0 && grep -c "localhost:9925" viewer/vite.config.ts | awk '$1>=1{exit 0} {exit 1}' && grep -q "feedback_api-client-proxy.md" viewer/vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "localhost:8700" viewer/vite.config.ts` outputs `0`
    - `grep -c "localhost:9925" viewer/vite.config.ts` outputs `>= 1`
    - `grep -q "feedback_api-client-proxy.md" viewer/vite.config.ts` exits 0 (comment references auto-memory rule by filename)
    - `viewer/src/lib/api/client.ts` is NOT modified by this task: `git diff --name-only` must NOT include `viewer/src/lib/api/client.ts`
    - `viewer/vite.config.ts` still parses as valid TypeScript (no syntax errors): `cd viewer && npx tsc --noEmit --project tsconfig.json` exits 0 OR `node -e "require('fs').readFileSync('viewer/vite.config.ts','utf8')"` succeeds (sanity file read)
  </acceptance_criteria>
  <done>
    `viewer/vite.config.ts` targets `http://localhost:9925`, contains zero references to `:8700`, and includes an inline comment citing `feedback_api-client-proxy.md`. The change is confined to that single file.
  </done>
</task>

</tasks>

<verification>
- `grep -c "localhost:8700" viewer/vite.config.ts` -> `0`
- `grep "localhost:9925" viewer/vite.config.ts` -> at least one match
- `grep "feedback_api-client-proxy.md" viewer/vite.config.ts` -> at least one match
- `git diff --name-only` shows `viewer/vite.config.ts` as the only modified file
- pytest suite still green: `.venv/bin/pytest tests/ -x -q` -> 197 passed (no Python code changed, sanity check only)
</verification>

<success_criteria>
- Viewer dev server (if started) would proxy `/api/*` requests to `http://localhost:9925` — confirmed by file contents; runtime verification belongs to the follow-up UAT pass, not this plan.
- No change to `viewer/src/lib/api/client.ts` or any other file.
- Zero regressions: pytest stays at 197/197.
</success_criteria>

<output>
After completion, create `.planning/phases/02-uat-gap-fixes/02-01-SUMMARY.md` with:
- File changed (path + diff snippet)
- Before/after proxy target values
- Confirmation the auto-memory rule comment was added
- Note that runtime re-verification of UAT tests 24-28, 30, 33-36 belongs to the follow-up UAT pass (not this phase)
</output>
