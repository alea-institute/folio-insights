---
phase: 06-did-substrate-6-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/folio_insights/shards/envelope.py
  - src/folio_insights/revision/content_edit.py
  - src/folio_insights/shards/audit.py
  - tests/shards/conftest.py
  - tests/shards/test_envelope_roundtrip.py
  - tests/identity/__init__.py
  - tests/identity/conftest.py
  - tests/identity/test_canonical_jcs_properties.py
  - tests/identity/test_jcs_golden.py
  - tests/identity/fixtures/jcs_golden/README.md
autonomous: false
requirements: [DID-01, DID-02, DID-03]
must_haves:
  truths:
    - "The locked identity stack is added to deps at pinned versions and didkit/pydid/python-jose/fastapi-users are explicitly rejected in a comment [DID-01, RISK-4]"
    - "AttestedSignature is REPLACED (not extended) with extra=forbid + signing_key_id + did_doc_snapshot_at + verified, and reserves an empty cosigners[] for deferred 6.3 [D-13, D-01]"
    - "AttestedSignature.action is a Literal whose values are reconciled against the PRD §3.1 source of truth, and every existing construction site (sign_attestation, add_edit, test builders) stays green [D-13, DID-02]"
    - "canonical_content_hash swaps RFC-8785 jcs.canonicalize into the single hash line with NFC + canonical-RFC3339-UTC-datetime + explicit-None pre-normalization, keeping the exclusion set and call site identical [D-12, DID-03]"
    - "verify(sign(x)) / canonical_content_hash is order-independent across 1000 shuffled-field-order shards (exit criterion 2) [D-12, DID-03]"
    - "Our canonicalization matches the cyberphone/json-canonicalization RFC-8785 reference on ~50 golden inputs (the cross-impl discipline that closes Pitfall F4) [D-12]"
    - "shards/ still imports zero crypto/RDF/storage libs — AttestedSignature stays pure Pydantic (dep-leak guard green)"
  artifacts:
    - path: "src/folio_insights/shards/envelope.py"
      provides: "Real 6.1 AttestedSignature (extra=forbid, signing_key_id, did_doc_snapshot_at, verified, reserved cosigners[], action Literal)"
      contains: "signing_key_id"
    - path: "src/folio_insights/revision/content_edit.py"
      provides: "JCS (RFC 8785) canonical_content_hash with NFC + datetime + None pre-normalization"
      contains: "jcs"
    - path: "tests/identity/test_canonical_jcs_properties.py"
      provides: "1000-shuffled-order JCS order-independence property test (exit criterion 2)"
      contains: "def test_"
    - path: "tests/identity/test_jcs_golden.py"
      provides: "Cross-implementation golden test vs cyberphone reference vectors (Pitfall F4)"
      contains: "def test_"
  key_links:
    - from: "src/folio_insights/revision/content_edit.py"
      to: "RFC 8785 canonicalization"
      via: "jcs.canonicalize over NFC-normalized model_dump"
      pattern: "jcs"
    - from: "src/folio_insights/shards/envelope.py"
      to: "AttestedSignature reshape"
      via: "ConfigDict(extra='forbid') + new signing fields"
      pattern: "signing_key_id"
---

<objective>
Lay the crypto **schema + canonicalization foundation** every other Phase-6 plan
builds on, in one cohesive (operator-gated) change:

1. **Add the locked identity stack** to `pyproject.toml` at the STACK.md pins
   (`PyNaCl`, `jcs`, `atproto`, `dag-cbor`, `joserfc`, `authlib`) + an `identity`
   pytest marker, and record the explicit rejection of `didkit`/`pydid`/
   `python-jose`/`fastapi-users` (DID-01, RISK-4). **Needs operator approval —
   hence `autonomous: false`.**
2. **Reshape `AttestedSignature`** from the permissive Phase-5 stub to the real 6.1
   shape (D-13): `extra="forbid"`, add `signing_key_id` / `did_doc_snapshot_at` /
   `verified`, reserve an empty `cosigners[]` for deferred 6.3, and turn `action`
   into a Literal reconciled against the PRD §3.1 source of truth. This is the ONE
   breaking reshape — fix every construction site in the same change.
3. **Swap RFC-8785 JCS into `canonical_content_hash`** (D-12) with the
   pre-normalization recipe that closes BLOCKING Pitfall F4 (NFC, canonical
   datetime, explicit None), keeping the exclusion set and call site identical.
4. **Prove F4 is closed:** the 1000-shuffled-order property test (exit criterion 2)
   and the ~50-input cross-impl golden test vs the cyberphone reference.

Purpose: Plan 02 (signer/verifier/resolver) signs over this hash and constructs
this `AttestedSignature`; Plan 03 (binding + CLI + preview) exercises both. If the
canonical hash is non-deterministic (F4), no signature ever verifies — so this plan
is the gate.

Output: pinned deps, reshaped `envelope.py`, JCS `content_edit.py`, reconciled
`audit.py`, migrated `tests/shards/`, and the new `tests/identity/` JCS gate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-did-substrate-6-5/06-CONTEXT.md
@.planning/phases/06-did-substrate-6-5/06-RESEARCH.md
@.planning/phases/06-did-substrate-6-5/06-VALIDATION.md

<interfaces>
<!-- AttestedSignature TODAY (shards/envelope.py L49-66) — REPLACE, do not extend: -->
<!--   model_config = ConfigDict(extra="allow")   ->  extra="forbid"               -->
<!--   did: str=""  action: str=""  signed_at: datetime|None=None                  -->
<!--   signature: str=""  over_content_hash: str=""                                -->
<!-- ADD (D-13): signing_key_id: str  (DID URL + #fragment)                         -->
<!--             did_doc_snapshot_at: datetime | None = None                        -->
<!--             verified: bool | None = None  (cache annotation; None until verified)-->
<!--             cosigners: list[...] = Field(default_factory=list)  (RESERVE, empty)-->
<!--   action -> Literal[...]  reconciled from PRD §3.1 (see Task 2)                 -->

<!-- canonical_content_hash TODAY (revision/content_edit.py L172-201):              -->
<!--   payload = shard.model_dump(mode="json", exclude=_HASH_EXCLUDED_FIELDS)       -->
<!--   canon = json.dumps(payload, sort_keys=True, separators=(",",":"), ensure_ascii=False)-->
<!--   return sha256(canon.encode()).hexdigest()                                    -->
<!--   _HASH_EXCLUDED_FIELDS (L161) STAYS: transaction_time, valid_time_start/end,  -->
<!--   content_edits, signatures.  Function signature + call site UNCHANGED (D-12 seam).-->

<!-- Existing action="content_edit" construction sites that MUST stay green:        -->
<!--   revision/content_edit.py:215 (sign_attestation)                              -->
<!--   shards/audit.py:127 (add_edit inline AttestedSignature)                       -->

<!-- PRD §3.1 action source-of-truth (read these exact lines):                      -->
<!--   §3.1.2 L117: action="promote";  §3.1.1 L125: action="resolve_contest"        -->
<!--   §3.1 role table L103: reviewer "edit content on any shard" (content_edit is real)-->
<!--   REQUIREMENTS DID-02: extract, promote, demote, contest, supersede, retract,  -->
<!--   distinguo, role-assertion (8 governance actions; spellings differ from PRD §3.1)-->

<!-- jcs lib API: jcs.canonicalize(obj) -> bytes (sorts keys per RFC8785, minimal   -->
<!--   number encoding). Does NOT NFC-normalize strings or fix datetime format —    -->
<!--   the caller must do both BEFORE calling it.                                    -->

<!-- dep-leak guard: tests/shards/test_dep_leak_guard.py forbids rdflib/pyoxigraph/ -->
<!--   oxrdflib/owlready2 (and storage libs) UNDER shards/. AttestedSignature stays  -->
<!--   pure Pydantic — no crypto import in envelope.py. jcs import goes in revision/.-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the locked identity stack to pyproject.toml (OPERATOR APPROVAL REQUIRED)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current dependencies L6-27 — cryptography>=41, base58>=2.1 already present; markers block)
    - .planning/research/STACK.md L42-68 (the §6.5 DID stack table with exact pins + the rejection list L53-56, L279-283)
    - .planning/phases/06-did-substrate-6-5/06-RESEARCH.md §1 (dependency table + sign-off note)
  </read_first>
  <behavior>
    - `uv sync` (or `uv lock`) resolves with the new deps present and no version conflict.
    - `uv run python -c "import nacl, jcs, atproto, dag_cbor, joserfc, authlib"` exits 0.
    - `pyproject.toml` carries a comment block explicitly rejecting didkit/pydid/python-jose/fastapi-users (DID-01 acceptance criterion).
    - `pytest -m identity` is a registered marker (no "unknown marker" warning).
  </behavior>
  <action>
    PAUSE for operator approval before installing (project policy: new deps need sign-off). Add to `[project].dependencies` in `pyproject.toml`, each pinned per STACK.md §6.5: `"pynacl==1.6.2"`, `"jcs==0.2.1"`, `"atproto==0.0.65"`, `"dag-cbor==0.3.3"`, `"joserfc==1.6.4"`, `"authlib==1.7.0"`. Bump `"cryptography>=41"` to `"cryptography>=46"` (STACK wants 46.0.7; the did:web verify path needs it) — confirm no other consumer pins it lower. Add a comment line above the DID block: `# DID stack (RISK-4): rejected didkit / pydid / python-jose / fastapi-users — see STACK.md`. Add `"identity: Phase 6 DID substrate tests"` to the `[tool.pytest.ini_options].markers` list (mirror the existing `"shards: ..."` entry). Run `uv lock` then `uv sync` to update `uv.lock`. Do NOT add itsdangerous/structlog/Redis (web-phase deps — deferred per RESEARCH §1).
  </action>
  <verify>
    <automated>uv run python -c "import nacl, jcs, atproto, dag_cbor, joserfc, authlib; print('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `pynacl==1.6.2`, `jcs==0.2.1`, `atproto==0.0.65`, `dag-cbor==0.3.3`, `joserfc==1.6.4`, `authlib==1.7.0`.
    - `pyproject.toml` contains the strings `didkit`, `pydid`, `python-jose`, `fastapi-users` inside a rejection comment.
    - `pyproject.toml` markers list contains `identity:`.
    - `uv run python -c "import nacl, jcs, atproto, dag_cbor, joserfc, authlib"` exits 0.
  </acceptance_criteria>
  <done>The locked DID stack is installed at pinned versions, rejections are documented, the identity marker is registered, and uv.lock is updated.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape AttestedSignature to the real 6.1 shape (D-13)</name>
  <files>src/folio_insights/shards/envelope.py, src/folio_insights/revision/content_edit.py, src/folio_insights/shards/audit.py</files>
  <read_first>
    - src/folio_insights/shards/envelope.py L49-66 (the AttestedSignature stub to REPLACE) + L182 (signatures: list[AttestedSignature])
    - src/folio_insights/revision/content_edit.py L207-221 (sign_attestation — action="content_edit" site) + L304-312 (edit_shard_content builds the ContentEdit.signature)
    - src/folio_insights/shards/audit.py L119-134 (add_edit inline AttestedSignature, action="content_edit") + L72 (ContentEdit frozen+extra=forbid) + L150-155 (model_rebuild — PRESERVE)
    - PRD-v2.0-draft-2.md §3.1 (L98-141 — the role/action source of truth: action="promote" L117, action="resolve_contest" L125, "edit content" L103) and §6.5 (the AttestedSignature shape)
    - .planning/REQUIREMENTS.md DID-02 (the 8 governance actions) and .planning/phases/06-did-substrate-6-5/06-RESEARCH.md §9 (touch-point map + the action reconciliation)
    - tests/shards/test_dep_leak_guard.py (envelope.py must stay crypto/RDF-free)
  </read_first>
  <behavior>
    - AttestedSignature(did="did:key:zX", action="promote", over_content_hash="h", signature="sig", signed_at=<dt>, signing_key_id="did:key:zX#zX", did_doc_snapshot_at=<dt>) constructs.
    - AttestedSignature with a bogus_field kwarg raises ValidationError (extra="forbid" now).
    - AttestedSignature with action="not_a_real_action" raises ValidationError (action is a Literal).
    - A fresh AttestedSignature has cosigners == [] (reserved, empty by default).
    - verified defaults to None; signing_key_id defaults to "" and did_doc_snapshot_at to None so the unsigned add_edit/sign_attestation stub paths still construct honestly.
    - The existing action="content_edit" sites (sign_attestation, add_edit) still construct after the Literal includes content_edit.
    - JSON round-trip (model_dump -> model_validate) of a ShardEnvelope with a populated signature yields an AttestedSignature instance (not a dict).
  </behavior>
  <action>
    In `shards/envelope.py`, REPLACE the `AttestedSignature` class body (do not extend): set `model_config = ConfigDict(extra="forbid")`. First, read PRD §3.1 + REQUIREMENTS DID-02 and define `SignedAction = Literal[...]` whose members are the governance actions reconciled to the PRD §3.1 source-of-truth spellings (the PRD uses e.g. `"promote"`, `"resolve_contest"`; map the DID-02 set `{extract, promote, demote, contest, supersede, retract, distinguo, role-assertion}` to the exact strings the PRD/code use) PLUS `"content_edit"` (PRD §3.1 L103 makes content editing a real reviewer action, and the Phase-5 audit path already signs with it — including it keeps every construction site green while the DID-07 preview in Plan 03 iterates the governance-action subset). Document the chosen set + its PRD line refs in the class docstring. Keep fields `did: str`, `action: SignedAction = "content_edit"` (default preserves the audit stub), `signed_at: datetime | None = None`, `signature: str = ""`, `over_content_hash: str = ""`. ADD: `signing_key_id: str = ""` (DID URL + `#fragment`), `did_doc_snapshot_at: datetime | None = None`, `verified: bool | None = None` (cache annotation), and `cosigners: list[AttestedSignature] = Field(default_factory=list)` (RESERVE for deferred 6.3 — present-but-empty; self-referential, so wrap the annotation as a forward ref / use the existing model and rely on Pydantic 2 self-reference support). Update the class docstring from "Permissive Phase 6 STUB" to the real 6.1 contract. Keep envelope.py pure-Pydantic (NO crypto/RDF import). Then verify `revision/content_edit.py` L215 `sign_attestation` and `shards/audit.py` L127 `add_edit` still construct under the new shape (action="content_edit" is now a valid Literal member; the new fields have defaults) — adjust only if a kwarg is now rejected. PRESERVE `ShardEnvelope.model_rebuild()` at audit.py bottom.
  </action>
  <verify>
    <automated>uv run pytest -m shards -x -q -k "envelope or roundtrip or audit or content_edit"</automated>
  </verify>
  <acceptance_criteria>
    - `src/folio_insights/shards/envelope.py` AttestedSignature contains `extra="forbid"`, `signing_key_id`, `did_doc_snapshot_at`, `verified`, and `cosigners`.
    - `src/folio_insights/shards/envelope.py` defines `action` as a `Literal[` (not `str`).
    - `grep -v '^#' src/folio_insights/shards/envelope.py | grep -c -E '\bimport (nacl|jcs|atproto|dag_cbor|joserfc|rdflib|pyoxigraph)\b'` returns 0.
    - `uv run python -c "from folio_insights.shards import AttestedSignature; s=AttestedSignature(); assert s.cosigners==[] and s.verified is None"` exits 0.
    - `uv run python -c "from folio_insights.shards import AttestedSignature; AttestedSignature(bogus=1)"` exits NON-zero (extra=forbid).
    - `uv run pytest -m shards -x -q` exits 0 (all shards tests green, incl. round-trip + audit).
  </acceptance_criteria>
  <done>AttestedSignature is the real 6.1 shape (extra=forbid + signing fields + reserved cosigners + action Literal reconciled to PRD §3.1); all construction sites green; envelope stays pure Pydantic.</done>
</task>

<task type="auto">
  <name>Task 3: Swap RFC-8785 JCS into canonical_content_hash with F4 pre-normalization (D-12)</name>
  <files>src/folio_insights/revision/content_edit.py</files>
  <read_first>
    - src/folio_insights/revision/content_edit.py L139-201 (the canonical_content_hash function + _HASH_EXCLUDED_FIELDS + the D-12 "swap into the single line" comment L159-160, L195-197)
    - .planning/phases/06-did-substrate-6-5/06-RESEARCH.md §2 (the exact recipe: model_dump(mode=json) -> pin datetime -> NFC recursive -> explicit None policy -> jcs.canonicalize)
    - .planning/research/PITFALLS.md F4 (the BLOCKING failure modes: datetime fmt, float precision, null, Unicode NFC)
    - src/folio_insights/shards/minting.py L89-93 (the sha256-over-normalized-payload precedent this mirrors)
  </read_first>
  <behavior>
    - canonical_content_hash(shard) returns a 64-char hex sha256 string.
    - Two shards identical in content but built with different field-insertion order / dict key order hash IDENTICALLY.
    - A shard whose `sense` contains a decomposed-form accent (NFD) hashes the SAME as the NFC-normalized equivalent.
    - Changing only `transaction_time` / `valid_time_*` / `content_edits` / `signatures` does NOT change the hash (exclusion set preserved).
    - The function signature and every call site (edit_shard_content L302, audit) are unchanged (D-12 seam).
  </behavior>
  <action>
    In `revision/content_edit.py`, replace the `json.dumps(...)` line (L200) inside `canonical_content_hash` with the RFC-8785 recipe from RESEARCH §2, keeping the function name, signature, `_HASH_EXCLUDED_FIELDS`, and `payload = shard.model_dump(mode="json", exclude=_HASH_EXCLUDED_FIELDS)` line. Add a module-level helper `_jcs_canonical_bytes(payload: dict) -> bytes` that: (1) recursively NFC-normalizes every string KEY and VALUE via `unicodedata.normalize("NFC", s)`; (2) keeps explicit `None` values (do NOT strip — lock the policy and document it; jcs serializes null deterministically); (3) returns `jcs.canonicalize(normalized)`. Then `return hashlib.sha256(_jcs_canonical_bytes(payload)).hexdigest()`. Pin datetime canonicalization: add a `@field_serializer` (or model serializer) on `ShardEnvelope` for the surviving `datetime` fields (e.g. `extracted_at`) that emits RFC-3339 UTC with a fixed representation (`Z` suffix, fixed precision) — OR normalize datetime-typed values inside `_jcs_canonical_bytes` to that fixed form; choose one, document it, and ensure both signer and verifier go through this single function. Import `jcs` and `unicodedata` at module top (revision/ allows non-shards deps). Drop the now-unused `sort_keys`/`separators` (jcs sorts keys itself). Update the function docstring + the L159-160/L195-197 seam comments to say JCS is now wired.
  </action>
  <verify>
    <automated>uv run pytest tests/revision/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/folio_insights/revision/content_edit.py` imports `jcs` and `unicodedata`.
    - `src/folio_insights/revision/content_edit.py` contains `jcs.canonicalize` and `unicodedata.normalize("NFC"`.
    - `canonical_content_hash`'s signature is unchanged: `def canonical_content_hash(shard: ShardEnvelope) -> str:`.
    - `_HASH_EXCLUDED_FIELDS` is unchanged (still excludes transaction_time, valid_time_start, valid_time_end, content_edits, signatures).
    - Datetime seam is stable across serialize/deserialize: a shard with a non-midnight microsecond-precision `extracted_at` hashes identically before and after a `model_validate(model_dump(mode="json"))` round-trip (closes the remaining F4 datetime surface — signer and verifier must agree).
    - `uv run pytest tests/revision/ -x -q` exits 0 (existing revision tests still green with JCS swapped in).
  </acceptance_criteria>
  <done>canonical_content_hash uses RFC-8785 JCS over NFC-normalized, canonical-datetime, explicit-None payload; exclusion set + call site unchanged; revision tests green.</done>
</task>

<task type="auto">
  <name>Task 4: tests/identity scaffold + JCS order-independence property test + cross-impl golden test</name>
  <files>tests/identity/__init__.py, tests/identity/conftest.py, tests/identity/test_canonical_jcs_properties.py, tests/identity/test_jcs_golden.py, tests/identity/fixtures/jcs_golden/README.md</files>
  <read_first>
    - tests/shards/conftest.py (the _sample_shard builder to import/adapt for random shard generation)
    - tests/revision/conftest.py (async store fixtures pattern, if needed)
    - .planning/phases/06-did-substrate-6-5/06-RESEARCH.md §2 (the two tests: 1000-shard property test + ~50-input cyberphone golden test) and § Validation Architecture (the test artifact names)
    - src/folio_insights/revision/content_edit.py (the canonical_content_hash under test, post-Task-3)
    - https://github.com/cyberphone/json-canonicalization test-data layout (input + expected canonical bytes) — vendor a representative subset offline
  </read_first>
  <behavior>
    - `pytest tests/identity/test_canonical_jcs_properties.py` builds 1000 random valid shards, and for each, several field/dict-order permutations hash identically (exit criterion 2).
    - The property test also applies a Unicode-noise transform (NFD vs NFC, NBSP) and asserts the hash is invariant.
    - `pytest tests/identity/test_jcs_golden.py` loads the vendored cyberphone fixtures and asserts our `_jcs_canonical_bytes` (or canonicalization core) output equals the reference canonical bytes for every fixture (~50).
  </behavior>
  <action>
    Create `tests/identity/__init__.py` (empty) and `tests/identity/conftest.py` with a hypothesis strategy (or factory) that builds valid `ShardEnvelope`s with randomized but valid field values, reusing `tests/shards/conftest.py` builders where possible. Create `tests/identity/test_canonical_jcs_properties.py` with `pytestmark = pytest.mark.identity` and a `@given(...)` (or a seeded 1000-iteration loop) test that, for each generated shard, computes `canonical_content_hash` over the shard and over copies whose `model_dump` dict key order / Optional-field presence order is permuted, and asserts all hashes are equal; add a sub-test feeding NFD vs NFC strings in `sense`/`reference`/`source_span` and asserting hash invariance. Cap hypothesis `max_examples`/`deadline` so the run stays under the 30s pytest timeout (mark `slow` if it cannot). Create `tests/identity/fixtures/jcs_golden/` with a `README.md` documenting provenance (cyberphone/json-canonicalization, RFC 8785 reference) and vendor ~50 `input`/`expected` pairs covering: Unicode (NBSP, em-dash, combining accents, emoji, surrogate-range), numbers (0, 1.0, -0, 1e-7, large ints, negatives), nested objects/arrays, null values, and key-ordering. Create `tests/identity/test_jcs_golden.py` (`pytestmark = pytest.mark.identity`) that iterates the fixtures and asserts our canonicalization core output bytes == the expected reference bytes. Keep all test selection path-based (`tests/identity/`) so no marker registration is strictly required, though Task 1 registers `identity`.
  </action>
  <verify>
    <automated>uv run pytest tests/identity/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/identity/test_canonical_jcs_properties.py` exists, contains `pytestmark = pytest.mark.identity`, and exercises >= 1000 shards (via @given max_examples or an explicit loop).
    - `tests/identity/test_jcs_golden.py` exists and loads fixtures from `tests/identity/fixtures/jcs_golden/`.
    - `ls tests/identity/fixtures/jcs_golden/ | wc -l` >= 1 (fixtures vendored).
    - `uv run pytest tests/identity/ -x -q` exits 0 (exit criterion 2 green; F4 golden check green).
  </acceptance_criteria>
  <done>The tests/identity tree exists; the 1000-shuffled-order property test (EC2) and the cyberphone cross-impl golden test (F4) are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI supply chain -> project deps | 6 new crypto/identity packages enter the trust base; a compromised/typo-squatted package would sign or verify maliciously |
| signer/verifier -> canonical_content_hash | The hash is the single value a signature binds; non-determinism (F4) silently breaks the security guarantee while tests "pass" |
| caller -> AttestedSignature construction | `action`/`signature`/`signing_key_id` are caller-supplied; the schema (extra=forbid + Literal) is the first integrity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (supply chain) | 6 new pip deps | mitigate | Pin EXACT versions from STACK.md (`==`); install only the RISK-4-locked set; document the rejection of didkit/pydid/python-jose/fastapi-users; `uv.lock` hashes pin the artifacts. `autonomous:false` forces operator review of the install. |
| T-06-02 | Spoofing | canonical_content_hash non-determinism (F4) | mitigate | RFC-8785 JCS + NFC + canonical datetime + explicit-None recipe; 1000-shard order-independence property test + cyberphone cross-impl golden test. A drifting hash would let a re-serialized shard read as a different/forged content — the tests close it. |
| T-06-03 | Tampering | AttestedSignature accepting unknown/forged fields | mitigate | `extra="forbid"` rejects unknown kwargs; `action` Literal rejects out-of-vocabulary actions; `verified` defaults None (an unverified sig can never read as verified by default). |
| T-06-04 | Repudiation | content_edit stub still unsigned after reshape | accept | `signing_key_id`/`signature` default empty so the Phase-5 audit stub stays HONESTLY unsigned; real signing arrives in Plan 02. Documented, bounded deferral (no false "verified"). |
</threat_model>

<verification>
- `uv run pytest -m shards -x -q` green (AttestedSignature reshape did not break envelope/audit/round-trip).
- `uv run pytest tests/revision/ -x -q` green (JCS swap did not break the content-versioning path).
- `uv run pytest tests/identity/ -x -q` green (EC2 order-independence + F4 cross-impl golden).
- `uv run pytest tests/shards/test_dep_leak_guard.py -x` green (shards/ still crypto/RDF-free).
- `uv run python -c "import nacl, jcs, atproto, dag_cbor, joserfc, authlib"` exits 0.
</verification>

<success_criteria>
- Locked DID stack installed at STACK.md pins; rejections documented; identity marker registered (DID-01).
- AttestedSignature reshaped to the real 6.1 shape (extra=forbid + signing_key_id + did_doc_snapshot_at + verified + reserved cosigners[]); action Literal reconciled to PRD §3.1; all construction sites green (D-13, DID-02).
- canonical_content_hash uses RFC-8785 JCS with the F4 pre-normalization recipe; exclusion set + call site unchanged (D-12, DID-03).
- verify(sign(x))/hash is order-independent across 1000 shuffled-order shards (EC2) and matches the cyberphone reference on ~50 golden inputs (F4 closed).
- shards/ still imports zero crypto/RDF/storage libraries (dep-leak guard green).
</success_criteria>

<output>
Create `.planning/phases/06-did-substrate-6-5/06-01-SUMMARY.md` when done.
</output>
