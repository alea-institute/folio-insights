---
phase: 01.1-upload-processing-ui
plan: 02
subsystem: api
tags: [fastapi, sse, asyncio, pipeline, processing, sse-starlette, pydantic]

# Dependency graph
requires:
  - phase: 01.1-01
    provides: Corpus CRUD API, file upload endpoint, ProcessingJob/ProcessingStatus models
  - phase: 01-knowledge-extraction-pipeline
    provides: PipelineOrchestrator with 7 stages, CorpusRegistry SHA-256 dedup, InsightsJob
provides:
  - JobManager with atomic disk persistence for ProcessingJob
  - run_pipeline_with_progress wrapper with per-stage SSE progress updates
  - POST /api/v1/corpus/{id}/process endpoint (202 background task)
  - GET /api/v1/corpus/{id}/stream SSE endpoint with status/activity/complete events
  - GET /api/v1/corpus/{id}/job convenience endpoint
  - CorpusRegistry SHA-256 dedup fix (IngestionStage loads existing manifest)
affects: [01.1-03, 01.1-04]

# Tech tracking
tech-stack:
  added: [sse-starlette]
  patterns: [background asyncio task with disk-based job polling, atomic file writes with temp+rename, SSE event generator with typed events]

key-files:
  created:
    - api/services/__init__.py
    - api/services/job_manager.py
    - api/services/pipeline_runner.py
    - api/routes/processing.py
    - tests/test_processing_api.py
  modified:
    - api/main.py
    - src/folio_insights/pipeline/stages/ingestion.py

key-decisions:
  - "JobManager keyed by corpus_id (one job file per corpus) matching folio-enrich's disk-based pattern"
  - "Pipeline runner iterates orchestrator._stages directly rather than calling orchestrator.run() to inject progress callbacks"
  - "SSE generator polls job file every 0.5s with typed events matching folio-enrich pattern"
  - "Atomic writes use asyncio.to_thread() wrapping sync tempfile+os.replace to avoid blocking event loop"

patterns-established:
  - "Background pipeline: asyncio.create_task() launches pipeline runner, SSE polls disk job file"
  - "SSE event types: status (progress change), activity (log entry), complete (terminal), error (missing job)"
  - "Atomic disk persistence: tempfile.mkstemp + os.replace for concurrent-safe JSON writes"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-03-18
---

# Phase 01.1 Plan 02: Processing Engine Summary

**Disk-based job manager with atomic writes, pipeline runner with stage-by-stage SSE progress updates, and CorpusRegistry SHA-256 dedup fix enabling re-processing skip**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-18T14:19:07Z
- **Completed:** 2026-03-18T14:22:59Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- JobManager persists ProcessingJob to disk with atomic writes (temp file + os.replace) preventing partial reads by SSE poller
- Pipeline runner wraps PipelineOrchestrator's 7 stages with per-stage progress updates: status, activity log, and completion events
- Fixed CorpusRegistry SHA-256 deduplication: IngestionStage now loads existing manifest from disk so re-runs skip already-processed files
- SSE stream endpoint emits typed events (status, activity, complete, error) following folio-enrich's proven pattern
- Processing trigger returns 202 and launches pipeline in background asyncio task
- 10 integration tests covering trigger, duplicate prevention, SSE events, job status, and edge cases

## Task Commits

Each task was committed atomically:

1. **Task 1: Job manager + pipeline runner + CorpusRegistry dedup fix** - `e5e0861` (feat)
2. **Task 2: Processing trigger + SSE stream + tests** - `0599162` (feat)

## Files Created/Modified
- `api/services/__init__.py` - Empty init for services package
- `api/services/job_manager.py` - JobManager class with atomic save, load_by_corpus, delete
- `api/services/pipeline_runner.py` - run_pipeline_with_progress with stage-by-stage updates
- `api/routes/processing.py` - Processing trigger, SSE stream, and job status endpoints
- `api/main.py` - Added processing router registration
- `src/folio_insights/pipeline/stages/ingestion.py` - Fixed CorpusRegistry to load from disk instead of creating fresh
- `tests/test_processing_api.py` - 10 integration tests

## Decisions Made
- JobManager uses corpus_id as file key (one job per corpus) rather than job UUID, simplifying SSE lookup
- Pipeline runner accesses orchestrator._stages directly to iterate stages individually with progress updates between each
- SSE poll interval set to 0.5s matching folio-enrich reference implementation
- Used asyncio.to_thread() for sync file I/O (write + read) to avoid blocking the event loop
- Processing route includes a reset_job_manager() helper for test isolation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Processing backend complete: trigger endpoint, SSE stream, job persistence
- Ready for Plan 03/04 (frontend) to connect: Process button triggers POST, EventSource connects to stream
- CorpusRegistry dedup fix ensures re-processing works correctly for the frontend "re-process" workflow

## Self-Check: PASSED

All 8 files verified present. Both task commits (e5e0861, 0599162) found in git history.

---
*Phase: 01.1-upload-processing-ui*
*Completed: 2026-03-18*
