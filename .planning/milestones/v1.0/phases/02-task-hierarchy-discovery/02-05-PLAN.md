---
phase: 02-task-hierarchy-discovery
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - viewer/src/lib/components/TaskDashboard.svelte
  - viewer/src/lib/components/DiffView.svelte
  - viewer/src/lib/components/ManualTaskDialog.svelte
  - viewer/src/lib/components/DiscoverButton.svelte
  - viewer/src/lib/components/DiscoveryProgress.svelte
  - viewer/src/lib/components/NavTabs.svelte
  - viewer/src/lib/components/KeyboardShortcuts.svelte
  - viewer/src/routes/+layout.svelte
  - viewer/src/routes/upload/+page.svelte
  - viewer/src/routes/tasks/+page.svelte
autonomous: false

requirements: [TASK-01, TASK-02]

must_haves:
  truths:
    - "Users can trigger task discovery from the upload page and watch progress via SSE"
    - "The Tasks tab appears in navigation between Upload and Review"
    - "The summary dashboard shows task/unit/contradiction statistics"
    - "Manual task creation dialog allows naming, FOLIO parent selection, and unit assignment"
    - "Re-run diff view shows new/removed/changed tasks for accept/reject"
    - "Keyboard shortcuts M=move, G=merge, Shift+A=bulk approve work on tasks page"
    - "Visual verification confirms the complete Upload -> Process -> Discover -> Review workflow"
  artifacts:
    - path: "viewer/src/lib/components/TaskDashboard.svelte"
      provides: "Summary statistics panel with 6 stat cards"
      min_lines: 60
    - path: "viewer/src/lib/components/DiscoverButton.svelte"
      provides: "Discover Tasks button with idle/processing/complete states"
      min_lines: 30
    - path: "viewer/src/lib/components/DiscoveryProgress.svelte"
      provides: "Discovery-specific progress display with 6 stage pills"
      min_lines: 40
    - path: "viewer/src/lib/components/NavTabs.svelte"
      provides: "Three-tab navigation: Upload, Tasks, Review"
      contains: "Tasks"
  key_links:
    - from: "viewer/src/routes/upload/+page.svelte"
      to: "viewer/src/lib/components/DiscoverButton.svelte"
      via: "Renders DiscoverButton after processing completes"
      pattern: "<DiscoverButton"
    - from: "viewer/src/routes/+layout.svelte"
      to: "viewer/src/lib/components/NavTabs.svelte"
      via: "NavTabs renders Upload/Tasks/Review tabs"
      pattern: "activePage.*tasks"
    - from: "viewer/src/routes/tasks/+page.svelte"
      to: "viewer/src/lib/components/TaskDashboard.svelte"
      via: "Dashboard toggle overlay"
      pattern: "<TaskDashboard"
---

<objective>
Complete the frontend with remaining components (TaskDashboard, DiffView, ManualTaskDialog, DiscoverButton, DiscoveryProgress), extend navigation and routing for /tasks, wire discovery trigger on the upload page, extend keyboard shortcuts, and verify the full workflow visually.

Purpose: Finishes all frontend work for Phase 2. The upload page gets a "Discover Tasks" button with SSE progress. Navigation gains a "Tasks" tab. The tasks page gets a summary dashboard, manual task creation dialog, and re-run diff view. Keyboard shortcuts are extended for task operations. A visual checkpoint confirms the end-to-end workflow.

Output: Complete frontend for task hierarchy discovery and review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-task-hierarchy-discovery/02-CONTEXT.md
@.planning/phases/02-task-hierarchy-discovery/02-UI-SPEC.md
@.planning/phases/02-task-hierarchy-discovery/02-04-SUMMARY.md

<interfaces>
<!-- From Plan 02-04 (must exist before this runs) -->

From viewer/src/lib/stores/discovery.ts:
```typescript
export const discoveryStatus = writable<'idle' | 'processing' | 'complete' | 'error'>('idle');
export const discoveryStage = writable<string>('');
export const discoveryProgress = writable<number>(0);
export const discoveryLog = writable<Array<{ timestamp: string; stage: string; message: string }>>([]);
export function startDiscoveryStream(corpusId: string): void;
export function closeDiscoveryStream(): void;
export function resetDiscovery(): void;
```

From viewer/src/lib/stores/tasks.ts:
```typescript
export const taskTreeData = writable<TaskTreeNode[]>([]);
export const selectedTaskId = writable<string | null>(null);
export const discoveryStats = writable<DiscoveryStatsResponse | null>(null);
export async function loadDiscoveryStats(corpusId: string): Promise<void>;
export async function loadTaskTree(corpusId: string, mode?: string): Promise<void>;
```

From viewer/src/lib/api/client.ts:
```typescript
export async function triggerDiscovery(corpusId: string): Promise<...>;
export function getDiscoverySSEUrl(corpusId: string): string;
export async function createTask(corpusId: string, label: string, ...): Promise<...>;
export async function fetchDiscoveryStats(corpusId: string): Promise<...>;
export async function submitHierarchyEdit(corpusId: string, ...): Promise<...>;
```

From viewer/src/lib/components/NavTabs.svelte:
```svelte
let { activePage }: { activePage: 'upload' | 'review' } = $props();
// Two tabs: Upload, Review -- needs "Tasks" added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard, DiffView, ManualTaskDialog, DiscoverButton, DiscoveryProgress components</name>
  <files>
    viewer/src/lib/components/TaskDashboard.svelte,
    viewer/src/lib/components/DiffView.svelte,
    viewer/src/lib/components/ManualTaskDialog.svelte,
    viewer/src/lib/components/DiscoverButton.svelte,
    viewer/src/lib/components/DiscoveryProgress.svelte
  </files>
  <read_first>
    viewer/src/lib/stores/tasks.ts,
    viewer/src/lib/stores/discovery.ts,
    viewer/src/lib/api/client.ts,
    viewer/src/lib/components/ProcessButton.svelte,
    viewer/src/lib/components/ProgressDisplay.svelte,
    viewer/src/lib/components/ConfirmDialog.svelte,
    viewer/src/lib/components/ActivityLog.svelte,
    viewer/src/app.css,
    .planning/phases/02-task-hierarchy-discovery/02-UI-SPEC.md
  </read_first>
  <action>
**1. TaskDashboard.svelte:**

Per 02-UI-SPEC "TaskDashboard Visual Spec":

- Overlay panel, toggled via prop or store boolean. Positioned top-right below header, 480px wide, max 70vh tall, `box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4)`, `background: var(--surface)`, `border: 1px solid var(--border)`, `border-radius: 6px`, `z-index: 100`.
- Grid of 6 stat cards, 2 columns, `gap: var(--md)`.
- Each card: `background: var(--surface2)`, `border: 1px solid var(--border)`, `border-radius: 6px`, `padding: var(--md)`.
  - Metric label: 11px uppercase, letter-spacing 0.5px, `color: var(--text-dim)`.
  - Value: 28px weight 600.
  - Subtext: 11px, `color: var(--text-dim)`.

The 6 cards per UI-SPEC:
1. **Total Tasks:** label "TASKS", value `{total_tasks}`, subtext "{N} top-level, {N} subtasks", `color: var(--text)`.
2. **Knowledge Units:** label "UNITS ASSIGNED", value `{total_units_assigned}`, subtext "{N} orphans remaining" (orange if > 0), `color: var(--text)`.
3. **Review Progress:** label "REVIEW PROGRESS", value `{review_progress_pct}%`, subtext "{N}% complete", `color: var(--green)`.
4. **Contradictions:** label "CONTRADICTIONS", value `{contradiction_count}`, `color: var(--red)` if unresolved > 0 else `var(--green)`, subtext "{N} resolved, {N} pending".
5. **Source Coverage:** label "SOURCE COVERAGE", value `{covered}/{total}`, subtext "{N}/{N} files represented", `color: var(--text)`.
6. **Confidence:** label "AVG CONFIDENCE", value with semantic color based on band, distribution bar (4px height, 3 colored segments proportional to high/medium/low counts).

Props: `stats: DiscoveryStatsResponse`, `visible: boolean`, `onclose: () => void`.

**2. DiffView.svelte:**

Per 02-UI-SPEC "DiffView Visual Spec":

- Displayed inline within TaskDetail when a re-run produces changes.
- Data source: `fetchDiscoveryDiff(corpusId)` from `api/client.ts` (Plan 02-04), which calls `GET /corpus/{corpus_id}/discovery/diff` (Plan 02-03), which reads `discovery_diff.json` written by `TaskDiscoveryOrchestrator._compute_diff()` (Plan 02-02).
- Props: `changes: Array<{ type: 'added' | 'removed' | 'changed'; description: string; id: string }>`, `onaccept: (id: string) => void`, `onreject: (id: string) => void`, `onacceptall: () => void`, `onrejectall: () => void`.
- Bulk actions at top: "Accept All Changes" (`color: var(--green)`) and "Reject All Changes" (`color: var(--red)`), outline buttons.
- Each change row: icon + description at 14px + Accept/Reject buttons.
  - Added: `background: var(--diff-added)`, green "+" icon.
  - Removed: `background: var(--diff-removed)`, red "-" icon.
  - Changed: `background: var(--diff-changed)`, orange "~" icon.
  - "Accept Change" button: `color: var(--green)`, outline. Text: "Accept Change".
  - "Reject Change" button: `color: var(--red)`, outline. Text: "Reject Change".

**3. ManualTaskDialog.svelte:**

Extends ConfirmDialog pattern per UI-SPEC:

- Modal dialog: `role="alertdialog"`, `aria-modal="true"`, focus trap.
- Heading: "Create a new task manually. Name it, choose its parent in the FOLIO hierarchy, and assign knowledge units." at 18px.
- Fields:
  - Task name input: `<input type="text" placeholder="Task name" />` with validation (required, min 2 chars).
  - FOLIO parent picker: `<select>` populated from task tree nodes (allow selecting any existing task as parent, or no parent for top-level).
  - Task type toggle: Procedural / Categorical.
- Buttons: "Create Task" (`background: var(--accent)`) and "Cancel" (`background: transparent`, `border: 1px solid var(--border)`).
- On submit: calls `createTask(corpusId, label, folioIri, parentTaskId)` and reloads task tree.

**4. DiscoverButton.svelte:**

Mirrors ProcessButton per UI-SPEC "DiscoverButton Visual Spec":

- Props: `status: 'ready' | 'disabled' | 'processing' | 'complete'`, `onclick: () => void`.
- 4 states:
  - Ready: `background: var(--accent)`, text "Discover Tasks", clickable.
  - Disabled: `background: var(--surface3)`, text "Discover Tasks", `opacity: 0.5`, `cursor: not-allowed`.
  - Processing: `background: var(--accent)`, text "Discovering...", disabled, spinner animation.
  - Complete: `background: var(--green)`, text "Tasks Discovered", transitions to link navigating to /tasks.
- Button: `height: 36px`, `padding: 0 var(--md)`, `border-radius: 4px`, `font-size: 14px`, `font-weight: 600`.

**5. DiscoveryProgress.svelte:**

Same layout as ProgressDisplay per UI-SPEC "DiscoveryProgress Visual Spec":

- 6 stage pills in order: "Heading Analysis", "FOLIO Mapping", "Content Clustering", "Hierarchy Construction", "Cross-Source Merging", "Contradiction Detection".
- Pill states: pending (surface2/text-dim), active (accent bg 20%/accent), complete (green bg 20%/green), error (red bg 20%/red).
- Progress bar: 3px height, border-radius 2px, accent during processing, green on complete, red on error.
- Props: `currentStage: string`, `progress: number`, `status: string`.
  </action>
  <verify>
    <automated>cd viewer && npx svelte-check --threshold error 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - viewer/src/lib/components/TaskDashboard.svelte contains `TASKS`
    - viewer/src/lib/components/TaskDashboard.svelte contains `CONTRADICTIONS`
    - viewer/src/lib/components/TaskDashboard.svelte contains `REVIEW PROGRESS`
    - viewer/src/lib/components/DiffView.svelte contains `Accept Change`
    - viewer/src/lib/components/DiffView.svelte contains `Reject Change`
    - viewer/src/lib/components/DiffView.svelte contains `var(--diff-added)`
    - viewer/src/lib/components/ManualTaskDialog.svelte contains `Create Task` or `createTask`
    - viewer/src/lib/components/ManualTaskDialog.svelte contains `role="alertdialog"`
    - viewer/src/lib/components/DiscoverButton.svelte contains `Discover Tasks`
    - viewer/src/lib/components/DiscoverButton.svelte contains `Discovering...`
    - viewer/src/lib/components/DiscoveryProgress.svelte contains `Heading Analysis`
    - viewer/src/lib/components/DiscoveryProgress.svelte contains `Contradiction Detection`
    - svelte-check exits with 0 errors
  </acceptance_criteria>
  <done>Five components created: TaskDashboard (6 stat cards), DiffView (accept/reject changes), ManualTaskDialog (create task dialog), DiscoverButton (4-state CTA), DiscoveryProgress (6 stage pills). All follow UI-SPEC visual specifications exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation routing, upload page extension, keyboard shortcuts, and page wiring</name>
  <files>
    viewer/src/lib/components/NavTabs.svelte,
    viewer/src/lib/components/KeyboardShortcuts.svelte,
    viewer/src/routes/+layout.svelte,
    viewer/src/routes/upload/+page.svelte,
    viewer/src/routes/tasks/+page.svelte
  </files>
  <read_first>
    viewer/src/lib/components/NavTabs.svelte,
    viewer/src/lib/components/KeyboardShortcuts.svelte,
    viewer/src/routes/+layout.svelte,
    viewer/src/routes/upload/+page.svelte,
    viewer/src/routes/tasks/+page.svelte,
    viewer/src/lib/stores/discovery.ts,
    viewer/src/lib/stores/tasks.ts,
    viewer/src/lib/stores/processing.ts,
    viewer/src/lib/api/client.ts,
    .planning/phases/02-task-hierarchy-discovery/02-UI-SPEC.md
  </read_first>
  <action>
**1. NavTabs.svelte update:**

Change the `activePage` prop type from `'upload' | 'review'` to `'upload' | 'tasks' | 'review'`.

Add a "Tasks" tab between Upload and Review:
```svelte
<a
    href="/tasks"
    role="tab"
    aria-selected={activePage === 'tasks'}
    class="nav-tab"
    class:active={activePage === 'tasks'}
    data-sveltekit-preload-data
>
    Tasks
</a>
```

Keep the same CSS styles -- no changes needed since `.nav-tab` and `.active` already handle styling.

**2. +layout.svelte update:**

Change the `activePage` derived value to handle the /tasks route:
```typescript
let activePage = $derived<'upload' | 'tasks' | 'review'>(
    $page.url.pathname.startsWith('/upload') ? 'upload' :
    $page.url.pathname.startsWith('/tasks') ? 'tasks' : 'review'
);
```

For the header-center and header-right sections: keep existing confidence tabs for review page. For tasks page, show a dashboard toggle button in header-right. For upload page, keep empty center.

Add to header-right when `activePage === 'tasks'`:
```svelte
<button class="settings-btn" aria-label="Task summary dashboard" title="Task summary dashboard" onclick={() => showDashboard = !showDashboard}>
    <!-- Dashboard icon SVG -->
</button>
```

**3. Upload page extension** (`viewer/src/routes/upload/+page.svelte`):

After the processing complete state, add:
- Import `DiscoverButton` and `DiscoveryProgress` and `ActivityLog` components.
- Import discovery store: `discoveryStatus`, `discoveryStage`, `discoveryProgress`, `discoveryLog`, `startDiscoveryStream`, `resetDiscovery`.
- Import `triggerDiscovery` from API client.

Add to the upload page template AFTER the processing complete section:

When `$processingStatus === 'complete'`:
- Show `<DiscoverButton>` with:
  - status="ready" when discovery hasn't started
  - status="processing" when `$discoveryStatus === 'processing'`
  - status="complete" when `$discoveryStatus === 'complete'`
  - onclick handler: call `triggerDiscovery(corpusId)` then `startDiscoveryStream(corpusId)`
- When `$discoveryStatus === 'processing'` or `$discoveryStatus === 'complete'`:
  - Show `<DiscoveryProgress currentStage={$discoveryStage} progress={$discoveryProgress} status={$discoveryStatus} />`
  - Show `<ActivityLog entries={$discoveryLog} />`
- When `$discoveryStatus === 'complete'`:
  - Auto-navigate to /tasks after 1.5s delay (same pattern as processing auto-nav to review):
    ```typescript
    $effect(() => {
        if ($discoveryStatus === 'complete') {
            const timer = setTimeout(() => { goto('/tasks'); }, 1500);
            return () => clearTimeout(timer);
        }
    });
    ```
  - Show "Review Task Tree" link styled in `--accent`.

**4. KeyboardShortcuts.svelte extension:**

Read the existing shortcuts modal and add new Phase 2 entries to the shortcuts list:
- `m / M` -- "Move task to new parent" (task tree focused)
- `g / G` -- "Merge selected task into another" (task tree focused)
- `Shift+A` -- "Bulk approve all high-confidence tasks"
- `1 / 2 / 3 / 4` -- "Confidence filter: all / high / medium / low"

These should appear in a "Task Operations" section after the existing shortcuts.

**5. Tasks page wiring** (`viewer/src/routes/tasks/+page.svelte`):

Update the /tasks page to wire in the new components from Task 1:
- Add dashboard toggle: `let showDashboard = $state(false);`
- Render `<TaskDashboard>` as overlay when `showDashboard` is true.
- Add "New Task" button in the tree pane header that opens `<ManualTaskDialog>`.
- Wire keyboard shortcuts (global handler):
  - `a/A` on tasks page: approve selected task/unit (call submitTaskReview)
  - `r/R`: reject selected task/unit
  - `e/E`: open inline editor for selected unit
  - `m/M`: enter move mode (highlight tree, next click = drop target)
  - `g/G`: open merge dialog (ConfirmDialog asking "Merge {task} into which task?")
  - `j/ArrowDown`: navigate to next task/unit
  - `k/ArrowUp`: navigate to previous task/unit
  - `ArrowRight`: expand tree node
  - `ArrowLeft`: collapse tree node
  - `Tab`: cycle pane focus: tree -> detail -> evidence
  - `?`: toggle keyboard shortcuts modal
  - `Shift+A`: bulk approve all high-confidence tasks
  - `Ctrl+F / Cmd+F`: focus tree search input
  - `Escape`: close editor, modal, or DnD mode
  - `1/2/3/4`: confidence filter shortcuts

Add `aria-live="polite"` region for screen reader announcements of state changes (task approved, moved, etc.).
  </action>
  <verify>
    <automated>cd viewer && npx svelte-check --threshold error 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - viewer/src/lib/components/NavTabs.svelte contains `Tasks` (the tab label)
    - viewer/src/lib/components/NavTabs.svelte contains `/tasks` (the href)
    - viewer/src/lib/components/NavTabs.svelte contains `'upload' | 'tasks' | 'review'`
    - viewer/src/routes/+layout.svelte contains `'/tasks'`
    - viewer/src/routes/+layout.svelte contains `activePage === 'tasks'`
    - viewer/src/routes/upload/+page.svelte contains `DiscoverButton`
    - viewer/src/routes/upload/+page.svelte contains `DiscoveryProgress`
    - viewer/src/routes/upload/+page.svelte contains `triggerDiscovery`
    - viewer/src/routes/upload/+page.svelte contains `startDiscoveryStream`
    - viewer/src/routes/upload/+page.svelte contains `goto('/tasks')`
    - viewer/src/lib/components/KeyboardShortcuts.svelte contains `Move task`
    - viewer/src/lib/components/KeyboardShortcuts.svelte contains `Merge selected`
    - viewer/src/routes/tasks/+page.svelte contains `TaskDashboard`
    - viewer/src/routes/tasks/+page.svelte contains `ManualTaskDialog`
    - viewer/src/routes/tasks/+page.svelte contains `aria-live="polite"`
    - svelte-check exits with 0 errors
  </acceptance_criteria>
  <done>NavTabs updated with 3 tabs (Upload/Tasks/Review). Layout routes to /tasks correctly. Upload page shows Discover Tasks button after processing, with SSE progress and auto-navigation to /tasks. Keyboard shortcuts extended with M=move, G=merge, Shift+A=bulk approve. Tasks page wired with dashboard overlay, manual task dialog, and full keyboard shortcut handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete Phase 2 frontend</name>
  <files>viewer/src/routes/tasks/+page.svelte</files>
  <action>
Verify the complete Phase 2 frontend: task hierarchy discovery viewer with tree navigation, detail pane, filtering, contradiction resolution, discovery evidence, summary dashboard, manual task creation, re-run diff view, upload page discovery trigger, and three-tab navigation.
  </action>
  <verify>
1. Start the dev server: `.venv/bin/python -m uvicorn api.main:app --port 8742`
2. In a second terminal: `cd viewer && npm run dev`
3. Open the viewer in browser
4. Verify the NavTabs show three tabs: Upload, Tasks, Review
5. Click "Tasks" tab -- verify three-pane layout renders (tree left, detail upper-right, evidence lower-right)
6. Verify empty states display correct copy per UI-SPEC
7. If a corpus with extraction data exists: navigate to Upload page, verify "Discover Tasks" button appears after processing
8. Click the dashboard toggle button in header -- verify overlay shows 6 stat cards
9. Press "?" to verify keyboard shortcuts modal includes new M, G, Shift+A entries
10. Verify dark theme matches: accent blue, surface backgrounds, border colors
  </verify>
  <done>Visual verification approved: three-tab navigation works, tasks page renders three-pane layout with correct empty states, discovery trigger visible on upload page, dashboard overlay shows stat cards, keyboard shortcuts modal includes Phase 2 entries.</done>
</task>

</tasks>

<verification>
- NavTabs displays 3 tabs: Upload, Tasks, Review
- /tasks route renders three-pane layout
- Upload page shows Discover Tasks button after processing completes
- Dashboard overlay shows 6 stat cards with correct labels
- Keyboard shortcuts modal includes M=move, G=merge entries
- All components follow 02-UI-SPEC dark theme and spacing
- svelte-check passes with 0 errors
- Visual verification checkpoint approved
</verification>

<success_criteria>
Complete frontend for Phase 2 task hierarchy discovery. The full workflow is functional: Upload -> Process -> Discover Tasks -> Review Task Tree. Navigation has 3 tabs. Tasks page has tree, detail, filter, evidence, dashboard, and manual task creation. Keyboard shortcuts extended for task operations. Dark theme consistent with Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/02-task-hierarchy-discovery/02-05-SUMMARY.md`
</output>
