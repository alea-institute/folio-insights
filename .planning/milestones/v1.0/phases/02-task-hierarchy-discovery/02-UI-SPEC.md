---
phase: 02
slug: task-hierarchy-discovery
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 02 -- UI Design Contract

> Visual and interaction contract for Phase 2: Task Hierarchy Discovery. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom CSS variables) |
| Preset | not applicable |
| Component library | none (custom Svelte 5 components) |
| Icon library | inline SVG (hand-drawn, 12-18px viewBox, stroke-based, stroke-width 1.5-2) |
| Font | -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif (system stack, inherited from Phase 1) |

**Source:** Existing `viewer/src/app.css` `:root` block. Phase 2 extends the established token set -- does not replace it.

---

## Spacing Scale

Declared values (multiples of 4, inherited from `app.css`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`var(--xs)`) | Icon gaps, inline padding, pill internal padding, filter chip vertical padding |
| sm | 8px (`var(--sm)`) | Compact element spacing, tree node horizontal padding, filter chip horizontal padding, filter chip gaps |
| md | 16px (`var(--md)`) | Default element spacing, pane padding, section headers |
| lg | 24px (`var(--lg)`) | Header bar horizontal padding, dialog internal padding, empty state padding |
| xl | 32px (`var(--xl)`) | Dashboard card spacing, major layout gaps |
| 2xl | 48px (`var(--2xl)`) | Dashboard section breaks, hero empty states |

Exceptions:
- Tree node height: 28px (established in FolioTree.svelte, maintained for visual consistency)
- Drag handle touch target: 44px minimum (accessibility, applies to tree DnD grab zones)
- Header bar: 48px height (established in +layout.svelte)
- Sidebar width: 280px (established in CorpusSidebar.svelte)

---

## Typography

Phase 2 uses exactly 4 font sizes and 2 font weights. All values are inherited from Phase 1's established patterns and consolidated for contract compliance.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Small | 11px | 400 | 1.3 | Badge text, IRI display (monospace), breadcrumbs, pill labels, confidence scores, timestamps, type tags, filter chip labels, group labels, toggle labels, metadata |
| Body | 14px | 400 | 1.5 | Unit text, dialog messages, source text, task descriptions, tree node labels, nav tabs, button labels, filter group titles, corpus sidebar items |
| Heading | 18px | 600 | 1.2 | Concept labels, section headings, empty state headings, task names in detail pane, modal dialog headings, contradiction resolution headings |
| Display | 28px | 600 | 1.2 | Dashboard stat card values |

Monospace: system monospace at 11px for IRI display (established in DetailView.svelte).

Letter spacing: -0.3px on headings, 0.5px on uppercase labels/pills (both established).

**Consolidation notes:**
- Former 13px "Label" role merged into 14px Body. Tree node labels, nav tabs, buttons, and filter chips all use 14px.
- Former 16px "Dialog title" role merged into 18px Heading. Modal dialog headings use 18px, same as section headings.
- Weight 500 eliminated. Tree node labels (previously 500) use 400, distinguished from structural ancestors by color (`--text` for task nodes vs `--text-dim` for ancestor nodes).

---

## Color

All colors are inherited from `app.css` `:root`. Phase 2 adds new semantic tokens for Phase 2-specific states but no changes to the base palette.

### Base Palette (established)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f1117` (`--bg`) | Page background, source text background |
| Secondary (30%) | `#1a1d27` (`--surface`) | Pane backgrounds, header, sidebar, cards |
| Tertiary surfaces | `#242836` (`--surface2`), `#2e3348` (`--surface3`) | Hover states, input backgrounds, nested cards, type tags |
| Border | `#2e3348` (`--border`) | All borders, dividers, pane separators |
| Text primary | `#e4e6f0` (`--text`) | Body text, headings, labels |
| Text secondary | `#8b8fa3` (`--text-dim`) | Metadata, placeholders, inactive elements, breadcrumbs |
| Accent (10%) | `#6c8cff` (`--accent`) | Selected states, active tabs, focus rings, primary CTA buttons, edit outlines |
| Accent dim | `#4a5fa0` (`--accent-dim`) | Hover borders on cards |
| Highlight | `rgba(108, 140, 255, 0.15)` (`--highlight`) | Selected tree nodes, active pills, source text highlight |

### Semantic Colors (established)

| Token | Value | Reserved For |
|-------|-------|-------------|
| `--green` | `#4caf7c` | Approved status, high confidence, "Approve" buttons, complete progress, checkmarks |
| `--orange` | `#e8a54c` | Medium confidence, "processing" warnings, jurisdiction flags, heading extraction path |
| `--red` | `#e05555` | Rejected status, low confidence, errors, destructive buttons, contradiction indicators |
| `--cyan` | `#5ec4d4` | EntityRuler extraction path badge |
| `--purple` | `#b07ee8` | LLM extraction path badge, "procedural" task type badge |

### New Phase 2 Tokens (to add to `:root`)

| Token | Value | Usage |
|-------|-------|-------|
| `--contradiction` | `rgba(224, 85, 85, 0.12)` | Background tint for contradiction pair cards in side-by-side view |
| `--orphan` | `rgba(232, 165, 76, 0.12)` | Background tint for orphan-flagged units in task detail |
| `--jurisdiction` | `rgba(232, 165, 76, 0.12)` | Background tint for jurisdiction-sensitive flags (shares orange family) |
| `--discovery-heading` | `rgba(94, 196, 212, 0.15)` | Discovery evidence: heading signal indicator |
| `--discovery-cluster` | `rgba(176, 126, 232, 0.15)` | Discovery evidence: clustering signal indicator |
| `--discovery-llm` | `rgba(108, 140, 255, 0.15)` | Discovery evidence: LLM signal indicator |
| `--diff-added` | `rgba(76, 175, 124, 0.12)` | Diff view: added tasks/units background |
| `--diff-removed` | `rgba(224, 85, 85, 0.12)` | Diff view: removed tasks/units background |
| `--diff-changed` | `rgba(232, 165, 76, 0.12)` | Diff view: modified tasks/units background |

Accent reserved for: selected tree node border + background, active nav tab underline, primary "Discover Tasks" CTA button, "Edit" outline button, focus-visible ring, active filter chip, active stage pill, progress bar fill (processing state).

---

## Component Inventory

### New Components (Phase 2)

| Component | File | Purpose |
|-----------|------|---------|
| TaskTree | `viewer/src/lib/components/TaskTree.svelte` | Merged FOLIO+task tree using @keenmate/svelte-treeview with DnD, badges, review progress indicators, search, "Tasks only" / "All concepts" toggle |
| TaskDetail | `viewer/src/lib/components/TaskDetail.svelte` | Upper-right detail pane: task summary, knowledge units grouped by type (Best Practices, Principles, Pitfalls, Procedural Rules, Citations), confidence badges, contradiction highlights |
| ContradictionView | `viewer/src/lib/components/ContradictionView.svelte` | Side-by-side contradiction display with source references, resolution controls (keep both / prefer A / prefer B / merge / jurisdiction) |
| DiscoveryEvidence | `viewer/src/lib/components/DiscoveryEvidence.svelte` | Lower-right source pane: heading context signal, clustered knowledge units, source passages that led to task creation |
| FilterToolbar | `viewer/src/lib/components/FilterToolbar.svelte` | Horizontal bar of filter chips: unit type, confidence band, review status, flags (contradictions, orphans, jurisdiction-sensitive) |
| TaskDashboard | `viewer/src/lib/components/TaskDashboard.svelte` | Summary statistics panel: total tasks/subtasks, unit distribution by type, source coverage, confidence distribution, contradiction count, review progress |
| ManualTaskDialog | `viewer/src/lib/components/ManualTaskDialog.svelte` | Modal dialog (extends ConfirmDialog pattern) for creating manual tasks: name input, FOLIO parent picker, unit assignment |
| DiffView | `viewer/src/lib/components/DiffView.svelte` | Re-run diff panel: new tasks discovered, units added/removed, hierarchy changes suggested, with accept/reject per change |
| DiscoverButton | `viewer/src/lib/components/DiscoverButton.svelte` | "Discover Tasks" CTA button with disabled/processing/complete states (mirrors ProcessButton pattern) |
| DiscoveryProgress | `viewer/src/lib/components/DiscoveryProgress.svelte` | Discovery-specific ProgressDisplay with 6 stage pills (Heading Analysis, FOLIO Mapping, Content Clustering, Hierarchy Construction, Cross-Source Merging, Contradiction Detection) |

### Extended Components (Phase 2 modifications)

| Component | Changes |
|-----------|---------|
| NavTabs | Add "Tasks" tab between "Upload" and "Review" (3 tabs total) |
| +layout.svelte | Add `activePage === 'tasks'` routing logic |
| upload/+page.svelte | Add DiscoverButton below ProcessButton, visible after processing completes; add DiscoveryProgress for task discovery SSE |
| +page.svelte (review) | Conditionally render TaskTree/TaskDetail/DiscoveryEvidence OR existing FolioTree/DetailView/SourceContext based on view mode |

### Reused Without Changes

| Component | Reuse Context |
|-----------|--------------|
| ConfidenceBadge | Task confidence display (same green/orange/red bands) |
| ConfirmDialog | Delete task confirmation, merge task confirmation, contradiction merge text input |
| LoadingSkeleton | Task tree loading, task detail loading |
| ActivityLog | Discovery pipeline activity log |
| KeyboardShortcuts | Extended shortcuts modal (add m=move, g=merge to existing list) |

---

## Layout Specifications

### Tasks Page (new route: `/tasks`)

Three-pane layout identical to review page structure:

```
+-------+----------------------------+
| Task  |      Task Detail           |
| Tree  |   (grouped units, review   |
| (left)|   controls, filter bar)    |
|  280- |                            |
|  360px+----------------------------+
|       |   Discovery Evidence       |
|       |   (heading context,        |
|       |    clustering signal,      |
|       |    source passages)        |
+-------+----------------------------+
```

- **Left pane:** TaskTree, initial width 320px, minimum 280px, maximum 480px
- **Upper-right pane:** FilterToolbar (sticky top) + TaskDetail, initial height 60%
- **Lower-right pane:** DiscoveryEvidence, initial height 40%
- **Dividers:** Same 4px draggable dividers with `--accent` hover color (established pattern)
- **Dashboard:** Toggle-able overlay panel, positioned top-right below header, 480px wide, max 70vh tall, shadow `0 8px 32px rgba(0, 0, 0, 0.4)`

### Primary Focal Point (first load)

When the Tasks page loads with a corpus selected and tasks discovered:
- **The first task node in the tree is auto-selected.** The tree scrolls to top, the first task node receives `--highlight` background and `--accent` left border, and its detail pane populates with that task's content.
- This ensures the user sees a populated detail pane immediately rather than an empty "No task selected" state.

When the Tasks page loads with no corpus selected:
- **The empty state in the tree pane is the focal point.** The "No corpus selected" heading is centered vertically at 18px/600, drawing the eye to the action required.

When the Tasks page loads with a corpus selected but no tasks discovered:
- **The empty state message is the focal point**, with a "Discover Tasks" inline link/button styled in `--accent` to guide the user to the upload page.

### Upload Page Extension

After processing completes, the upload content area gains:

```
[ProgressDisplay - complete state]
[Success text]
[DiscoverButton - "Discover Tasks"]     <-- NEW
[DiscoveryProgress - 6 stage pills]     <-- NEW (visible during discovery)
[ActivityLog - discovery activities]     <-- NEW (visible during discovery)
```

The DiscoverButton replaces the ProcessButton's position in the vertical flow. It appears only when `processingStatus === 'complete'` and discovery has not yet completed.

---

## TaskTree Visual Spec

### Node Anatomy

```
[chevron 12px] [label 14px] [unit_count 11px dim] [review_indicator] [flags]
```

- **Task nodes (is_task: true):** Label at 14px weight 400, `color: var(--text)`, unit count badge in parentheses `(23)` at 11px in `--text-dim`, review indicator (checkmark / partial / empty circle), optional flag icons
- **Structural ancestor nodes (is_task: false):** Label at 14px weight 400, `color: var(--text-dim)`, no badge, no review indicator -- visible for hierarchy context only
- **Selected node:** `background: var(--highlight)`, `border-left: 2px solid var(--accent)`, label color `var(--accent)`
- **Node height:** 28px (established)
- **Indentation:** 16px per depth level (established child-node padding pattern)
- **DnD active state:** Dragged node gets `opacity: 0.6` + `outline: 1px dashed var(--accent)`. Drop target shows 2px `--accent` line at above/below position, or `background: var(--highlight)` for "drop as child"

**Task vs ancestor distinction:** Task nodes are distinguished from structural ancestors solely by text color (`--text` vs `--text-dim`), the presence of the unit count badge, and the review indicator. No weight difference is needed.

### Review Progress Indicators (per node)

| State | Visual | Size |
|-------|--------|------|
| Complete (all approved) | Filled circle in `--green` | 8px |
| Partial (some reviewed) | Half-filled circle: left half `--green`, right half `--surface3` | 8px |
| Unreviewed | Empty circle outline in `--text-dim` | 8px |

### Flag Indicators (inline, after review indicator)

| Flag | Visual | Color | Accessible Label |
|------|--------|-------|-----------------|
| Has contradictions | Inline SVG: two arrows pointing at each other | `--red` | `title="Has contradictions"` + `aria-label="Has contradictions"` |
| Has orphan units | Inline SVG: question mark in circle | `--orange` | `title="Has orphan units"` + `aria-label="Has orphan units"` |
| Jurisdiction-sensitive | Inline SVG: map pin | `--orange` | `title="Jurisdiction-sensitive"` + `aria-label="Jurisdiction-sensitive"` |
| Manually created | Inline SVG: pencil | `--text-dim` | `title="Manually created"` + `aria-label="Manually created"` |
| Procedural task | Inline SVG: numbered list | `--purple` | `title="Procedural task"` + `aria-label="Procedural task"` |

Flag icons: 12px (minimum touch-friendly size for inline indicators), rendered inline at the end of the node row with 4px gaps. Each SVG element carries both `title` (for mouse hover tooltip) and `aria-label` (for screen readers) attributes.

### Toggle Control

Position: Top of tree pane, below search input, above tree nodes.

```
[Tasks only] [All concepts]
```

Two-button toggle group (same pattern as view-toggle in +page.svelte): `background: var(--surface2)`, `border: 1px solid var(--border)`, `border-radius: 6px`, active state `background: var(--accent)`, `color: #fff`. Font: 11px uppercase, letter-spacing 0.5px.

---

## TaskDetail Visual Spec

### Task Header

```
[Task label - 18px 600]
[FOLIO IRI - 11px monospace dim + copy button]
[Task type badge: "Procedural" or "Categorical"]
[Confidence badge - reuse ConfidenceBadge component]
```

- Task type badge: Pill at 11px uppercase, `letter-spacing: 0.5px`
  - Procedural: `background: rgba(176, 126, 232, 0.15)`, `color: var(--purple)`
  - Categorical: `background: var(--surface3)`, `color: var(--text-dim)`

### Unit Type Groups

Below the header, knowledge units are grouped by type. Each group has a collapsible section:

```
[chevron] Best Practices (12)            [Approve All]
  [unit card]
  [unit card]
  ...
[chevron] Principles (8)                 [Approve All]
  [unit card]
  ...
[chevron] Pitfalls (5)                   [Approve All]
  [unit card]
  ...
[chevron] Procedural Rules (3)           [Approve All]
  [unit card]
  ...
[chevron] Citations (7)                  [Approve All]
  [unit card]
  ...
```

- Group header: 14px weight 600, `color: var(--text)`, group count in `--text-dim`
- "Approve All" button per group: 11px, `color: var(--green)`, right-aligned, visible on hover
- Unit cards: Same visual pattern as existing DetailView unit cards (surface2 background, border, 6px radius, status dot, confidence badge, type tag, extraction path badges)
- Contradiction-flagged units: Left border `2px solid var(--red)` + subtle background tint `var(--contradiction)`

### Review Controls (per selected unit)

Reuse existing ReviewControls pattern. Extended toolbar for task context:

```
[Approve] [Reject] [Edit] [Move (M)] [Skip Unit] [Approve All High]
```

- "Move" button: `background: transparent`, `border: 1px solid var(--orange)`, `color: var(--orange)` -- triggers DnD mode or task picker dropdown

---

## ContradictionView Visual Spec

Renders inside TaskDetail when a contradiction is selected or when viewing a task with contradictions.

### Side-by-Side Layout

```
+----------------------------+----------------------------+
| Position A                 | Position B                 |
| [source badge: Mauet Ch.5] | [source badge: Lubet Ch.3] |
| [authority: 8/10]          | [authority: 6/10]          |
|                            |                            |
| "Always ask open-ended     | "Begin cross-examination   |
|  questions on cross..."    |  with leading questions..." |
|                            |                            |
| [confidence: 82]           | [confidence: 74]           |
+----------------------------+----------------------------+
| Contradiction type: partial                             |
| NLI score: 0.87                                         |
+--[Keep Both]--[Prefer A]--[Prefer B]--[Merge Statement]--[Mark Jurisdictional]-+
```

- Two columns, each with `background: var(--contradiction)` tint
- Source badges: Pill style, `background: var(--surface3)`, 11px
- Authority level: Bar or number display, `N/10`, color scaled from `--text-dim` (low) to `--green` (high)
- Resolution buttons: Row of outline buttons, same height (36px) and style as ReviewControls
  - "Keep Both": `border: 1px solid var(--text-dim)`, `color: var(--text-dim)`
  - "Prefer A": `border: 1px solid var(--accent)`, `color: var(--accent)`
  - "Prefer B": `border: 1px solid var(--accent)`, `color: var(--accent)`
  - "Merge Statement": `border: 1px solid var(--orange)`, `color: var(--orange)` -- opens inline text editor
  - "Mark Jurisdictional": `border: 1px solid var(--orange)`, `color: var(--orange)`
- NLI score: Displayed as percentage, 11px, `color: var(--text-dim)`, with label "NLI: 87%"
- Contradiction type label: 11px uppercase pill, `letter-spacing: 0.5px`
  - "full": `color: var(--red)`, `background: rgba(224, 85, 85, 0.15)`
  - "partial": `color: var(--orange)`, `background: rgba(232, 165, 76, 0.15)`
  - "jurisdictional": `color: var(--orange)`, `background: rgba(232, 165, 76, 0.15)`

---

## FilterToolbar Visual Spec

Horizontal bar, sticky at the top of the TaskDetail pane, below the task header.

```
[Type: Best Practices | Principles | Pitfalls | Rules | Citations]
[Confidence: High | Medium | Low]
[Status: Unreviewed | Approved | Rejected]
[Flags: Contradictions | Orphans | Jurisdiction]
```

### Filter Chip Style

- Inactive: `background: var(--surface2)`, `color: var(--text-dim)`, `border-radius: 9999px`, `padding: var(--xs) var(--sm)` (4px 8px), `font-size: 11px`
- Active: `background: var(--highlight)`, `color: var(--accent)`, `border: 1px solid var(--accent-dim)`
- Hover (inactive): `color: var(--text)`
- Group labels ("Type:", "Confidence:", etc.): 11px uppercase, `letter-spacing: 0.5px`, `color: var(--text-dim)`, `margin-right: var(--xs)` (4px)
- Groups separated by `var(--sm)` (8px) gap; chips within group separated by `var(--xs)` (4px) gap
- Entire toolbar: `padding: var(--sm) var(--md)`, `border-bottom: 1px solid var(--border)`, `background: var(--surface)`, `flex-wrap: wrap`, `gap: var(--sm)`

---

## DiscoveryEvidence Visual Spec

Lower-right pane (replaces SourceContext when in task view).

### Header

Same pattern as SourceContext header: `padding: var(--sm) var(--md)`, `border-bottom: 1px solid var(--border)`.

```
Discovery Evidence    [Heading] [Clustering] [LLM]
```

Signal toggle pills (same style as stage pills): filter which evidence type is shown.

### Content Sections

**Heading Signal:**
- Breadcrumb path: `Ch. 5 > Cross-Examination > Leading Questions` at 11px in `--text-dim`
- Heading context confidence: ConfidenceBadge
- Background tint: `var(--discovery-heading)`

**Clustering Signal:**
- List of co-clustered knowledge units (truncated to first 5, expandable)
- Cluster ID and distance metric at 11px
- Background tint: `var(--discovery-cluster)`

**LLM Signal:**
- LLM task label suggestion
- LLM reasoning text at 14px
- Background tint: `var(--discovery-llm)`

Each section: `padding: var(--sm) var(--md)`, `border-bottom: 1px solid var(--border)`, section title at 14px weight 600.

---

## TaskDashboard Visual Spec

Overlay panel, toggled by a dashboard icon button in the header-right area.

### Layout

Grid of stat cards, 2 columns, `gap: var(--md)`.

### Stat Card Anatomy

```
[Metric label - 11px uppercase dim]
[Value - 28px 600]
[Subtext - 11px dim]
```

- Card: `background: var(--surface2)`, `border: 1px solid var(--border)`, `border-radius: 6px`, `padding: var(--md)`
- Metric label: 11px uppercase, `letter-spacing: 0.5px`, `color: var(--text-dim)`
- Value: 28px weight 600, `color: var(--text)` (or semantic color for certain metrics)
- Subtext: 11px, `color: var(--text-dim)`

### Metrics

| Card | Metric Label | Value Color | Subtext |
|------|-------------|-------------|---------|
| Total Tasks | "TASKS" | `--text` | "{N} top-level, {N} subtasks" |
| Knowledge Units | "UNITS ASSIGNED" | `--text` | "{N} orphans remaining" (orange if > 0) |
| Review Progress | "REVIEW PROGRESS" | `--green` | "{N}% complete" |
| Contradictions | "CONTRADICTIONS" | `--red` if unresolved > 0, else `--green` | "{N} resolved, {N} pending" |
| Source Coverage | "SOURCE COVERAGE" | `--text` | "{N}/{N} files represented" |
| Confidence | "AVG CONFIDENCE" | Semantic color based on band | Distribution bar (green/orange/red segments) |

Distribution bar: 4px height, `border-radius: 2px`, three segments colored by confidence band, proportional width.

---

## DiffView Visual Spec

Displayed inline within the TaskDetail area when a re-run is detected and new changes are available.

### Change Row Anatomy

```
[icon] [change description - 14px]     [Accept Change] [Reject Change]
```

- Added (new task/unit): `background: var(--diff-added)`, green "+" icon
- Removed (task/unit removed): `background: var(--diff-removed)`, red "-" icon
- Changed (hierarchy modification): `background: var(--diff-changed)`, orange "~" icon
- "Accept Change" button: `color: var(--green)`, outline style
- "Reject Change" button: `color: var(--red)`, outline style
- "Accept All Changes" / "Reject All Changes" bulk actions at top: same pattern as "Approve All" in ReviewControls

---

## DiscoverButton Visual Spec

Mirrors the existing ProcessButton pattern.

| State | Background | Text | Behavior |
|-------|-----------|------|----------|
| Ready | `var(--accent)` | "Discover Tasks" | Clickable, triggers discovery pipeline |
| Disabled (no processed corpus) | `var(--surface3)` | "Discover Tasks" | `opacity: 0.5`, `cursor: not-allowed` |
| Processing | `var(--accent)` | "Discovering..." | Disabled, shows spinner animation |
| Complete | `var(--green)` | "Tasks Discovered" | Transitions to link/button navigating to /tasks |

Button: `height: 36px`, `padding: 0 var(--md)`, `border-radius: 4px`, `font-size: 14px`, `font-weight: 600`.

---

## DiscoveryProgress Visual Spec

Same layout as existing ProgressDisplay. Different stages:

```
[Heading Analysis] [FOLIO Mapping] [Content Clustering] [Hierarchy Construction] [Cross-Source Merging] [Contradiction Detection]
```

Pill states: pending (surface2/text-dim), active (accent bg 20%/accent), complete (green bg 20%/green), error (red bg 20%/red). Exactly the same CSS as existing stage pills.

Progress bar: 3px height, `border-radius: 2px`, same color logic (accent during processing, green on complete, red on error).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Discover Tasks" |
| Secondary CTA (upload page, post-discovery) | "Review Task Tree" |
| Empty state heading (task tree, no corpus) | "No corpus selected" |
| Empty state body (task tree, no corpus) | "Select a corpus from the sidebar to view its task hierarchy." |
| Empty state heading (task tree, no tasks) | "No tasks discovered" |
| Empty state body (task tree, no tasks) | "Run task discovery on a processed corpus to build the advocacy task hierarchy." |
| Empty state heading (task detail, no selection) | "No task selected" |
| Empty state body (task detail, no selection) | "Select a task from the tree to view its knowledge units and review status." |
| Empty state (discovery evidence, no selection) | "Select a task to view its discovery evidence." |
| Empty state (filter yields no results) | "No units match the current filters." |
| Error state (discovery pipeline) | "Task discovery failed. Check the activity log for details and retry." |
| Error state (tree load failure) | "Could not load task tree. The server may be unreachable." |
| Destructive: delete task | Delete Task: "This will remove the task and reassign its {N} knowledge units to orphan status. This cannot be undone." |
| Destructive: merge tasks | Merge Tasks: "This will merge {task_b} into {task_a}. All knowledge units and subtasks will be moved. This cannot be undone." |
| Destructive: reject all low-confidence | Reject All Low: "This will reject {N} task assignments with confidence below 50%. You can undo individual rejections later." |
| Contradiction resolution | "How should this contradiction be resolved?" |
| Manual task creation | "Create a new task manually. Name it, choose its parent in the FOLIO hierarchy, and assign knowledge units." |
| Diff view header | "Changes detected from re-run. Review and accept or reject each change." |
| Dashboard toggle tooltip | "Task summary dashboard" |
| DiffView: accept action | "Accept Change" |
| DiffView: reject action | "Reject Change" |
| DiffView: bulk accept | "Accept All Changes" |
| DiffView: bulk reject | "Reject All Changes" |
| ContradictionView: keep both | "Keep Both" |
| ContradictionView: prefer A | "Prefer A" |
| ContradictionView: prefer B | "Prefer B" |
| ContradictionView: merge | "Merge Statement" |
| ContradictionView: jurisdiction | "Mark Jurisdictional" |
| ReviewControls: skip | "Skip Unit" |
| Processing stage: heading_analysis | "Heading Analysis" |
| Processing stage: folio_mapping | "FOLIO Mapping" |
| Processing stage: content_clustering | "Content Clustering" |
| Processing stage: hierarchy_construction | "Hierarchy Construction" |
| Processing stage: cross_source_merging | "Cross-Source Merging" |
| Processing stage: contradiction_detection | "Contradiction Detection" |
| SSE complete message | "Task discovery complete. {N} tasks discovered, {N} contradictions detected." |
| Orphan batch review | "Review {N} orphan units assigned to their nearest task." |
| Keyboard shortcut: Move (M) | "Move task to new parent" |
| Keyboard shortcut: Merge (G) | "Merge selected task into another" |

---

## Interaction Contracts

### Keyboard Shortcuts (extending Phase 1)

| Key | Context | Action |
|-----|---------|--------|
| a / A | Task tree or detail focused | Approve selected task/unit |
| r / R | Task tree or detail focused | Reject selected task/unit |
| e / E | Detail focused | Open inline editor for selected unit |
| m / M | Task tree focused | Enter move mode (DnD keyboard alternative) |
| g / G | Task tree focused | Open merge dialog for selected task |
| j / ArrowDown | Any | Navigate to next task/unit |
| k / ArrowUp | Any | Navigate to previous task/unit |
| ArrowRight | Task tree focused | Expand selected node |
| ArrowLeft | Task tree focused | Collapse selected node |
| Tab | Any | Cycle pane focus: tree -> detail -> evidence |
| ? | Any | Toggle keyboard shortcuts modal |
| Shift+A | Any | Bulk approve all high-confidence tasks |
| Ctrl+F / Cmd+F | Any | Focus tree search input |
| Escape | Any | Close editor, modal, or DnD mode |
| 1 / 2 / 3 / 4 | Any | Confidence filter: all / high / medium / low |

### Drag-and-Drop Interactions

| Action | Trigger | Visual Feedback | Result |
|--------|---------|-----------------|--------|
| Move subtask | Drag tree node | Ghost node at 60% opacity, 2px accent line at drop position | Task reparented; hierarchy_edits record created |
| Reassign unit | Drag unit card from detail pane onto tree node | Unit card ghost, tree node highlight | New task_unit_link created (multi-task); source assignment preserved |
| Reorder subtask | Drag tree node above/below sibling | 2px accent line between siblings | canonical_order updated |
| Drop rejected | Drop on invalid target (e.g., onto itself, onto a non-task ancestor) | Red flash on target, snap back | No change |

### State Transitions

**Upload page flow:**
```
Upload files -> Process -> [Processing...] -> Complete
    -> Discover Tasks -> [Discovering...] -> Complete
    -> "Review Task Tree" link navigates to /tasks
```

**Task review states per node:**
```
unreviewed -> approved (via A key or button)
unreviewed -> rejected (via R key or button)
unreviewed -> edited (via E key, then save)
approved -> rejected (re-review)
rejected -> approved (re-review)
any -> moved (via M key or DnD)
any -> merged (via G key or merge dialog)
```

---

## Accessibility Requirements

- All tree nodes: `role="treeitem"`, `aria-expanded`, `aria-selected`, `tabindex="0"`
- Tree container: `role="tree"`, `aria-label="Task hierarchy tree"`
- Filter chips: `role="checkbox"`, `aria-checked`, grouped in `role="group"` with `aria-label`
- Contradiction resolution buttons: `role="radiogroup"` with `aria-label="Contradiction resolution"`
- DnD: `aria-grabbed`, `aria-dropeffect` attributes during drag operations
- All modals: `role="alertdialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`, focus trap
- Screen reader announcements: `aria-live="polite"` region for all state changes (discovery progress, review actions, DnD results)
- `prefers-reduced-motion`: disable all transition/animation durations (established in app.css)
- Focus-visible: 2px solid `var(--accent)` outline with 2px offset (established)
- Minimum touch target: 44px for interactive elements on tree nodes and filter chips
- All tree flag icons: must include both `title` attribute (hover tooltip) and `aria-label` attribute (screen reader) with descriptive text (see Flag Indicators table above)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| npm (standard) | @keenmate/svelte-treeview@4.8.0 | Standard npm package; reviewed in RESEARCH.md; no shadcn registry vetting required |

No shadcn registries are used. No third-party component registries. The project uses a custom CSS variable design system with hand-built Svelte 5 components. The only new dependency is the npm treeview package identified in RESEARCH.md.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
