---
milestone: v1.1
milestone_name: post-v1.0 Railway dev deploy + UAT gap closure
audited: 2026-04-20T22:10:00Z
status: passed
scores:
  requirements: 5/5
  phases: 2/2
  integration: 8/8
  flows: 4/4
phases_in_scope:
  - 01-deploy-on-railway-as-dev-server
  - 02-uat-gap-fixes
plans_in_scope: 8
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-deploy-on-railway-as-dev-server
    items:
      - "Image size 8.67 GB dominated by torch + NVIDIA CUDA; deploys fine but cold-pull is slow. Deferred: CPU-only torch pin to reach ~1–2 GB."
      - "fastapi / uvicorn[standard] / python-multipart installed via Dockerfile belt-and-suspenders line; not declared in pyproject.toml as direct deps. Deferred hardening."
      - "GitHub auto-redeploy was disconnected mid-milestone (discovered during 02-05 verification) and reconnected manually. No codified check that Railway service.source != null before assuming autodeploy works."
      - "output/default/ has no corpus-meta.json — catalog loader skips it silently. Safe-to-delete or backfill."
  - phase: 02-uat-gap-fixes
    items:
      - "Full UAT re-run against live Railway URL for tests 14–15, 19, 24–28, 30, 33–36, 37–41, 47 deferred to /gsd-verify-work (not this audit). SUMMARY evidence shows fixes landed; UI regression sweep outstanding."
      - "pytest default folio_enrich_path = '../folio-enrich/backend' breaks in worktrees — requires FOLIO_INSIGHTS_FOLIO_ENRICH_PATH env var. Pre-existing, not introduced here."
      - "OWL/TTL/JSONLD/HTML/SHACL export paths only exercised via unit tests, not the new output/demo/ fixture (plan 02-04 covered markdown only)."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 2
  overall: missing
  note: "Nyquist validation enabled globally; no *-VALIDATION.md in either phase directory. Neither phase is a frontend-only or AI-behaviour phase — Nyquist is typically not wired retroactively for deploy + bug-fix phases. Flagging for awareness; not treating as a blocker given `requirements: none` in both phases and the UAT-driven issue-closure model."
verification_artifacts:
  missing_verification_md:
    - 01-deploy-on-railway-as-dev-server (no 01-VERIFICATION.md)
    - 02-uat-gap-fixes (no 02-VERIFICATION.md)
  substituted_by: "Per-plan SUMMARY.md frontmatter + Self-Check sections + COMPREHENSIVE-UAT.md issue closure + live Railway curl evidence in 01-03 and 02-05 SUMMARYs"
---

# Milestone v1.1 — Audit Report

**Scope:** Post-v1.0 Railway dev deploy (Phase 01) + UAT gap closure for 5 issues surfaced in COMPREHENSIVE-UAT.md (Phase 02).
**Plans shipped:** 8 (3 in Phase 01, 5 in Phase 02)
**Final status:** **passed** — all 5 UAT-sourced requirements satisfied, cross-phase wiring verified, live URL smoke-tests green.

## Requirements Coverage (UAT-sourced I-1 … I-5)

Both phases declare `requirements: none` in their frontmatter — this milestone has no formal REQ-IDs. UAT issues I-1 through I-5 from `.planning/phases/COMPREHENSIVE-UAT.md` serve as the de-facto definition of done. Phase 01 predates the UAT sweep and is verified by its own plan-level SUMMARYs.

| Req | Description | Plan | Verification Status | Evidence |
|-----|-------------|------|---------------------|----------|
| I-1 | LLM-path FOLIO tags must resolve to canonical IRIs (threshold issue) | 02-03 | **satisfied** | `folio_tagger.py:50` `_FOLIO_LABEL_RESOLUTION_THRESHOLD = 0.6`; `:291` routes unresolved → `extraction_path="proposed_class"`; 4 new regression tests; full suite 203 passed |
| I-2 | Bundle export 422 → 404 parity with sibling export endpoints | 02-02 | **satisfied** | `api/routes/export.py:307` `body: BundleRequest \| None = None`; 404 at `:319-320` raised before body defaulting; +2 regression tests (199 passed) |
| I-3 | Vite proxy target 8700 → 9925 (dev viewer → API) | 02-01 | **satisfied** | `viewer/vite.config.ts:15` `target: 'http://localhost:9925'`; `client.ts` unchanged (`API_BASE = ''`); viewer/src grep for `:8700`/`:5173` clean |
| I-4 | output/demo/ fixture with ≥1 approved task for export validation | 02-04 | **satisfied** | `output/demo/` ships `corpus-meta.json`, `extraction.json`, `review.db` (2 approved + 2 links), `sources/demo-advocacy.md`; `scripts/seed_demo_corpus.py` idempotent; `.gitignore` whitelists `!output/demo/` |
| I-5 | Railway `processing_status` should show `completed` for bundled corpora | 02-05 | **satisfied** | `.dockerignore:46-47` excludes `output/.jobs` + `output/.jobs/**`; live curl (SUMMARY 02-05) shows both `test1` and `demo` `processing_status=completed`; `/health` 200; `/` 200; deploy `a80bcb75` success |

Phase 01 definition of done (per 01-CONTEXT + ROADMAP): single Railway dev service, live `*.up.railway.app` URL, bundled viewer + API, redeploy on push — **satisfied** via 01-03 SUMMARY live verification at `https://folio-insights-production.up.railway.app`.

## Phase-Level Results

### Phase 01 — Deploy on Railway as Dev Server
**Status:** complete (3/3 plans, completed 2026-04-13)

| Plan | Deliverable | Status | Notes |
|------|-------------|--------|-------|
| 01-01 | Multi-stage Dockerfile + .dockerignore + /health | PASS | Docker smoke green; image 8.67 GB (torch+CUDA dominated) |
| 01-02 | railway.toml + relative-URL audit | PASS | TOML parsed; client.ts `API_BASE = ''` preserved; rebuild smoke green |
| 01-03 | Live deploy + README + STATE docs | PASS | `/health` 200, `/` 200, `/api/v1/corpora` 200 on live URL |

### Phase 02 — UAT Gap Fixes
**Status:** complete (5/5 plans; Wave 1 on 2026-04-19, Wave 2 on 2026-04-20)

| Plan | Issue | Status | Regression Net |
|------|-------|--------|----------------|
| 02-01 | I-3 vite proxy | PASS | +0 tests |
| 02-02 | I-2 bundle-export 404 parity | PASS | +2 tests (197 → 199) |
| 02-03 | I-1 FOLIO threshold + proposed_class routing | PASS | +4 tests (199 → 203) |
| 02-04 | I-4 output/demo/ fixture | PASS | +0 tests; idempotent seed verified |
| 02-05 | I-5 .dockerignore exclude output/.jobs/ | PASS | +0 tests; live URL verification |

Final pytest state from 02-03 SUMMARY: **203/203 passed**.

## Cross-Phase Integration Check

Integration checker (gsd-integration-checker semantics, general-purpose agent) verified 8 wiring points against master HEAD:

| # | Check | Result |
|---|-------|--------|
| 1 | `.dockerignore` keeps `output/` bundled but excludes `output/.jobs[/**]` | PASS (`.dockerignore:46-47`) |
| 2 | `.gitignore` whitelists `!output/demo/` + all 5 demo fixture files exist | PASS (`.gitignore:25-27`) |
| 3 | Dockerfile invariants preserved post-02-05 (multi-stage, non-root, HEALTHCHECK, PORT, COPY output/) | PASS (`Dockerfile:6,23,53,59,68-72`) |
| 4 | Vite proxy 9925 + relative `API_BASE=''` + no residual `:8700`/`:5173` in viewer/src | PASS |
| 5 | `export_bundle` 404 raised before body defaulting | PASS (`api/routes/export.py:319-320`) |
| 6 | `_FOLIO_LABEL_RESOLUTION_THRESHOLD = 0.6` + `proposed_class` fallback | PASS (`folio_tagger.py:50, :291`) |
| 7 | `railway.toml` builder/healthcheck invariants intact | PASS |
| 8 | `/health` route registered before StaticFiles catch-all | PASS (`api/main.py:33-36` vs mount at `:116`) |

No cross-phase integration issues found.

## End-to-End Flow Coverage

| Flow | Status | Evidence |
|------|--------|----------|
| Dev viewer → API via Vite proxy | PASS | client relative URLs + proxy 9925 aligned with bootup |
| Railway build → live `/api/v1/corpora` with `demo`+`test1` completed | PASS (static + live curl) | `.dockerignore` scrubs `.jobs/` only; Dockerfile ships `output/`; 02-05 live curl confirms both corpora `completed` |
| Bundle export on empty corpus → 404 parity | PASS | Matches sibling owl/ttl/jsonld/validation 404 detail string |
| LLM extraction → populated IRI or `proposed_class` routing | PASS | Threshold + fallback enforced; 4 regression tests |

## Tech Debt (Non-Blocking)

### Phase 01
- **Image size 8.67 GB** dominated by torch + NVIDIA CUDA transitives from sentence-transformers. Deferred: CPU-only torch pin to ~1–2 GB.
- **FastAPI / uvicorn / python-multipart** installed via Dockerfile belt-and-suspenders; not declared in `pyproject.toml`. Optional hardening.
- **Railway autodeploy** disconnected mid-milestone (discovered during 02-05 verification); reconnected manually. No codified pre-deploy check that `service.source != null`.
- **`output/default/`** has no `corpus-meta.json` — catalog loader silently skips it. Safe to delete or backfill.

### Phase 02
- **Full UAT re-run** against live URL for tests 14–15, 19, 24–28, 30, 33–36, 37–41, 47 deferred to `/gsd-verify-work 2`. SUMMARY evidence indicates fixes landed; UI regression sweep outstanding.
- **Worktree pytest env quirk**: default `folio_enrich_path='../folio-enrich/backend'` requires `FOLIO_INSIGHTS_FOLIO_ENRICH_PATH` in worktrees. Pre-existing; not introduced here.
- **Export-format coverage** in `output/demo/`: plan 02-04 exercised markdown export only. OWL/TTL/JSONLD/HTML/SHACL paths covered by unit tests but not by the new fixture end-to-end.

## Verification Artifact Gaps (Informational)

Neither phase produced a top-level `*-VERIFICATION.md`. Coverage was instead aggregated from:
- Per-plan `*-SUMMARY.md` files (all 8 present, all Self-Check PASSED)
- `.planning/phases/COMPREHENSIVE-UAT.md` — issue catalogue with severity + repro steps
- Live Railway curl evidence embedded in 01-03 and 02-05 SUMMARYs

Nyquist validation is enabled globally but neither phase has a `*-VALIDATION.md`. Given both phases carry `requirements: none` (UAT-issue-driven rather than REQ-ID-driven) and represent deploy + bug-fix work rather than frontend/AI-behaviour phases, the absence is recorded but not treated as a blocker. Run `/gsd-validate-phase 01` and `/gsd-validate-phase 02` if retroactive Nyquist coverage is desired.

## Verdict

**passed** — milestone v1.1 achieved its definition of done. All 5 UAT issues closed with code + tests + live-URL evidence; zero integration gaps; zero broken flows; tech debt is identifiable and non-blocking.

Proceed to `/gsd-complete-milestone 1.1`.
